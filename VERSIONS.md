# BIDCOMAGRO-SYSTEM — Control de Versiones

Cada archivo tiene un comentario `// @version X.Y` (JS) o `<!-- @version X.Y -->` (HTML).
Regla: **incrementar la versión cada vez que se edita un archivo**.
- Corrección de bug → X.Y+1 (ej: 1.0 → 1.1)
- Feature nueva → X+1.0 (ej: 1.3 → 2.0)

---

## ComandasPedidos
| Archivo | Versión | Notas |
|---------|---------|-------|
| CP_Código.js | 1.43 | FIX "CP_diagAutoMail queda cargando y no muestra nada": el paso 4 llamaba _cpDetalleVenta(k) (relee TODA la hoja Ventas) + _cpDestinatariosEnvio (relee Resellers y RTV) por CADA envío con guía sin mail → con un backlog grande de envíos pendientes hacía N lecturas completas y se colgaba (o cortaba a los 6 min sin loguear, porque el Logger.log está al final). Ahora lee cada hoja UNA sola vez antes del loop: nuevo _cpVentaResellerRtvMap() (mapa liviano IDVENTA→{reseller,rtv} de 1 lectura de Ventas) y _cpDestinatariosEnvio(det,cfg,resellerMap,rtvMap) acepta los mapas ya leídos (retrocompatible: si no se pasan, lee como antes). El diagnóstico da el mismo resultado, en tiempo constante. No cambia el flujo de envío real |
| CP_Código.js | 1.42 | Los FALTANTES ya no van en los mails: se quitó el bloque de pendientes tanto del mail de despacho (reseller+RTV, _cpMailHtml) como del de autorización (Sole, _cpMailAprobacionHtml). Ahora cada mail muestra SOLO lo que se envía en esa comanda (datos + detalle despachado/a cargar). Decisión del usuario: sacarlo para TODOS los destinatarios del despacho (reseller y RTV comparten un único correo). NO se toca el seguimiento de pendientes: _cpPendingVenta/_cpSyncPendientesEntrega y la hoja PENDIENTES_ENTREGA siguen igual, y las solapas "Con faltantes"/pendientes de la UI no cambian. El param 'pendientes' queda sin usar en ambas funciones y _cpPendBloqueHtml queda definido pero sin llamadas (código muerto inofensivo). La vista previa refleja lo mismo (usa _cpMailHtml) |
| CP_Código.js | 1.41 | "Cliente" → "Razón Social" en todos lados (el campo siempre fue la razón social, no un cliente distinto): los dos chips del mail (despacho al reseller y aprobación a Sole) ahora dicen "Razón Social" (el valor ya era det.razonSocial); encabezado de la hoja PENDIENTES_ENTREGA pasa de 'Cliente' a 'Razón Social' (col F, solo afecta hojas nuevas; se lee por índice, no por nombre). El placeholder {CLIENTE} del asunto NO se toca (es un token interno; su valor resuelto ya es la razón social) para no romper asuntos ya configurados en _CONFIG |
| CP_Código.js | 1.40 | FIX emojis del mail que llegaban rotos ("������") en TODOS los correos: el transporte de GmailApp mangla los caracteres no-ASCII del htmlBody (sobre todo emojis de plano astral, 4 bytes). Nuevo helper _cpHtmlAscii(s) convierte toda la parte no-ASCII del HTML a referencias numéricas (&#NNN;, ej. 📦→&#128230;), aplicado en el chokepoint único _cpGmailSend justo antes de enviar → cubre reseller, RTV, Sole/aprobación y recordatorios de una. Los emojis/acentos viajan como ASCII puro y el cliente los renderiza igual, sin depender del charset del transporte. No toca entidades ya escapadas (&amp;/&nbsp;/&#233; son ASCII) ni el asunto (GmailApp lo codifica con RFC-2047 aparte). La vista previa (iframe) queda sin cambios: el navegador ya maneja UTF-8 bien |
| CP_Código.js | 1.39 | Diagnóstico del mail automático que no sale: nueva función SOLO LECTURA CP_diagAutoMail() (correr desde el editor → Ver registro) que reporta en un tiro TODO lo que debe estar bien para que el trigger CP_autoMailEnvios mande el mail al reseller+RTV: (1) EMAIL_PRUEBA vacío —si tiene un mail, TODO se redirige a esa dirección y reseller/RTV no reciben nada, causa #1 tras usar el modo prueba—; (2) AUTO_MAIL_DESPACHO=SI; (3) trigger CP_autoMailEnvios instalado; (4) por cada envío sin mail: si tiene guía y destinatarios, clasificándolos en esperan-guía / sin-destinatarios / sin-comanda / LISTOS, con ejemplos. Cierra con un veredicto de la causa más probable. No manda ningún correo |
| CP_Código.js | 1.38 | FIX "PDF <n° comanda> todavía no está en Drive" cuando el PDF SÍ existe: _cpBuscarPdfRaw solo usaba folder.searchFiles('title contains'), que depende del ÍNDICE de búsqueda de Drive — ese índice tarda en ver un archivo recién subido, y como el mail a Sole se dispara al instante de crear el envío, no encontraba el PDF. Ahora, si el índice no trae un match por PREFIJO de nombre (convención: el nombre empieza con el n° de comanda, ej. "15861196 - Flo Agro"), hay fallback que recorre la carpeta EN VIVO (getFiles, listado directo, sin índice) y matchea por prefijo normalizado (alfanumérico). El match es solo por prefijo → sin falsos positivos por substring. Afecta el adjunto del mail a Sole, del mail al reseller y el chip PDF del front |
| CP_Código.js | 1.37 | FIX "al crear un envío la venta desaparece de todas las solapas": CP_getComandas armaba la lista SOLO con filas marcadas ID_Entrega='CARGAR'. Cuando la venta se procesa en Masterchief el ERP le saca el flag CARGAR → la venta (con sus envíos) quedaba huérfana e invisible en Pendientes/Con faltantes/Completadas. Ahora también se incluye toda venta que TENGA envíos en la hoja ENVIOS aunque ya no esté en CARGAR (se reconstruye su detalle desde Ventas por ID_Venta) → cae en Completadas o Con faltantes según su pendiente. Nuevo flag c.fueraDeCargar (true = se muestra por tener envíos, ya no está en CARGAR) para el front. Las ventas sin envíos siguen apareciendo solo si están en CARGAR |
| CP_Código.js | 1.36 | MODO PRUEBA de correos: nueva clave EMAIL_PRUEBA en _CONFIG (se auto-agrega a la hoja). Si tiene un mail, el chokepoint único _cpGmailSend redirige TODOS los correos (reseller, RTV, Sole/aprobación, recordatorios) SOLO a esa dirección: pisa "to", corta cc/bcc y antepone al asunto "[PRUEBA → <destino real>]" para ver a quién habría ido. Vaciar EMAIL_PRUEBA vuelve a producción. No cambia adjuntos, gating por guía, ni rate-limit |
| CP_Código.js | 1.35 | El cuerpo del mail al reseller ahora dice EXPLÍCITAMENTE qué se adjunta: nueva sección "📎 Documentos adjuntos a este correo" (bloque verde) que lista la(s) comanda(s) de ese envío y, en el primer envío, cada documento definido por nombre. _cpMailHtml recibe docsAdjuntos (nombres); la vista previa (CP_previewMailEnvio) y el envío real (_cpEnviarEnvioCore) calculan igual esPrimerEnvio + los nombres, así el preview muestra lo mismo que se manda. Se separó _cpDocsDefinidosArchivos ([{id,name}], 1 solo listado de la carpeta) de _cpDocsDefinidosBlobs (blobs) para no listar la carpeta dos veces |
| CP_Código.js | 1.34 | Mail al reseller (por envío) ahora ADJUNTA archivos (antes solo linkeaba el PDF en el HTML): (1) el PDF de la comanda de ESE envío siempre (_cpPdfBlobs); (2) los "documentos definidos" (nuevo _cpDocsDefinidosBlobs = todos los archivos de la carpeta Drive CP_DOCS_FOLDER_ID) SOLO en el primer envío de la venta. "Primer envío" = ningún otro envío de la venta se mandó todavía al reseller (robusto ante borrados/reintentos). 2do/3er envío adjuntan solo su comanda. Sin cambios en el gating: el mail sigue saliendo únicamente cuando el envío tiene guía; destinatarios reseller+RTV+fijos sin cambios. Carpeta vacía/sin configurar → no adjunta documentos ni falla |
| Env.js | 1.14 | FIX raíz de "PDF <n°> todavía no está en Drive": CP_PDF_FOLDER_ID apuntaba a '1TotuLhpmXa94ycEIyeaTV7bIetFNha4Y', un ID de carpeta INEXISTENTE (verificado por API: "Entity not found") → DriveApp.getFolderById tiraba error y ningún PDF se encontraba nunca. Corregido a '11otuLnpmXa94ycEfyea1V7bletFNha4Y' (carpeta "Comandas Master C.", verificada como padre real del PDF "15861196 - FLO AGRO.pdf"). Complementa el fallback de búsqueda de CP_Código 1.38 |
| Env.js | 1.13 | Nueva constante CP_DOCS_FOLDER_ID: carpeta de Drive con los "documentos definidos" (manual/certificados/instructivos) que se adjuntan al reseller solo en el primer envío. Se adjuntan TODOS los archivos de la carpeta; se cambia el contenido agregando/sacando archivos, sin tocar código. Vacío por default (hay que pegar el ID de la carpeta) |
| CP_Index.html | 2.18 | "Cliente" → "Razón Social": encabezado del CSV de envíos (exportarEnvios) pasa de 'Cliente' a 'Razon_Social', consistente con el CSV de faltantes que ya usaba 'Razon_Social'. El valor exportado ya era la razón social. Las cards ya mostraban razonSocial como principal y la ficha ya decía "Razón Social"; sin más cambios de UI |
| CP_Index.html | 2.17 | Ventana de carga inicial (splash a pantalla completa): al abrir la app se muestra un overlay con la marca DJI + spinner + "Cargando comandas… · Leyendo ventas, envíos y guías de despacho" que cubre el arranque (antes solo había un spinner chico en #content mientras CP_getComandas lee varias hojas + Drive). Se oculta con fade al terminar la 1ª carga —tanto en éxito como en error— vía _ocultarVentanaCarga(), llamada en los handlers de cargar(); no reaparece en los auto-refresh (solo se oculta, nunca se re-muestra) para no molestar. Usa las CSS vars → respeta modo oscuro |
| CP_Index.html | 2.16 | Ayuda actualizada: nueva sección "Armar un envío" (tocá el SKU para copiar+marcar cargado en MC, auto-foco al N° de comanda, stepper −/+, botón excluir 🚫, aviso de comanda duplicada, cuándo se habilita Crear envío); tip nuevo sobre el contenido del mail al reseller (despachado + faltantes + vista previa); se corrige la referencia vieja a "Qué cargar" (ahora el control es "Armar envío") |
| CP_Index.html | 2.15 | UX del operador (6 mejoras): (1) auto-foco + glow en el N° de comanda cuando marcás todas las filas como cargadas en MC (fin natural del flujo fila-por-fila); (2) aviso EN VIVO de comanda duplicada mientras tipeás (.dup-note, antes solo saltaba al crear); (3) botón "Crear envío" dice POR QUÉ está deshabilitado (.crear-hint: "Falta N° de comanda" / "Poné cuánto enviás") y ahora exige cantidad>0 además de comanda válida (sincCrearBtn); (4) el auto-refresh preserva la posición de scroll (_keepScroll → window.scrollY en render); (5) botón "excluir" por fila (ban) que alterna 0 ⇄ cantidad completa, con la fila tachada/atenuada (ccToggleExcl, clase .excl); (6) stepper +/− en la cantidad (ccStep). ccMark corregido para buscar .cc-falta dentro del ítem (el input quedó envuelto en .cc-step) |
| CP_Index.html | 2.14 | Consistencia UX: (2) el SKU del control muestra un ícono de copiar sutil al hover (cc-copy-hint) → señal de copia consistente con los chips de ID/comanda/guía; (3) la cantidad dice "a enviar X" en ventas sin envíos previos (pendiente) y "falta X" solo en parciales (pedLbl por estado) |
| CP_Index.html | 2.13 | Correcciones UX: (1) el Hero ya no dice "Todo al día" si hay parciales — contempla faltantes (pintarHero recibe parcialCount: pendientes>0 muestra "+M con faltantes"; 0 pendientes y parciales>0 → "M ventas con faltantes por enviar" en ámbar; "más antigua" mira pendiente+parcial); (2) "Pedido (lo facturado)" ahora colapsado en un &lt;details&gt; ("Ver detalle de facturación") para no duplicar visualmente el control; (3) el link "reiniciar" del control aparece sólo con ≥1 SKU marcado |
| CP_Index.html | 2.12 | Limpieza de redundancias: (1) triple "Listos" colapsado — se elimina el botón de navegar (btn-ready/verListos); queda un único botón verde "Enviar N listos" (aparece con 1+) + el chip de filtro; (2) código muerto borrado: función copiarCargar + CSS .cargar-copy (restos del panel fusionado); (3) se recupera el tag "de kit" en las filas del control (kitBy desde c.cargar.noKit, reusa .cc-flag) |
| CP_Index.html | 2.11 | Fusión: se elimina el panel "Qué cargar en Masterchief" duplicado; la interacción de copiar+marcar se integra al control "Armar envío" (una sola lista). Cada fila: tocás el SKU → copia el código y lo marca cargado (gris/tachado + check), + input de cuánto enviás. Contador "N/M cargados en MC" + reiniciar en el header. Selectores de _cargadosMc pasan a [data-mck] (incluye el panel .ctrl) |
| CP_Index.html | 2.10 | UX carga fila por fila: en "Qué cargar en Masterchief" cada fila es clickeable → copia el SKU y queda en gris/tachada como "ya cargada", con contador "N/M cargados" y botón "reiniciar"; el estado (_cargadosMc por idVenta##sku) sobrevive al auto-refresh (restaurarCargados en render); click de nuevo destilda. cargarFila/actualizarCargCount/reiniciarCargados |
| CP_Index.html | 2.9 | UI/UX: (A) copiar al portapapeles — panel "Qué cargar en Masterchief" restaurado con botón Copiar (SKU⇥cant), + chips de copia en ID_Venta, comanda y guía (copiarTxt/copiarChip/copyChip); (B) modal de Ayuda "?" en el header con flujo, leyenda de estados/badges y cheatsheet de atajos (openAyuda/ayudaHTML); (C) botón "Enviar N" que manda el mail de TODOS los envíos listos secuencialmente respetando rate-limit/auditoría (enviarTodosListos/listaListos), aparece con 2+ listos; Esc cierra también Ayuda |
| CP_Index.html | 2.8 | UI/UX: menú "Herramientas" (agrupa CSV Faltantes/Envíos, Reporte de tiempos, Modo oscuro) descongestiona la toolbar; solapas+toolbar+filtros STICKY al hacer scroll (stickyhead); barra de progreso enviado/total por venta en el header de la card; confirmaciones con modal propio (uiConfirm, reemplaza confirm() nativo en crear/enviar/borrar/reenviar/reintentar); atajos de teclado (/ buscar, r actualizar, 1-4 solapas, Esc cierra modales/menú); highlight de envíos recién "listos" (just-ready); modo oscuro (toggle + localStorage); recuerda solapa+filtros entre recargas (localStorage); responsive/mobile (media queries) |
| CP_Código.js | 1.33 | Diagnóstico CP_diagEnvios(): compara los ID_Venta de la hoja ENVIOS contra los que hoy tienen flag CARGAR en Ventas → revela por qué una venta con envío no pasa a parcial/completo (mismatch de ID_Venta por tipo/formato/ceros a la izquierda, o venta que ya salió del flag CARGAR). Solo lectura, no cambia ningún flujo |
| CP_Código.js | 1.32 | Faltantes en el mail de aprobación/recordatorio a Sole: ahora el mail a Sole también muestra el bloque "⚠ Faltantes (queda pendiente de esta venta)" con lo que resta enviar tras este envío (antes solo salía en el mail al reseller). Helper compartido _cpPendBloqueHtml (reseller + aprobación + recordatorio usan el mismo) → si no hay nada pendiente, no sale nada. _cpMailAprobacionHtml recibe pendientes; _cpEnviarMailSoleCore y _cpEnviarRecordatorioSole calculan _cpPendingVenta(det, arr) |
| CP_Código.js | 1.31 | UX log: CP_poblarRtvDesdeResellers ahora informa claro (RTV encontrados en Resellers, agregados nuevos, total en la hoja, cuántos con mail y cuántos faltan) — el "0 agregados" es idempotente (no re-agrega los que ya están), no un error |
| CP_Código.js | 1.30 | Seguridad (features A+B+C): AUDITORÍA — hoja AUDITORIA (Fecha/Usuario/Acción/ID_Venta/Envío/Detalle) con _cpAuditar en crear/borrar envío, mail reseller, reenviar aprobación, reintentar fallidos, recordatorio Sole. ALLOWLIST — _cpUsuarioAutorizado (OPERADORES_AUTORIZADOS en _CONFIG; vacío = todos) gatea CP_crearEnvio/CP_borrarEnvio/CP_enviarMailEnvio/CP_reenviarAprobacion/CP_reintentarFallidos/CP_setNotaResellerEnvio (devuelven noAutorizado). RATE-LIMIT — _cpGmailSend único punto de envío con tope MAIL_MAX_POR_10MIN (CacheService, fail-open). Minor: CP_selfCheckReadOnly (verifica log≠Ventas); _cpUsuarioActual. setup crea AUDITORIA |
| Env.js | 1.12 | CP_AUDIT_TAB ('AUDITORIA') |
| CP_Código.js | 1.29 | Seguridad: _cpSafeCell también sobre idVenta (viene del cliente) al escribir en ENVIOS y PENDIENTES_ENTREGA + sku/descripcion/reseller/cliente del sync (defensa en profundidad) → cierra el vector de inyección de fórmulas que quedaba por idVenta |
| appsscript.json | — | Seguridad: saco el Advanced Gmail service (enabledAdvancedServices) que no se usaba — el código sólo usa GmailApp. No toca oauthScopes → sin re-autorización. (previo: webapp.access ANYONE_ANONYMOUS → DOMAIN, solo usuarios logueados de bidcom) |
| appsscript.json | — | Seguridad: webapp.access ANYONE_ANONYMOUS → DOMAIN (solo usuarios logueados de bidcom). Alinea el manifest con el deploy ya restringido para que un futuro clasp push no lo reabra. NO cambia scopes → sin re-autorización |
| CP_Index.html | 2.7 | Seguridad: csvCell antepone ' cuando el valor empieza con = + - @ o tab (anti CSV/Excel formula injection en los export) |
| CP_Código.js | 1.28 | Seguridad (sin re-auth): _cpSafeCell antepone ' a texto libre que empieza con = + - @ antes de escribirlo en celdas (notas Aprobador/Reseller) → evita inyección de fórmulas en el log; _cpBuscarPdfRaw sanitiza la comanda a alfanumérico antes de la query de Drive (anti-inyección) |
| CP_Código.js | 1.27 | Optimización C: caché de la tabla de KITS (_cpKitMap usa CacheService, TTL 5 min; la lectura real queda en _cpKitMapRaw). Sólo se cachea si no está vacío y entra en el límite de 100 KB. Evita abrir+leer el archivo de Kits en cada refresh. CP_olvidarKits() invalida manual si editás la tabla. Resellers/_CONFIG NO se cachean a propósito (para que un mail faltante se refleje ya) |
| CP_Index.html | 2.6 | Optimización: el auto-refresh se saltea cuando la pestaña está oculta (document.hidden) y refresca al instante al volver (visibilitychange) — menos llamadas al server sin perder frescura |
| CP_Código.js | 1.26 | Optimización: _cpSS(id) memoiza los spreadsheets abiertos por ejecución (los globals se reinician por request → sin datos viejos); reemplaza ~9 openById por refresh por 3 (Ventas/Log/Kits una sola vez). Sin cambios de comportamiento |
| CP_Código.js | 1.25 | Performance: caché positive-only del PDF de Drive (_cpBuscarPdf usa CacheService, TTL 1 h; se separa la búsqueda real en _cpBuscarPdfRaw). Sólo se cachean los ACIERTOS (comanda con PDF ya subido no cambia) — los "no encontrado" nunca se cachean, así un PDF recién subido aparece en el siguiente refresh. Evita decenas de searchFiles por refresh (auto-refresh cada 3 min). CP_olvidarPdf(comanda) para invalidar manual si se reemplaza el archivo |
| CP_Index.html | 2.5 | Filtros rápidos (chips SLA vencido / Listos para enviar / Con error de mail + desplegable por reseller, con Limpiar) que se combinan con la búsqueda; aviso en auto-refresh cuando aparecen envíos nuevos "listos" (readyKeys diff); umbrales SLA tomados de res.sla (ya no hardcodeados); botón + modal "Resellers sin mail" (ventas sin destinatario, client-side); reporte de tiempos suma bloque "Comanda → despacho" y columnas Despacho/Horas_despacho en el CSV |
| CP_Código.js | 1.24 | SLA configurable (SLA_WARN_HORAS/SLA_DANGER_HORAS en _CONFIG → CP_getComandas devuelve res.sla); _cpParseFechaAr (parsea "dd/MM/yyyy HH:mm"); _cpEnviosMap agrega mailResellerTs; CP_reporteTiempos suma tramo comanda→despacho (mail al reseller como proxy) con resumen.despacho + columnas despachoStr/horasDespacho |
| CP_Index.html | 2.4 | Reporte de tiempos CARGAR → comanda cargada: botón "Tiempos" + modal con promedio/mediana/mín/máx (hasta la primera comanda y sobre todas), contador de ventas CARGAR sin cargar y su antigüedad, y descarga CSV con detalle por venta/envío (reporteTiempos/renderReporte/descargarReporteCSV/hLegible) |
| CP_Código.js | 1.23 | Reporte de tiempos: CP_reporteTiempos (delta fecha de envío − CARGAR más antiguo de la venta; rows por envío + pendientes sin cargar + resumen primer/todos); helpers _cpInfoVentasMap (lee Ventas 1 vez: reseller/cliente) y _cpStats (n/promedio/mediana/mín/máx); origen exacto (edit) vs aprox (deteccion) |
| CP_Index.html | 2.3 | Productividad: indicador "N listos para enviar" en toolbar (guía OK sin mail) + badge "Listo para enviar" y orden primero en solapa Envíos (esReady); auto-refresh de datos cada 3 min (trae guías nuevas; se saltea si estás escribiendo o hay modal — _puedeAutoRefresh) preservando solapa/búsqueda/cards abiertas (data-key + _open/restaurarAbiertas); botón "Reenviar a Sole" por envío sin despachar (reenviarAprobacion); chequeo en vivo del PDF en Drive al tipear la comanda (chequearPdf → CP_checkPdf); export CSV de envíos (exportarEnvios/descargarCSV); operador visible en cada envío |
| CP_Código.js | 1.22 | Productividad: RECORDATORIO_HORAS en _CONFIG (def 24); recordatorio automático a Sole cada X h para comandas sin despachar (CP_recordatoriosSole + trigger CP_setupRecordatorios cada 6 h, anti-repetición con ScriptProperties); _cpEnviarMailSoleCore/_cpMarcarMailAprob (factoriza el mail a Sole, marca col Mail Aprobador) + CP_reenviarAprobacion; CP_checkPdf (PDF en Drive por comanda); c.envios incluye operador; crearEnvio informa mailSoleSinPdf |
| CP_Index.html | 2.2 | Sync inverso + cola de reintentos: los ítems pendientes marcados ENTREGADO en PENDIENTES_ENTREGA muestran badge verde y arrancan en 0 (no se re-envían); pill "N entregados fuera del sistema" en la card; banner rojo de error de mail por envío; botón "Reintentar fallidos" en la toolbar (aparece sólo si hay errores) → CP_reintentarFallidos; render inicializa los labels "queda N"; sincRetryBtn/reintentarFallidos |
| CP_Código.js | 1.21 | Sync inverso + cola de reintentos: _cpEntregaMap (lee PENDIENTES_ENTREGA Estado/Fecha); CP_getComandas adjunta pending[].entregado/entregadoFecha + c.entregadosFuera + envio.mailError (desde col Estado "MAIL ERROR · …"); CP_reintentarFallidos reintenta todos los envíos con guía sin mail (independiente de AUTO_MAIL_DESPACHO) |
| CP_Index.html | 2.1 | Mejoras: vista previa del mail al reseller antes de enviar (modal con iframe + destinatarios/CC/CCO + avisos sin guía/sin destinatario, botón "Enviar ahora"); export CSV de faltantes (1 producto por línea, BOM para Excel); spinner anti doble-click en "Crear envío" y "Enviar"; nota BigQuery (~30 min hasta que aparece la guía) en armar envío y en envíos sin guía; previewMail/cerrarPreview/exportarFaltantes/csvCell |
| CP_Código.js | 1.20 | Mejoras: MAIL_BCC en _CONFIG (CCO para mail reseller + aprobación); CP_previewMailEnvio (arma el HTML del mail sin enviar/marcar); helper _cpDestinatariosEnvio (reseller+RTV+fijos, reutilizado por envío y preview); captura de error de envío (registra "MAIL ERROR · …" en col Estado y devuelve mailError, el auto-trigger reintenta) |
| CP_Index.html | 2.0 | Modelo N-ENVÍOS por venta: 4 solapas (Pendientes / Con faltantes / Completadas / Envíos); en cada venta se arma un "envío" (comanda + cuánto se envía de cada producto + notas aprobador/reseller); lista de envíos dentro de la card y solapa Envíos global; mail al reseller por envío (gatilla con la guía); borrar envío; notas por envío. Reescritura completa del front (crearEnvio/enviarMailEnvio/borrarEnvio/guardarNotaResellerEnvio) |
| Env.js | 1.11 | CP_ENVIOS_TAB reemplaza CP_CARGADOS_TAB |
| CP_Código.js | 1.19 | Modelo N-ENVÍOS: hoja ENVIOS (1 fila por envío: comanda/productos/guía/mails/notas); _cpEnviosMap/_cpPendingVenta/_cpDetalleEnvio; CP_crearEnvio (append + mail Sole con PDF adjunto + sync pendientes), _cpEnviarEnvioCore/CP_enviarMailEnvio (mail reseller por envío, gatilla con guía col K), CP_autoMailEnvios, CP_borrarEnvio, CP_setNotaResellerEnvio; CP_getComandas clasifica pendiente/parcial/completo con c.envios y c.pending; PENDIENTES_ENTREGA = lo que falta enviar |
| CP_Index.html | 1.15 | 4 pedidos del jefe: (1) mail reseller ahora se habilita con la guía/N° seguimiento (texto "En espera del N° de seguimiento"); (2) dos campos de nota del operador (aprobador Sole / reseller) en Pendientes + editar nota reseller en Cargados (guardarNotaReseller); marcarCargado envía ambas notas |
| CP_Código.js | 1.18 | Fix: el mail al reseller mostraba cantidades completas aunque hubiera faltantes — ahora "Detalle despachado" muestra lo REALMENTE enviado (pedido−faltante) y omite ítems 100% faltantes (_cpMailHtml recibe faltMap). PENDIENTES_ENTREGA: dropdown Estado PENDIENTE/ENTREGADO, anchos/bold para leer desde Excel, y el sync preserva Estado+Fecha manuales (no los pisa) |
| CP_Código.js | 1.17 | 4 pedidos del jefe: (1) mail reseller gatilla por guía (col K), no por estado DESPACHADO; (2) PDF de comanda ADJUNTO al mail de Sole (_cpPdfBlobs); (3) hoja PENDIENTES_ENTREGA con 1 producto por línea (_cpSyncPendientesEntrega desde marcar/setFaltantes); (4) notas Aprobador/Reseller en CARGADOS (cols K/L) + CP_setNotaReseller, se muestran en cada mail; estadoDespacho informativo |
| CP_Index.html | 1.14 | Más barreras: aviso si no se cargó ningún ítem (todo 0); si la venta ya fue cargada por otro (yaCargada) pide confirmar sobrescribir (_marcarEnviar con force); botón mail deshabilitado "Sin destinatario" cuando no hay mail de reseller/RTV/fijos; "Volver a pendientes" oculto si ya se envió el mail |
| CP_Código.js | 1.16 | Barreras backend: CP_marcarCargado con force + respuesta yaCargada si ya estaba cargada; CP_desmarcarCargado bloquea si Email Enviado; CP_getComandas adjunta cargado.destino/tieneDestino (reseller/RTV/fijos) |
| CP_Index.html | 1.13 | Barreras anti-error al cargar: botones ✓/"Marcar" deshabilitados hasta comanda válida; input filtra a dígitos y / (validarComanda); valida formato (comandaValida); confirma si se marca con faltantes; avisa si la comanda ya se usó en otra venta (comandaDuplicada) |
| CP_Código.js | 1.15 | Validación defensiva del N° de comanda en CP_marcarCargado (regex dígitos con / opcional) |
| CP_Index.html | 1.12 | Faltantes se definen al cargar: el control de carga editable aparece también en Pendientes; al marcar (✓ header, Enter o botón "Marcar como cargado" del cuerpo) se envían comanda + faltantes juntos (leerFaltantesCard); toast indica cuántos faltantes quedaron |
| CP_Código.js | 1.14 | CP_marcarCargado(idVenta, comanda, faltantes): guarda comanda y faltantes en un solo paso; helper _cpFaltantesJson compartido con CP_setFaltantes; return incluye conFaltante |
| CP_Index.html | 1.11 | Despachar con faltantes por ítem: en comandas cargadas el panel pasa a "Control de carga" con input por componente (cuánto cargué → falta lo que resta, en vivo); tercera solapa "Con faltantes" con contador; badge "Incompleta"; guardarFaltantes()/ccMark()/fmtCant() (reemplaza nota libre setPendiente) |
| CP_Código.js | 1.13 | Faltantes estructurados: CARGADOS col J = JSON {SKU:cant} (_cpParseFaltantes); CP_setFaltantes() reemplaza CP_setPendiente; c.cargado.faltantes/conFaltante; CP_getComandas separa cargadosCount vs faltantesCount; mail a reseller+RTV incluye bloque "Pendiente de enviar (faltante)"; cargar/detalle agregan cantNum |
| CP_Index.html | 1.10 | Paso 5: botón "Enviar mail al reseller + RTV"; si ya se envió, pasa a "Reenviar" con confirmación de advertencia (anti-duplicado); toast muestra a quién se envió |
| CP_Código.js | 1.12 | Resellers tab confirmada: _cpResellerMap() lee B=Reseller/C=RTV/J=Email → saca mail del reseller Y nombre del RTV de la misma hoja (fallback Ventas col Z); CP_poblarRtvDesdeResellers() pre-carga la hoja RTV con los nombres; setup lo corre solo |
| CP_Código.js | 1.11 | Paso 5 completo: destinatarios reseller (pestaña Resellers, _cpResellerMailMap con detección de columnas) + RTV (hoja RTV en log, _cpRtvMailMap) + fijos _CONFIG; _cpEnviarDespachoCore compartido; envío automático opcional CP_autoMailDespacho (trigger horario, AUTO_MAIL_DESPACHO=SI) con anti-duplicado por Email Enviado; CP_setupAutoMail; _cpDetalleVenta agrega rtv; CP_debugMails; setup crea hojas CARGADOS/_CONFIG/RTV |
| CP_Index.html | 1.9 | Aviso a Sole en el toast al cargar; editor "¿Quedó algo pendiente?" en cada comanda cargada (input + Guardar → CP_setPendiente) + badge rojo "Falta" en el header cuando hay faltante; setPendiente() |
| CP_Código.js | 1.10 | Paso 4 mail a Sole automático al marcar cargado (MAIL_APROBACION en _CONFIG, asunto APROBAR MC, con PDF+detalle, _cpMailAprobacionHtml); _CONFIG auto-completa claves nuevas (CP_CONFIG_DEFAULTS); faltante: col Pendiente en CARGADOS + CP_setPendiente() + cargado.pendiente |
| CP_Index.html | 1.8 | Link al PDF de la comanda en la vista Cargados (chip rojo) — resuelto automáticamente por N° de comanda desde Drive |
| CP_Código.js | 1.9 | _cpBuscarPdf(): busca el PDF de la comanda en CP_PDF_FOLDER_ID por nombre que empieza con el N°; adjunta cargado.pdfs y lo incluye en el mail (fila "PDF comanda") |
| Env.js | 1.8 | CP_PDF_FOLDER_ID: carpeta Drive con los PDF de comandas (nombre empieza con el N°) |
| CP_Index.html | 1.7 | Despacho: en Cargados, si la comanda figura DESPACHADO en Comandas Master muestra estado, transportista y guía OCA (con link tracking) + botón "Enviar mail" (Reenviar si ya se mandó); si no, "En espera de despacho"; función enviarMail() |
| CP_Código.js | 1.8 | Step 2 despacho+mail: _cpMasterMap() lee "Comandas Master" (idComprobante col A → estado F/guía K/transportista B); CP_getComandas enriquece cargados con despachado/guiasLinks; _cpConfig()/_cpConfigHoja() (_CONFIG con MAIL_DESTINATARIOS/CC/ASUNTO/OCA_TRACKING_URL); _cpDetalleVenta(); CP_enviarMailDespacho() vía GmailApp + _cpMailHtml(); _cpMarcarEmailEnviado() persiste estado/guía/fecha/email en CARGADOS |
| Env.js | 1.7 | CP_MASTER_TAB ("Comandas Master") + CP_DESPACHADO + CP_CONFIG_TAB ("_CONFIG") |
| CP_Index.html | 1.6 | Marcar como cargado: input N° comanda + ✓ en el header de cada pendiente (Enter o botón) → CP_marcarCargado; sale de Pendientes; tabs Pendientes/Cargados con contadores; vista Cargados con badge de comanda, detalle y "Volver a pendientes" (CP_desmarcarCargado); toast; jsq() para escapar ID en onclick |
| CP_Código.js | 1.7 | Estado "cargado": hoja CARGADOS en sheet de log (ID_Venta/Comanda/Fecha/Operador/Estado/Guia/Transportista/Fecha Despacho/Email); CP_marcarCargado (upsert) / CP_desmarcarCargado (no si despachado) / _cpCargadosHoja / _cpCargadosMap; CP_getComandas adjunta c.cargado y separa pendientes de cargados (ventas=pendientes, cargadosCount) |
| Env.js | 1.6 | CP_CARGADOS_TAB ("CARGADOS") en el sheet de log: comandas cargadas por el operador |
| CP_Index.html | 1.5 | Contador de antigüedad (SLA) por comanda: badge en el header (visible colapsado) con tiempo pendiente desde CARGAR, color verde/amarillo/rojo (umbrales AGING 4h/24h), actualización en vivo cada 30s (tickAging), pulso en rojo; ordena las más antiguas arriba; hero muestra "más antigua: hace X"; s/f si no hay registro |
| CP_Index.html | 1.4 | Panel "Qué cargar en Masterchief": explota cada KIT en sus componentes reales (código+desc+cantidad) con botón Copiar (portapapeles); rótulo "Pedido (lo facturado)" en las líneas de kit; marca "directo" si el SKU no está en la tabla de kits |
| CP_Código.js | 1.5 | Explosión de kits: _cpKitMap() lee sheet BOM (KIT col C → componentes col A, cantidad = filas repetidas); CP_getComandas adjunta c.cargar (componentes agregados a nivel comanda, cantidad = comp × cant kits); _kitKey/_addCargar; CP_debugKits(); línea marca esKit |
| CP_Código.js | 1.6 | El log ya NO se escribe en el sheet de Ventas: _cpLogHoja/_cpLogMap usan CP_LOG_SS_ID (sheet separado). Ventas queda 100% solo lectura |
| Env.js | 1.5 | Separa lectura/escritura: CP_SS_ID (Ventas, solo lectura) vs CP_LOG_SS_ID (1mOOeUD, donde se escribe CARGAR_LOG) |
| Env.js | 1.4 | CP_KITS_SS_ID + CP_KITS_TAB ("Actual"): sheet de recetas/BOM kit→componentes |
| CP_Index.html | 1.3 | Muestra el momento en que se marcó CARGAR: tiempo relativo ("hace X") en el encabezado colapsado + fila "Marcado CARGAR" en la ficha (rango desde→hasta si difiere; "Detectado ≈" para snapshot; "sin registro" si falta); helper relTime() |
| CP_Código.js | 1.4 | Registro del momento CARGAR: trigger instalable CP_onEditVentas (estampa fecha/hora exacta al escribir CARGAR en ID_Entrega) → hoja CARGAR_LOG (ID_Venta/SKU/fecha/origen/usuario); CP_setupTrigger() (instalación 1 vez); CP_snapshotCargar() (detección aprox. para filas ya marcadas); _cpLogMap/_cpLogStamp/_cpKey/_cpLogHoja/_fmtTs; CP_getComandas adjunta cargadoTs/cargadoStr por línea y comanda |
| CP_Index.html | 1.2 | Líneas consolidadas por SKU: muestra cantidad real a despachar; marca "facturado en N" (badge amarillo) cuando un SKU venía dividido en varias líneas de factura |
| CP_Código.js | 1.3 | Consolida líneas por SKU dentro de cada comanda sumando cantidades fraccionadas (0.65+0.35=1 real a despachar) + totales; campo partes por línea; helper _fmtCant (entero sin decimales, fracción con coma) |
| CP_Index.html | 1.1 | Comandas colapsadas por defecto: encabezado compacto clickeable (ID_Venta + operación + resumen cliente/ítems/total USD + chevron) que expande el detalle; botón "Expandir/Colapsar todo" en toolbar; funciones toggle()/toggleTodo()/sincToggleBtn() |
| CP_Index.html | 1.0 | Tablero interno (sin login, estética Portal Reseller): lee hoja "Ventas" y muestra las ventas con ID_Entrega=CARGAR agrupadas por ID_Venta en tarjetas con aviso "cargar en Masterchief"; buscador local, resumen/hero con conteo, badges RTV/Aprob. Comercial, totales USD/ARS; carga automática al abrir + botón Actualizar |
| CP_Código.js | 1.2 | CP_debugCrudo(): diagnóstico que vuelca pestañas del archivo, dimensiones y primeras 4 filas (crudo + normalizado) para detectar por qué no matchea el encabezado |
| CP_Código.js | 1.1 | Detección de columnas por nombre de encabezado (_norm + CP_ALIASES + _cpDetectar) en vez de índices fijos — resuelve el bug de "al día" con columnas ocultas/corridas; busca la fila de headers en las primeras 6 filas; tolera columnas ausentes; CP_debugColumnas() para diagnóstico desde el editor |
| CP_Código.js | 1.0 | Backend solo lectura: doGet sirve CP_Index; CP_getComandas() lee la hoja, filtra ID_Entrega=CARGAR, agrupa por ID_Venta sumando Total USD/ARS y juntando líneas SKU; helpers _num/_fmtUSD/_fmtARS |
| Env.js | 1.2 | CP_SS_ID corregido a 1KVaNIQ...R4sg (el sheet con la data real; el 1mOOeUD estaba vacío) |
| Env.js | 1.1 | CP_SS_ID real (1mOOeUD...WBN44) apuntando al sheet definitivo |
| Env.js | 1.0 | Config: CP_SS_ID (sheet fuente), CP_TAB="Ventas", CP_FLAG="CARGAR", mapa de columnas VC (28 cols A-AB) |

## OMW
| Archivo | Versión | Notas |
|---------|---------|-------|
| OMW_Index.html | 1.5 | Fix reseller view: comparación reseller case-insensitive; agrega stage rechazado con card roja + botón Re-enviar a RTV; función reenviarPedido(); cotizador: caché completo via OMW_getTablaCondiciones() — sin round-trip por cada cambio de SKU/método |
| OMW_Código.js | 1.4 | OMW_getTablaCondiciones(): devuelve toda la tabla de precios en un call; _pedidosAll(): backward compat para stages viejos ('admin','depo','done','rechazado' lowercase) |
| Env.js | 1.4 | CP extendido a 18 cols (ID_COTIZ/SUBTOTAL/DESCUENTO/FECHA_FORMAL cols N-Q); OMW_STAGES constantes para los 7 stages del pipeline |
| OMW_Index.html | 1.4 | Sistema de cotizaciones reseller: botón "Guardar Cotización" en cotizador → form de datos del cliente (nombre/empresa/CUIT/tel/email/notas) → guarda en tab COTIZACIONES; sección "Mis Cotizaciones" en vista reseller con cards clickeables que recargan el cotizador; estados Nueva/Seguimiento/Cerrada; pre-fill SKU+método al recuperar cotización |
| OMW_Código.js | 1.3 | OMW_guardarCotizacion(datos): appends a tab COTIZACIONES con auto-ID; OMW_getCotizaciones(reseller): lista newest-first filtrada por reseller; OMW_actualizarEstadoCotiz(id, estado, reseller): valida dueño; _hojaCotiz(): auto-crea tab si no existe; setupOMW() incluye COTIZACIONES |
| Env.js | 1.3 | TAB_COTIZACIONES + CCOT (columnas tab COTIZACIONES, 17 cols A-Q) |
| OMW_Index.html | 1.3 | Fix: botón "Crear Pedido" en cotizador visible para todos los roles internos (antes solo rtv/admin); resellers siguen sin verlo |
| OMW_Index.html | 1.2 | Cotizador: botón "Cotizar" en topbar (todos los roles); modal con selector SKU+Método (auto-cotiza al seleccionar); breakdown completo (PVP, comisión, CFT, subtotal, IVA, total); "Crear Pedido" pre-rellena importe+pago+producto |
| OMW_Código.js | 1.2 | OMW_getCatalogo(): lista única de SKUs+métodos disponibles desde tab Condiciones; OMW_getCotizacion(sku,metodo): devuelve breakdown completo con pvpStr/totalStr/etc formateados |
| Env.js | 1.2 | COTIZADOR_SS_ID + TAB_CONDICIONES + CC (columnas tab Condiciones, índices 1-15 cols B-P) |

## HUB_PRO
| Archivo | Versión | Notas |
|---------|---------|-------|
| HUB_Código.js | 2.24 | HUB deja de tocar stock al finalizar una OT: se quitó la llamada a procesarSalidaRepuestos en el cierre a "Finalizado" y se eliminó la función. Antes descontaba STOCK_REPUESTOS.STOCK_ACTUAL + escribía MOVIMIENTOS_STOCK, pero como el stock real ya se lee de Carmen (STOCK_REPUESTOS es caché), ese descuento no afectaba el stock real y duplicaba el registro. El consumo de repuestos se registra fuera de HUB. El cierre conserva FECHA_CIERRE y _cerrarSolicitudesPendientes; el resto (Entregado/Cancelado/etc.) sin cambios. El front puede seguir mandando data.repuestosItems: ahora se ignora (inofensivo). Sin re-auth |
| HUB_Código.js | 2.23 | FIX: al crear OT desde el HUB no se chequeaba el N° de serie → se podían abrir 2 casos abiertos para el mismo equipo (incluso si el Portal Reseller ya había abierto uno). Ahora crearNuevaOT valida S/N: nuevo helper _otBuscarSnAbierto(sn) recorre 'Ordenes de trabajo' (la MISMA hoja donde escribe también el Portal Reseller vía enviarCasoAlHub) y bloquea si hay una OT con ese S/N en estado NO cerrado (usa _esCerrada, misma definición que el front: Finalizado/Entregado/CANCELADO/Rechazado DJI/Sin respuesta·Cerrado NO bloquean). S/N vacío se ignora. Lee con force (sin cache) y dentro del lock → sin carreras. Mensaje de error: "Ya existe la orden X abierta (estado) con el N° de serie …". El front ya lo muestra como toast (mismo camino que el duplicado CAS/FWRC). Simétrico al chequeo que el Portal ya tenía (RS_OTs enviarCasoAlHub) |
| Index.html | 2.14 | Unificación CAS/FWRC en UN solo campo (el operador veía 2 campos siendo "uno u otro"). En el modal "Nueva Orden" y en el panel de detalle ahora hay UN input "CAS / FWRC" (#nev-cas / #f-cas); se quitaron #nev-fwrc y #f-fwrc. loadOrden puebla el campo con cas || fwrc (los casos viejos que solo tenían FWRC se muestran igual); guardar()/crearCaso ya no mandan fwrc → la col P (FWRC) queda como LEGADO no-destructivo: los valores viejos no se pisan, siguen siendo buscables y salen en el CSV. Todo se guarda en la col O (CAS), que es la que usan remitos/mails/tránsito ("Caso DJI (CAS/FWR)"). Se limpiaron los arrays de reset (limpiarFormulario/resetUIPartial) y el CSS de #f-fwrc |
| HUB_Código.js | 2.22 | Acompaña la unificación CAS/FWRC (un solo caso DJI, uno u otro). _otBuscarDuplicadoCasFwrc ahora compara el valor contra AMBAS columnas (CAS col O y FWRC col P) de las demás OTs, así el campo unificado no puede colisionar ni con un CAS ni con un FWRC viejo; el conflicto se reporta con campo "CAS/FWRC". Sin cambios de schema (la col P FWRC se mantiene, solo deja de escribirse desde el formulario; actualizarOrden ya tenía el guard data.fwrc!==undefined, así que al no mandarse no la pisa) |
| Env.js | 1.1 | SCHEMA.OT: nueva columna FWRC=15 (col P) para el nº de caso FWRC de DJI (opcional, único). CAS ya existía en 14 (col O) |
| HUB_Código.js | 2.21 | Unicidad de CAS/FWRC más robusta: nuevo helper _normCasFwrc(v) que normaliza SOLO para comparar (minúsculas + sin guiones ni espacios), así "AB-123", "ab 123" y "AB123" cuentan como el mismo número y no se puede esquivar el chequeo metiendo un guion o cambiando mayúsculas. El valor se sigue GUARDANDO tal cual lo escribió el operador; la normalización es solo para detectar duplicados en _otBuscarDuplicadoCasFwrc |
| HUB_Código.js | 2.20 | CAS/FWRC opcionales al crear orden + unicidad. (1) Se resolvió un marcador de conflicto de merge que había quedado en el header (líneas 2-7, solo comentarios) — el archivo no parseaba/no corría; se tomó el header limpio de dev, el @version canónico sigue en 2.x. (2) Nuevo helper _otBuscarDuplicadoCasFwrc(cas,fwrc,otExcluir): busca duplicados (CAS vs col CAS, FWRC vs col FWRC, case-insensitive, lee del sheet con force) devolviendo {campo,valor,ot} o null. (3) crearNuevaOT ahora PERSISTE cas→col O(14) y fwrc→col P(15) (antes ni siquiera guardaba CAS) y, con el lock tomado, rechaza si ya existe ese CAS/FWRC → devuelve resultado 'Error: El <campo> <valor> ya está usado en la orden <OT>'. (4) actualizarOrden escribe FWRC (col P, guard !==undefined) y valida unicidad contra las demás órdenes (excluye la propia por nº de OT) devolviendo el mensaje en resultado. (5) cargarTodo expone fwrc en cada orden |
| Index.html | 2.13 | CAS/FWRC opcionales en HUB. Modal "Nueva Orden": 2 inputs opcionales #nev-cas + #nev-fwrc (se mandan en el payload de crearNuevaOT; se limpian en crearCaso; confirmarNuevaOT ahora muestra el motivo REAL del error —ej. duplicado— en vez de "Error al crear OT" genérico). Panel de edición: el campo "CAS / FWR" se separó en #f-cas ("CAS") + nuevo #f-fwrc ("FWRC"), ambos en azul; loadOrden puebla fwrc, guardar() lo manda, y los arrays de limpieza + búsqueda + CSV incluyen fwrc (búsqueda por FWRC; export con columnas CAS y FWRC separadas). Se quitó un @version espurio duplicado (línea 3, artefacto de merge) |
| HUB_Código.js | 2.19 | FIX doble mail al FINALIZAR: se enviaban 2 mails ("Actualización de su Orden de Servicio" al reseller + "Alerta —" al supervisor). Se quitó "Finalizado" de ESTADOS_NOTIFICAR_SUPERVISOR (ahora []) → al finalizar solo se avisa al reseller. El supervisor sigue recibiendo URGENTE/Backorder y la solicitud de facturación OOW (bloque 4) se manda igual |
| Index.html | 2.12 | Reparaciones Reseller/Reseller Propio: 2 badges claros + selectores editables. (1) Origen del repuesto: quién pone la pieza — "🔧 Repuesto adelantado" / "📦 Reseller reparó con su stock" (precargado desde el Portal via aftEstado, editable). (2) Cierre del caso: "📦 Parte de reposición" / "🧾 NC" (lo definimos nosotros, solo informativo). Selectores #f-origen/#f-cierre en el panel de la OT (bajo el estado) + badge de color al lado; badges chicos en la celda de estado de la lista para verlo de un vistazo. Helpers _origenLabel/_origenColor/_cierreLabel/_cierreColor/_repoBadge/_pintarRepoBadges. guardar() manda origenRepuesto+cierreTipo |
| HUB_Código.js | 2.18 | Soporte de origen del repuesto (col AA=26) y tipo de cierre (col AB=27). cargarTodo expone origenRepuesto (col AA; si vacía se deriva del texto del informe col M: "YA REPARADO"→Stock reseller, "PENDIENTE…NECESITA REPUESTOS"→Adelantado, helper _origenRepuestoDe) y cierreTipo (col AB). actualizarOrden persiste ambos (data.origenRepuesto/cierreTipo) con guarda de columnas (getRange no auto-expande → insertColumnsAfter si maxColumns<28). Nuevas constantes ORIGEN_REPUESTO/CIERRE_TIPO en Env.js. Solo informativo (no dispara mails/facturación); concurrencia optimista (col Z) intacta |
| Env.js (HUB) | 1.1 | SCHEMA.OT: nuevas columnas ORIGEN_REPUESTO (26/AA) y CIERRE_TIPO (27/AB) para los badges de origen del repuesto y cierre reposición/NC |
| HUB_Código.js | 2.17 | Diagnóstico "ningún mail de batería sale". Nueva HUB_diagnosticoBateria(numeroOT) read-only (no manda mails): reporta equipo, tipo en EQUIPOS col B, esBateria(), estado actual y si está en ESTADOS_NOTIFICAR_RESELLER, email del reseller y del usuario "Gestion Logistica", y lista los problemas de config concretos. Sospechosos: (a) esBateria=false por tipo mal escrito en EQUIPOS (debe ser 'bateria' exacto) → no dispara reposición; (b) usuario 'Gestion Logistica' sin email en Usuarios_Internos → enviarEmailReposicionBateria hace return al inicio y aborta el mail entero (logística + copia reseller) sin dejar registro en EMAIL_LOGS. Wrapper HUB_diagnosticoBateriaTest() editable |
| HUB_Código.js | 2.16 | Fix casos de batería: el reseller no recibía aviso cuando DJI aprobaba. (1) Se agregó "Aprobado por DJI" a CONFIG.ESTADOS_NOTIFICAR_RESELLER (antes solo estaba "Rechazado DJI" → el rechazo avisaba pero la aprobación no). (2) armarEmailReseller ahora tiene textos propios para los estados de batería (Caso Enviado, Aprobado por DJI, Rechazado DJI, Bateria enviada a reseller) en vez del genérico "Su orden fue actualizada". Los textos se aplican SOLO si esBateria(data.equipo) es true, porque "Rechazado DJI" también existe en el circuito Reseller Propio y no debe usar la redacción de batería. El mail de reposición (Scrap Enviado (Evidencias) → Gestión Logística + reseller) sigue igual |
| HUB_Código.js | 2.15 | Recuperación de los mails de apertura perdidos (OT creadas desde el HUB antes de v2.14). Nueva HUB_notificarOT(numeroOT, forzarAbierto): busca la OT por número en la hoja, arma el data desde la fila ACTUAL y llama enviarNotificaciones(data, "-", tecnico) → dispara el aviso del estado actual (o fuerza "Abierto" con forzarAbierto=true) y lo registra en EMAIL_LOGS. NO modifica la OT, solo manda el mail. Wrapper HUB_notificarOTTest() editable (NUMEROS separados por coma + FORZAR_ABIERTO) para correr desde el editor. Nota: los cambios de estado FUTUROS de esas OT ya notificaban solos (pasan por actualizarOrden), lo único que faltaba era el aviso de apertura |
| HUB_Código.js | 2.14 | Fix: las OT creadas desde el HUB (no desde el Portal Reseller) no mandaban ningún mail de cambio de estado ni dejaban registro en EMAIL_LOGS. Causa: crearNuevaOT escribía la fila con estado "Abierto" y registrarLog, pero nunca llamaba a enviarNotificaciones (a diferencia de actualizarOrden). Ahora crearNuevaOT arma un dataNotif (ot, estado "Abierto", reseller, equipo, sn, garantía, circuito, cas, prioridad→bool, _fechaEstimada) y llama enviarNotificaciones(dataNotif, "-", tecnico) → dispara el mail de apertura al reseller/técnico (ambos tienen "Abierto" en sus listas) y queda registrado en EMAIL_LOGS. Envuelto en try/catch para no romper la creación si falla el envío |
| HUB_Código.js | 2.13 | Fix: el mail de facturación a Administración mostraba los repuestos SIN precios (usaba construirTablaRepuestos, tabla Env/Ped/Estado sin importes). Nueva construirTablaRepuestosFacturacion(rep) → {html,total} con precios: PVP (DB_REPUESTOS col F) × descuento reseller 40%, P.Unit neto + Subtotal + SUBTOTAL REPUESTOS, misma lógica que el presupuesto. armarEmailFacturacion ahora usa la tabla con precios + suma un TOTAL A FACTURAR (repuestos + mano de obra, sin impuestos). construirTablaRepuestos (mails de estado al reseller) queda igual: sigue sin precios |
| HUB_Código.js | 2.12 | Fix: al FINALIZAR, el mail al reseller mostraba repuestos "Pendiente (N)" + nota "Algunos repuestos están pendientes de envío" aunque la orden estaba cerrada. construirTablaRepuestos(rep, cerrada) acepta flag; armarEmailReseller lo pasa como (nvo==="Finalizado") → en OT finalizada env=ped (todo entregado, estado OK, sin nota de pendientes). Supervisor (armarEmailSupervisor) sin cambios: sigue viendo el backorder real |
| HUB_Código.js | 2.11 | Fix: el mail al reseller salía en CADA guardado (incluso re-guardando el mismo estado sin cambios). Ahora el bloque 1 (RESELLER) de enviarNotificaciones exige estadoNuevo != estadoAnterior → solo notifica al reseller cuando realmente cambia de estado. Supervisor/técnico sin cambios (urgente/backorder/asignación pueden requerir aviso sin cambio de estado) |
| HUB_Código.js | 2.10 | Solicitud de facturación al finalizar OOW (como WOS): nuevo CONFIG.EMAIL_FACTURACION (Cecilia.f + lucia.c). En enviarNotificaciones, bloque 4: al pasar a "Finalizado" (solo en la transición, estadoAnterior!=Finalizado) y garantía OOW, manda mail a Administración con armarEmailFacturacion(data,tecnico) → detalle OT/reseller/equipo/SN/CAS + mano de obra (de manoObraGuardada, con subtotal) + repuestos (construirTablaRepuestos). Se envía a un hilo propio por destinatario (no entra al hilo del reseller). Registra en EMAIL_LOGS como "Facturación" |
| Index.html | 2.11 | Performance (B): dataset = solo OTs activas (cargarOrdenes(false)); las cerradas se traen on-demand al entrar a Finalizados (_cargarCerradas → cargarOrdenes(true), separa activas/cerradas). datasetCerradas + _cerradasLoaded + _cerradasCount + EST_CERRADOS/_esCerradoCli. Se extrae _listaFiltrada() compartida por render() y abrirDesdeTabla() (arregla la duplicación de filtros y garantiza el mismo índice). _fuenteActual elige activas o cerradas según vista/filtro. abrirOTPorNombre busca en ambas (lazy-load si hace falta) → no se rompe abrir OTs cerradas por link. El dropdown de estado incluye los 5 estados cerrados. NOTA: la "inconsistencia de estados terminales" que mencioné en 2.10 era de WOS, NO de HUB — en HUB son consistentes, por eso B se pudo hacer sin riesgo |
| Index.html | 2.10 | Performance (A+C): load()/auto-refresh usan cargarOrdenes() (liviano, sin catálogo) en vez de cargarTodo — el catálogo estático se trae una sola vez con cargarCatalogoFront() (cache sessionStorage 30 min) al inicio, no en cada refresh. Se ELIMINA el auto-refresh huérfano iniciarSincronizacionSilenciosa que llamaba a cargarTodo() completo cada 60s sin redibujar nada (renOT/fil/dOT no existen) → era puro gasto. La vista sigue siempre fresca (OT en vivo). B (excluir OTs cerradas del payload) queda pendiente: requiere reconciliar la definición de "estado terminal" (render usa Finalizado/Entregado/CANCELADO/Rechazado DJI/Sin respuesta·Cerrado; actualizarStats usa Entregado_Cerrado/Entregado_Confirmado/Cancelado) |
| Index.html | 2.9 | Auto-refresh de fondo (ya no hace falta F5 para ver cambios de la planilla / otros usuarios / Portal): _bgRefresh() corre silencioso cada 90s (o el intervalo de Ajustes si el usuario puso uno) y también al volver a la pestaña (visibilitychange). Con guardas: no refresca si hay OT abierta, edición sin guardar (isDirty), un modal abierto, un input/select con foco, o la pestaña oculta. load(silent) omite overlay/toasts en modo silencioso; _loadEnCurso evita cargas superpuestas (el load manual siempre corre, el de fondo se difiere). Antes autoRefresh=0 = nunca refrescaba solo |
| Index.html | 2.8 | Fix definitivo descripción "estirada" en Nota de Entrega: la causa eran caracteres full-width/CJK del catálogo DJI (ej "（ZS）") que la fuente estándar de jsPDF no puede medir. sanitizePdf() convierte full-width→ASCII y saca glifos no soportados; se aplica a código/descripción de repuestos, equipo, sn y trabajo técnico (reemplaza el despaciarDesc de v2.7 que era diagnóstico errado) |
| Index.html | 2.6 | Nota de Entrega PDF: anchos de columna fijos (Código 38 / Descripción 120 / Cant. 18) + overflow linebreak en la tabla autoTable de REPUESTOS UTILIZADOS |
| Index.html | 2.2 | Botón "Pedir Repuestos": visible para IW y OOW (antes solo OOW); condición cambiada a !esTerminal |
| HUB_Código.js | 2.9 | Performance (B): cargarTodo(soloOrdenes, incluirCerradas) + cargarOrdenes(incluirCerradas). Por defecto NO manda las OTs cerradas (Finalizado/Entregado/CANCELADO/Rechazado DJI/Sin respuesta·Cerrado) — las saltea contándolas → devuelve cerradasCount. Helper _esCerrada. La vista Finalizados pide cargarOrdenes(true). Reduce el payload de cada refresh proporcional a la historia acumulada |
| HUB_Código.js | 2.8 | Performance (A): cargarTodo(soloOrdenes) — con soloOrdenes=true devuelve solo la lista viva (ordenes+tecnicos) SIN leer/serializar el catálogo. Nuevo cargarOrdenes()=cargarTodo(true) para los refrescos. El catálogo (repuestos/resellers/equipos/mano de obra) se trae con cargarCatalogo() una sola vez. La vista sigue siempre fresca (OT se lee en vivo, v2.7) |
| HUB_Código.js | 2.7 | Fix "hay que hacer F5 para ver el estado nuevo": cargarTodo lee la hoja OT con getSheetValues(hojaOT, true) → SIEMPRE en vivo, nunca del cache. La causa era CacheService: actualizarOrden invalida el cache (línea 638) pero el remove tiene propagación diferida entre ejecuciones, y el load() inmediato tras guardar leía el valor viejo (F5 unos segundos después ya veía el remove propagado). Mismo patrón force=true que la línea 486 |
| HUB_Código.js | 2.6 | HUB_generarPedidoRepuestos: 6 fixes — (1) OBS ya no duplica garantia; (2) permite múltiples pedidos por OT saltando SKUs ya pedidos; (3) MASTER abierto una sola vez; (4) bodyHtml diferenciado primer pedido vs adicional; (5) backfill threadId en filas preexistentes; (6) registrarEmailLog solo en primer pedido |
| Index.html | 2.5 | Botón "Piezas Recibidas": condición ampliada a cualquier circuito que contenga "Reseller" (cubre Reseller y Reseller Propio) y estado no terminal |
| Index.html | 2.4 | Botón "Piezas Recibidas" (naranja) visible en Reseller Estándar con estado "Informe de reparacion"; abre modal con textarea libre; al confirmar escribe entrada timestampeada en Notas Internas y avanza estado a "Piezas dañadas recibidas" via guardar() |
| Index.html | 2.3 | Modal Pedir Repuestos: número preview corregido OT-X (era REP-OT-X); checkbox DJI basado en estado "Aprobado"; botón btn-rep-wos muestra "WOS: OT-X" en verde tras éxito |
| HUB_Código.js | 2.5 | HUB_generarPedidoRepuestos: usa _enviarConHilo para continuar el hilo Gmail existente de la OT en lugar de crear thread nuevo; WOS responde en el mismo hilo al despachar |
| HUB_Código.js | 2.4 | HUB_generarPedidoRepuestos: lookup precio desde Lista_Repuestos (×0.60 precio reseller) al crear filas WOS; fallback a 0 si SKU no está en catálogo |
| HUB_Código.js | 2.3 | HUB_generarPedidoRepuestos: lee data.envio y lo escribe en col L; OBS incluye "Taller · entrega técnico + nombre" cuando circuito=Taller |
| Index.html | 2.1 | Modal "Pedir Repuestos": selector de modalidad de entrega — Taller muestra badge fijo "Entrega directa al técnico", Reseller muestra radio Envío/Retiro; envio y circuito se pasan al backend |
| HUB_Código.js | 2.2 | HUB_generarPedidoRepuestos: 26 cols (COL schema igual a Pedidos_resellers); formula =E-F-Z en col G; hilo Gmail ancla via draft.send() guardado en col R; lookup email reseller en MASTER; estado inicial Confirmado |
| HUB_Código.js | 2.1 | Flujo batería unificado: trigger reposición mueve de "Aprobado por DJI" a "Scrap Enviado (Evidencias)"; nuevos estados Caso Enviado y Bateria enviada a reseller en ESTADOS_NOTIFICAR_RESELLER |
| Index.html | 2.0 | EST_BAT reemplaza EST_RPB con 7 estados; detección batería sin restricción de circuito; esTerminal incluye Rechazado DJI y Sin respuesta · Cerrado |
| GUIA_FLUJOS.html | 1.3 | Flujo batería: tab renombrado, sección reescrita con nuevos 7 estados; aplica a Reseller Común y Propio |
| Env.js | 1.2 | Agrega SCHEMA.EQUIPOS (NOMBRE, TIPO, PREFIJO, MESES) |

## PORTAL_RESELLER
| Archivo | Versión | Notas |
|---------|---------|-------|
| Guia_Reseller.html | 1.3 | Guía del reseller reescrita completa al estado actual del portal (antes 1.2, desactualizada). Índice ampliado a 11 secciones. Nuevas: "Recorrido por el portal" (tabla de todas las pestañas actuales: Nueva Reparación, Mis Reparaciones, Recursos, Novedades, Curso, Carrito, Cotizador, Mensajes), "Envío de repuestos dañados" (bloque completo EUCAWOODS/SIPCA Carmen de Areco CP 6725 + maps + horario + Mariano Castronuevo + escribir N° WH/REP en el paquete + adjuntar tracking/remito + reemplazo o nota de crédito), "Cotizador" (presupuesto cliente final, 25% off, PDF por email) y "Curso y eventos" (inscripción con asistentes). Tipos de gestión reescritos con los flujos reales: Taller Central (envío del equipo), Reparación en mi taller (Reseller común: carpeta Drive con factura/fotos/piezas/video/lista, estado Ya reparé/Pendiente, envío de piezas dañadas), Aftersales propio (CAS/FWRC + reposición DJI). Pedidos actualizados: USD netos con 40% descuento ya aplicado, carga masiva SKU,Cantidad, catálogo/despiece, retiro/envío, backorder SLA 48hs, confirmar recepción. FAQ actualizada. Solo contenido estático, sin JS |
| Index.html | 1.63 | Panel de "Envío de repuestos dañados" en el detalle de la OT, SOLO para el flujo Reseller común (r.flujo==='Reseller', no Propio) y mientras el caso está activo (paso 2-3). Muestra, con el N° de OT bien grande para escribir en el paquete: dirección completa de EUCAWOODS (SIPCA, Carmen de Areco, CP 6725 + link a maps), horario, contacto de Mariano Castronuevo (tel: clickable), el recordatorio de adjuntar el comprobante/tracking/remito por la mensajería, y el aviso de que al recibir OK se envían los repuestos de reemplazo o la nota de crédito. Botones: "Etiqueta de envío" (reusa imprimirEtiqueta) y "Cargar tracking / remito" (_envioAdjuntarComprobante lleva a la mensajería con texto pre-cargado). Toggle en mostrarResultado; el accion redundante de "Informe de reparación" se recortó en RS_OTs 1.12. Solo front, sin re-auth |
| Index.html | 1.62 | Aviso en la pestaña "Curso": badge rojo (#badge-curso, muestra "!" o el nº de eventos pendientes) + pulso (clase camp-viva) cuando hay un evento abierto que este reseller no respondió (antes de loguear, resalta si hay cualquier evento activo). _cursoAplicarBadge(evs) cuenta los eventos con miInscripcion==null; _cursoCheckBadge() lo consulta con obtenerEventosPortal. Se llama en window.onload, tras el login (con el reseller ya identificado), al renderizar la pestaña y al guardar una inscripción (limpia el aviso si no quedan pendientes) |
| RS_Main.js | 1.1 | doGet: nueva ruta action==='resumen-curso' → _paginaResumenCurso(t, ev) para el panel interno de inscripciones. No cambia el resto del ruteo ni appsscript.json |
| RS_Eventos.js | 1.1 | Panel interno de inscripciones (página web con token, mismo patrón que _tokenAprobacion): _tokenResumenCurso() (SHA-256 de 'resumen-curso|APPROVAL_SECRET'), urlResumenCurso() (correr en el editor → devuelve/loguea el link .../exec?action=resumen-curso&t=<token>) y _paginaResumenCurso(token, soloEvento) que renderiza, por evento, tarjetas con total de personas + resellers que van + no asisten y una tabla Reseller · Personas · Asistentes (nombre + email). Se sirve desde el doGet de RS_Main.js. Solo lectura, sin scopes nuevos |
| RS_Eventos.js | 1.0 | NUEVO módulo EVENTOS/CURSOS: inscripción de resellers desde el portal (reusable para futuros eventos). Hoja EVENTOS (config: ID, Título, Fecha, Lugar, Descripción, Fecha límite, Activo — vacío/SI visible) que carga el equipo interno; hoja INSCRIPCIONES_EVENTOS con 1 fila por asistente (Fecha, EventoID, Evento, Reseller, Email reseller, Asiste SI/NO, Nombre asistente, Email asistente, Comentario), ambas auto-creadas con fila de ejemplo. Funciones: obtenerEventosPortal(reseller) → eventos activos + la inscripción previa del reseller (prellenado); guardarInscripcionEvento(eventoId, reseller, {asiste, asistentes:[{nombre,email}], comentario}) hace UPSERT por evento+reseller (borra filas previas e inserta las nuevas, LockService), completa email del reseller desde Resellers col J y valida ≥1 asistente/máx 50; resumenInscripcionesEvento(eventoId) interno (Ver registro) → totalResellers/totalPersonas + lista con nombres. Sin GmailApp, sin scopes nuevos → sin re-auth |
| Index.html | 1.61 | Pestaña nueva "Curso" (tb-curso, tab-curso) para inscribirse a cursos/eventos. Gateada por PIN como Pedidos/Cotizador (pedirPinCurso → usa resellerActual). Muestra los eventos activos (obtenerEventosPortal); por evento: datos (fecha/lugar/descripción/límite), toggle "✓ Sí, voy / No puedo ir", lista de asistentes (nombre + email OPCIONAL) con "Agregar asistente" y contador en vivo, comentario, y "Guardar inscripción" → guardarInscripcionEvento. Prellena la inscripción previa y muestra banner "Ya estás anotado — N personas". Funciones _curso*: Mostrar/Cargar/Render/EventoHTML/Init/AddAsist/SetAsiste/Sync/Estado/Refrescar/Guardar + helper _cAttr. Reusa toast()/_escHtml(). Hook post-login (goTab==='curso' → _cursoCargar). Solo front + backend RS_Eventos.js, sin re-auth |
| Env.js (Portal) | 1.8 | SCHEMA.SHEETS += EVENTOS:'EVENTOS' e INSCRIPCIONES_EVENTOS:'INSCRIPCIONES_EVENTOS' (para el módulo de inscripción a cursos/eventos de RS_Eventos.js) |
| Index.html | 1.60 | FIX definitivo del catálogo en la OT (regresión reportada: "agregué un repuesto desde catálogo, lo borré y ya no puedo entrar al modal de las subcategorías"). El síntoma —se elige modelo y subcategoría pero el click no abre el despiece— es la firma de un modal atrapado en un ancestro oculto (display:none). En 1.58 se resolvía reubicando #cat-modal/#cat-zoom al <body> en runtime (_catOverlayEnBody) la 1ª vez que se abría; ese reanclado por-timing es frágil. Fix estructural: se MOVIÓ el markup de #cat-modal y #cat-zoom FUERA del tab de Pedidos (#tab-ped) a nivel <body> (junto a #spin/#toasts/#ped-loading-overlay), así su visibilidad ya no depende de qué tab está activo. _catOverlayEnBody pasa a re-anclar SIEMPRE de forma idempotente (blindaje contra hermanos tardíos o contenedores ocultos). Un solo #cat-modal/#cat-zoom (sin duplicados). Solo front, sin backend ni re-auth |
| Index.html | 1.59 | Carrito de repuestos de la OT: ahora se puede MODIFICAR la cantidad (antes solo se podía eliminar). _renderRepNueva pinta la celda Cant. con controles −/input/+ (mismo patrón que _pedQty/_pedQtyInput de Pedidos): nuevas funciones _repNuevaQty(idx,delta) (clamp mín. 1) y _repNuevaQtyInput(idx,val) (onchange, restaura sin re-render si el valor es inválido para no perder el foco). De paso se escapan sku/descripcion con _escHtml (evita romper la tabla con < o &). Solo front |
| Index.html | 1.58 | FIX del catálogo en la OT (1.57): al abrir el catálogo desde el flujo de la OT se elegía modelo y se veía la subcategoría, pero el click NO abría el modal del despiece. Causa: #cat-modal y #cat-zoom viven dentro del tab de Pedidos (#tab-ped); en la OT ese tab está en display:none → los overlays, aunque sean position:fixed, no se muestran por tener un ancestro oculto. Fix: nuevo _catOverlayEnBody(id) que reubica el overlay al <body> la primera vez que se abre (appendChild preserva ids, inline onclick y addEventListener) → se muestra desde cualquier flujo. Aplicado en _catAbrir (cat-modal) y _catZoomAbrir (cat-zoom). Sin cambios de DOM estático ni backend. Pedidos sigue igual (el modal ahora vive en body, mismo comportamiento visual) |
| Index.html | 1.57 | "Repuestos para esta reparación" (OT) ahora es EXACTAMENTE igual a "Agregar Repuestos" (Pedidos): mismo buscador rápido por índice local, modo CATÁLOGO (despieces DJI), auto-SUSTITUCIÓN de SKUs reemplazados y explosión de kits. Enfoque: componente compartido con contexto _repCtx ('ped'|'ot'). Nuevos helpers _repCartActivo/_repIds/_repRender/_repBuscar. Se volvieron context-aware _pedAgregarItem (qty/cart/render/limpiar/focus + kits), _pedBuscar, _pedRenderResultados, _pedAgregarDesconocido, _pedLimpiarBusqueda, _catAgregar y los 3 render de catálogo (cat-conjuntos → wrap activo). _pedSetMode fija ctx='ped'; _repNuevaModo fija ctx='ot', suma la rama catálogo y carga lazy _pedCargarIndice()/_catCargar(); al mostrarse el panel de la OT (esLocal) también se precarga el índice. Markup del panel OT rehecho a 3 modos (Buscar SKU + Catálogo + Excel) con #repnueva-cat-conjuntos y #repnueva-no-result (pedido especial). El modal de despiece #cat-modal es único/compartido; los carritos _pedCart y _repNueva quedan INDEPENDIENTES. La sustitución la hace client-side _pedAgregarItem → enviarCasoAlHub graba el SKU ya sustituido, sin cambios de backend. El Excel de la OT sigue crudo. buscarRepNueva/_addRepNueva quedan sin uso (código muerto inofensivo). Solo front, sin re-auth |
| Index.html | 1.56 | Historial de cotizaciones: nuevo botón verde "Pedir repuestos" por cotización (solo si tiene repuestos) que carga esos repuestos al carrito de RS_Pedidos y salta a esa sección. Nueva func _cotPedirRepuestos(id): parsea itemsJson (data.repuestos), navega con showTab('ped','tb-ped')+_pedMostrarMain() y RECIÉN DESPUÉS mergea al _pedCart (porque _pedMostrarMain restaura el carrito guardado y lo pisaría), con estado 'consultar_Backorder'. La mano de obra no va (pedidos = solo repuestos). Precio del carrito es cosmético: confirmarPedidoPortal reprecia server-side con _buildPriceMap; igual _cotAplicarPreciosPedido reintenta hasta ~5s a que cargue _pedIndice y pone el precio del reseller (col 4) para que el carrito no muestre 0. Espeja el patrón de _pedRepetirPedido. Solo front |
| RS_OTs.js | 1.12 | El accion del estado "Informe de reparación" (flujo Reseller común) ya no repite la dirección de Carmen (DIR_CARMEN): ahora dice "Envianos las piezas dañadas siguiendo las instrucciones de más abajo", porque Index.html 1.63 muestra un panel completo de envío de repuestos dañados. DIR_CARMEN sigue usándose en el flujo Taller (envío del equipo). Sin re-auth |
| RS_OTs.js | 1.11 | FIX integración Portal→HUB PRO: los repuestos que carga el reseller en una OT nueva (enviarCasoAlHub) se unían con '\n' en la col Q (REPUESTOS), pero el HUB los lee con split(' ; ') → veía TODO como un solo repuesto y tomaba solo el primero (el resto quedaba pegado en el campo cantidad y se perdía). Ahora se unen con ' ; ' (separador canónico del HUB, el mismo que usa HUB Index.html al guardar). El mail no se ve afectado: se arma aparte desde el array JSON (RS_Email.js join('<br>')). NOTA: OTs creadas ANTES de este fix quedan con '\n' en la hoja y el HUB las sigue leyendo mal hasta que se editen en el HUB o se les sumen repuestos desde el Portal (RS_Repuestos 1.8 las auto-sana al mergear). Cleanup: el header tenía dos @version (1.0 stale + 1.10 real) → consolidado a uno. Sin re-auth |
| RS_OTs.js | 1.10 | Unicidad de S/N alineada al criterio del HUB PRO: el chequeo de duplicado en enviarCasoAlHub usaba un set terminal más chico ({Finalizado, Entregado, CANCELADO}) que el _esCerrada del HUB, así que Portal y HUB decidían distinto (Portal bloqueaba contra OTs en 'Rechazado DJI'/'Sin respuesta · Cerrado', el HUB no). Ahora ESTADOS_TERMINAL del Portal incluye también 'Rechazado DJI' y 'Sin respuesta · Cerrado' → mismo set que HUB_Código.js _ESTADOS_CERRADOS. Ambos sistemas escriben/leen la MISMA hoja 'Ordenes de trabajo', así que ahora deciden idéntico qué cuenta como caso abierto para el mismo equipo. Mantener sincronizado con HUB si cambia el set |
| RS_Pedidos.js | 2.7 | FIX botón "Confirmar recepción" que reaparecía en el HISTORIAL DE PEDIDOS DE REPUESTOS. Causa: RS_confirmarRecepcion registraba la confirmación SOLO en la hoja de WOS (Pedidos_resellers) y NUNCA en la hoja propia del Portal (PEDIDOS_REPUESTOS). El historial deriva estadoSimple del cruce con WOS → si ese cruce cambia/falla, el botón vuelve. Ahora la confirmación es AUTORITATIVA en el registro propio del pedido: (1) nuevo helper _rsMarcarPedidoConfirmado(numero) setea PEDIDOS_REPUESTOS.ESTADO='Entregado_Confirmado' + invalida su cache; se llama SIEMPRE al confirmar (aunque no haya líneas WOS para cerrar); (2) obtenerHistorialPedidosPortal, si el pedido tiene estado propio 'Entregado_Confirmado', lo mapea a 'recibido' ANTES del cruce con WOS → gana sobre WOS y el botón no reaparece. Pedidos confirmados ANTES de este fix se auto-corrigen al re-confirmar. Sin re-auth |
| RS_OTs.js | 1.9 | FIX cache: confirmarRecepcionRepuestos (repuestos de una OT/reparación: "Repuestos enviados" → "En reparacion") era la ÚNICA mutación de la hoja OT que NO llamaba invalidateSheetValues(SCHEMA.SHEETS.OT) tras escribir (aprobarPresupuesto, guardarCliente, marcar repuestos sí lo hacen). Como _leerOrdenes lee vía getSheetValues (cache, TTL 60s), el botón "Confirmar recepción" reaparecía por ~1 min tras confirmar. NOTA: esto explica solo una reaparición corta (60s), no "otro día" — si el reclamo del reseller es sobre el HISTORIAL DE PEDIDOS DE REPUESTOS (otro flujo, RS_confirmarRecepcion, lee/escribe fresh sin cache), la causa es otra y hay que verlo con un nº de pedido real |
| Index.html | 1.55 | Carpeta Drive de nueva OT: se agregó un link/botón "Ver carpeta modelo" (abre en nueva pestaña la carpeta de ejemplo 1YtJc3oC0QrXcbRe2s-WgsSEweMcLeJAF) dentro del panel #panel-drive-link, entre el input #inp-drive y la nota, para que el reseller vea cómo tiene que armar la suya. También se reforzó la nota informativa ("Mirá la carpeta modelo de arriba…"). Solo UI, sin cambios de backend ni de validación del link |
| Index.html | 1.54 | Catálogo Explotadas: los ítems de referencia tipo "Vea X" (ej. T50 "Tanque de liquidos 1" N2 "Vea Bomba de impulsor", N3 "Vea Caudalimetro") ya NO se muestran como comprables. El parser los había tomado como SKU (col Material Number) → el front mostraba "A consultar" + botón +. Ahora _catRefName detecta el prefijo (vea/véase/see) y _catFindConjunto ubica el conjunto destino por nombre (normalizado sin acentos/may.); la fila se pinta como referencia (SKU/precio "—", sin agregar) con un botón "Ver conjunto →" que abre ese conjunto con _catAbrir (o texto "Se pide en otro conjunto" si no se encontró). Sin cambios de datos ni backend |
| Index.html | 1.53 | Catálogo Explotadas: el modal del conjunto ahora tiene flechas ‹ › en el header para desplazarse entre conjuntos del mismo modelo (ej. en "Tanque de liquidos 1": ← abre "Cables", → abre "Tanque de liquidos 2"), con contador de posición "N / total" y tooltip del conjunto vecino. _catNav(dir) avanza ±1 sobre _catModeloActual.conjuntos (sin wrap: en los extremos el botón se deshabilita/atenúa). También navega con teclado ← / → (salvo cuando el foco está en un input/select). _catAbrir actualiza el estado de los botones y resetea el scroll al cambiar. Solo front, sin backend ni re-auth |
| Index.html | 1.52 | Cotizador: botón "Repetir" en el historial de presupuestos (igual que "Repetir pedido" de RS_Pedidos). _cotCargarHistorial guarda el itemsJson por id (_cotHistorialItems) y renderiza el botón azul; _cotRepetirCotizacion(id) parsea { repuestos, manoObra }, mergea repuestos al carrito (por sku+desc, suma cantidades), reprecia cada SKU vigente desde _pedIndice (así el carrito coincide con el PDF final) y reproduce la mano de obra; toast + scroll al carrito. Sin re-autorización |
| RS_Cotizador.js | 1.8 | obtenerHistorialCotizaciones ahora devuelve itemsJson (col 8: { repuestos, manoObra }) para que el front pueda "Repetir" un presupuesto. Sin otros cambios |
| Index.html | 1.51 | Cotizador: la búsqueda por SKU ahora respeta los códigos de reemplazo igual que RS_Pedidos. _cotRenderResultados muestra el indicador naranja "Reemplazado por X" y _cotAgregarItem auto-sustituye: si el repuesto está reemplazado, cotiza el nuevo SKU (busca su desc/modelos/precio en _pedIndice, recalcula el PVP con _cotBaseDesde40) y avisa con toast "viejo → nuevo (reemplazado automaticamente)". Reutiliza el mismo _pedBuscarLocal que ya compartía; sin cambios de backend (RS_Cotizador prizea por SKU, así que al sustituir el SKU ya cotiza bien). No cubre kits (solo reemplazos, que es lo pedido) |
| Index.html | 1.50 | validarSN acepta VARIOS prefijos de S/N separados por coma (DJI cambió de lote y los S/N pueden empezar de 2 maneras): el S/N vale si arranca con CUALQUIERA de los prefijos de la col PREFIJO de EQUIPOS; el cartel lista todos ("...debe comenzar con \"SP\" o \"SR\""). Antes hacía startsWith contra el valor completo, así que una coma no servía. Además se RESOLVIÓ el merge main←dev de este archivo: login OBLIGATORIO (dev: el modal de PIN se re-abre si se cierra sin sesión), logout con el fix de recarga vía window.top (dev), tab inicial Recursos, label "Nueva Reparación" con acento |
| Env.js | 1.7 | Resolución de merge main←dev: se tomó la base de dev PERO se CONSERVÓ la variable LISTA_PRECIOS_SS_ID (la usa RS_Repuestos.js 3 veces) que dev había perdido → sin ella el listado de repuestos/accesorios tiraría ReferenceError |
| RS_Pedidos.js | 2.6 | Resolución de merge main←dev: se conservó el comentario de dev sobre usar getSheetByName (no getActiveSheet). Sin cambios funcionales |
| RS_Pedidos.js | 2.5 | Carga super-RTV · dirección de envío: confirmarPedidoPortal acepta params.direccionEnvio → cuando modoSuper y envío ≠ Retiro, la usa como resellerMeta.direccion (bloque del cliente en el PDF) y la antepone a observaciones (así aparece en la planilla col N y en el mail a logística). Retiro no pide dirección. Forma de pago la resuelve el front ("Pagado" por defecto) |
| Index.html | 1.49 | Carga super-RTV · entrega/pago: en el card del carrito, campo "Dirección de envío" (#ped-super-dir) que aparece solo en modo super y cuando Entrega = Envío (_superToggleEnvio, oculto en Retiro); es obligatorio para confirmar un Envío. Forma de pago se pre-carga en "Pagado ✅" (estos pedidos ya vienen pagos; el super puede cambiarla). _pedGenerarPedido valida la dirección y la pasa como params.direccionEnvio |
| RS_Pedidos.js | 2.4 | Carga super-RTV: el super (soporteagrasdji@bidcom.com.ar) puede cargar pedidos a nombre de CUALQUIER cliente o reseller, con descuento GLOBAL manual (0% = PVP). confirmarPedidoPortal acepta params.modoSuper → re-verifica el email de sesión con _esRTVSuper (server-side, no confía en el cliente), usa params.cliente como nombre (col B), params.emailCliente como CC y params.descuentoPct como descuento global (nuevo _descInfoResolve: override 0-100 o cae al dto del reseller). buscarRepuestoConStockPortal y obtenerIndiceRepuestosPortal reciben pctOverride opcional para mostrar precios con el descuento del super. Mismo circuito: Pedidos_resellers → WOS → descuenta Carmen. Reseller normal: sin cambios (override undefined → dto del reseller) |
| Index.html | 1.48 | Modo super-RTV "Cargar pedido": botón en la vista RTV (solo esSuper) que abre el flujo de pedido completo (catálogo/buscador/carrito/kits/stock) en contexto de un cliente arbitrario. Card #ped-super-card con Cliente/Reseller (datalist de resellers), Email opcional y Descuento % global (0 = PVP). Estado _superPedido + helpers _pedCtxReseller/_pedDtoOverride; _pedCargarIndice y _pedBuscar pasan el override; cambiar el descuento re-precia catálogo y carrito (_superReprecioCarrito por ratio). _pedGenerarPedido/_pedConfirmarRevision agregan modoSuper/cliente/emailCliente/descuentoPct; se saltea la validación de dirección (no hay perfil). _pedMostrarMain ramifica: en super no carga historial/borradores. Volver a RTV limpia el modo. Reseller normal: idéntico |
| RS_RTV.js | 1.5 | obtenerDatosRTV devuelve resellerRtv { nombre → emailRTV } (solo autorizados) para que el super filtre por RTV en el front. Sin cambios en obtenerPedidosRTV/obtenerOrdenesRTV |
| RS_RTV.js | 1.4 | Super-RTV: whitelist _RTV_SUPER (soporteagrasdji@bidcom.com.ar) que ve TODOS los resellers, no solo los de su columna EMAIL_RTV. Helper _resellersAutorizadosRTV(email) centraliza el mapa de autorización (super→todos / normal→por EMAIL_RTV) y lo usan obtenerDatosRTV (devuelve resellers ordenados + esSuper), obtenerPedidosRTV y obtenerOrdenesRTV. Solo lectura, autorización server-side por identidad Google (Session email). Sujeto a los topes existentes (200 pedidos / 400 órdenes más recientes) |
| RS_RTV.js | 1.3 | Cumplimiento RTV maneja pedidos CANCELADOS: _mapaCumplimientoPedidos lee col J (ESTADO) y aplica el fallback de WOS (línea con estado "Cancelado" sin CANT_CANCEL → se cancela lo solicitado). _resumenCumplimiento marca anulado=true cuando el pedido está TOTALMENTE cancelado (base<=0 y cancelado>0) → así no aparece como "0% pendiente". Un pedido con algunos entregados y otros cancelados sigue siendo parcial/completo sobre lo no-cancelado (mismo criterio que WOS) |
| RS_RTV.js | 1.2 | Vista RTV de pedidos ahora muestra CUMPLIMIENTO por pedido (entregado/pendiente/%). Nuevos _mapaCumplimientoPedidos() (lee Pedidos_resellers una vez → numero→{solicitado ΣE, entregado ΣF, cancelado ΣZ, items por SKU}) y _resumenCumplimiento(reg) (calcula pendiente = (solicitado-cancelado)-entregado y % = entregado/(solicitado-cancelado), en UNIDADES, con detalle por ítem). obtenerPedidosRTV adjunta `cumplimiento` a cada pedido. Solo lectura, autorización server-side sin cambios |
| Index.html | 1.47 | Historial: botón "Confirmar recepción" solo si el pedido está COMPLETO (est === 'enviado'; antes salía también en 'enviado_parcial') y desaparece una vez confirmado (estado 'recibido', que ya no lo muestra). La fila de despacho (NE/tracking) ahora también se ve en estado 'recibido'. Se apoya en el estadoSimple 'recibido' que ahora devuelve el backend |
| Index.html | 1.46 | RTV · selector de RTV para el super: nuevo #rtv-sel-rtv (oculto salvo esSuper) que lista los RTV distintos con conteo de resellers (+ "Todos los RTV" y "Sin RTV asignado"). Al elegir un RTV se acota el sub-selector de resellers a ese RTV y se filtra la lista (_rtvPoblarSelRtv/_rtvPoblarSelReseller/_rtvResellerCoincide/_rtvOnFiltroRTV/_rtvAplicarFiltros). _rtvFiltrar y _rtvFiltrarOrdenes usan el filtro combinado (RTV + reseller); la tarjeta de cumplimiento general recalcula sobre el subconjunto. RTV normal: selector oculto, sin cambios |
| Index.html | 1.45 | RTV · ventana de carga al ingresar: reusa el overlay full-screen #ped-loading-overlay (ahora _pedMostrarCargando(steps) acepta pasos custom) con _rtvLoadingSteps ("Verificando acceso RTV" → "Cargando pedidos de tus resellers" → "Calculando cumplimiento"). _iniciarSesionRTV lo muestra y se oculta (_rtvOcultarEntryLoading) al terminar la carga de pedidos (éxito o error). El refresco manual sigue con el spinner inline (flag _rtvEntryLoading solo en el ingreso) |
| Index.html | 1.44 | RTV · % general EXCLUYE los pedidos "pendientes" (0% entregado, aún sin definir): en _rtvResumenHTML no suman a totEnt/totBase, solo cuentan en su chip. El % ahora se calcula únicamente sobre pedidos con entregas iniciadas (parcial/completo). Nota chica bajo el % cuando hay pendientes excluidos. Cancelados seguían fuera |
| Index.html | 1.43 | RTV · pedidos CANCELADOS: si el pedido está totalmente cancelado (cumplimiento.anulado) el encabezado muestra chip "Cancelado" (no 0% rojo), el detalle muestra banner "Pedido cancelado — N u. canceladas" en vez de la barra, y en la tabla el ítem anulado dice "Cancelado" en Pendiente (Pedido='—'). El resumen general cuenta "cancelados" en su propio chip y los excluye del % y de "pendientes" |
| Index.html | 1.42 | RTV · pedidos: tarjeta de resumen arriba de la lista (_rtvResumenHTML/_rtvChip) con cumplimiento GENERAL ponderado por unidades (Σentregado/Σ(solicitado-cancelado)) + barra + conteo completos/parciales/pendientes/sin datos. Se antepone en _rtvRenderPedidos → recalcula según el filtro por reseller |
| Index.html | 1.41 | RTV · pedidos: barra de cumplimiento + % en el encabezado de cada pedido (verde 100% / ámbar parcial / rojo 0%, helper _rtvCumplBar) y detalle reescrito (_rtvDetalleHTML): resumen entregado/pendiente/cancelado + tabla por ítem con columnas Pedido/Entregado/Pendiente (✓ si completo). Fallback al snapshot (cantidad pedida) si el pedido aún no tiene filas en Pedidos_resellers |
| Env.js | 1.6 | Descuento por reseller: SCHEMA.RESELLERS.DESCUENTO = 17 (col R). % de descuento sobre lista; vacío = 40% (histórico), 0 = sin descuento. Se localiza por encabezado "Descuento"/"Dto" si existe; este índice es fallback |
| RS_Pedidos.js | 2.3 | "Confirmar recepción" solo en pedidos COMPLETOS y una sola vez: _mapEstadoWosSimple ahora mapea Entregado_Confirmado → 'recibido' (antes → 'enviado', por eso el botón reaparecía al recargar tras confirmar). obtenerHistorialPedidosPortal cuenta líneas recibido y calcula estadoSimple: entregadas===noCancel → 'recibido' si ya hay confirmación, si no 'enviado'; parcial sigue 'enviado_parcial'. Así el botón (que ahora solo se muestra en 'enviado') no sale en parciales ni vuelve tras confirmar |
| RS_Pedidos.js | 2.2 | FIX mail duplicado de "pedido cerrado": RS_confirmarRecepcion no tenía lock ni guarda → 30 clicks del botón "Confirmar recepción" cerraban 30 veces y mandaban 30 mails. Ahora: LockService (serializa clicks) + guarda de idempotencia (si el pedido ya está Entregado_Confirmado, return temprano sin re-cerrar ni reenviar) + el mail de cierre se manda UNA sola vez (solo en la transición real, actualizados>0). Nuevo _rsEnviarCierreRecepcion(numero, reseller, threadId): responde en el hilo del pedido (Thread_ID col R) o mail directo al reseller. No colisiona con el cierre por respuesta de mail de WOS: quien setee Entregado_Confirmado primero bloquea al otro (WOS escanea Entregado_Cerrado) |
| RS_Pedidos.js | 2.1 | Descuento DISCRECIONAL por reseller (antes 40% fijo para todos). Nuevo _resellerDescuentoInfo(nombre) → {pct, factor} leyendo la columna de la hoja Resellers (por encabezado "descuento"/"dto", fallback SCHEMA.RESELLERS.DESCUENTO); vacío/no hallado/inválido = 40%, clamp 0–100, factor=(100-pct)/100. _buildPriceMap(factor) parametrizado (default 0.60). Aplican el factor del reseller: obtenerIndiceRepuestosPortal(reseller) (devuelve descuentoPct), buscarRepuestoConStockPortal(query, reseller) y confirmarPedidoPortal (usa params.reseller = sucursal en grupos). Accesorios usan el mismo factor. PDF/email: _generarPdfPedido/_enviarEmailPedidoPortal reciben descInfo → revierten PVP con /factor y rotulan "N% dto." (o "sin descuento" si 0). El Cotizador queda intacto (usa _buildPriceMap() default 0.60 y revierte a PVP de lista, independiente del dto de compra) |
| Index.html | 1.40 | Descuento por reseller en pantalla: nueva global _pedDtoPct (la trae obtenerIndiceRepuestosPortal) + helpers _pedFactor/_pedTieneDto/_pedPvpDesde. Se pasa resellerActual a obtenerIndiceRepuestosPortal y buscarRepuestoConStockPortal. Todos los "/0.60" y "-40%" hardcodeados (resultados de búsqueda, carrito ítem+total, revisión, y _cotBaseDesde40 del cotizador) ahora usan el factor real: PVP tachado y badge "-N%" solo si hay dto (>0); si el reseller es 0% muestra solo el precio de lista sin tachado. Guía: "40% de descuento" → "tu descuento asignado" |
| Index.html | 1.39 | BUGFIX cerrar sesión: la web quedaba TODA en blanco. cerrarSesion() hacía location.reload(), que dentro del iframe sandbox de Apps Script recarga el frame interno y lo deja en blanco (el contenido se sirve vía el wrapper de Google, no es recargable). Fix: navega la ventana TOP al web app (window.top.location.href = _deployUrl) → doGet fresco que muestra el login; se mantiene el flag portal_logout en sessionStorage para forzar el PIN y evitar auto-login. Fallback a location.reload() si no hay _deployUrl |
| Index.html | 1.38 | BUGFIX "Solicitar acceso al Portal": no se podía escribir en los campos del formulario de registro. Se abría (abrirRegistro) haciendo loginM.hide() + _regModal.show() sincrónicamente; como hide() de Bootstrap es asíncrono, el focus-trap del modal de login seguía activo y robaba el foco (los clicks/select funcionaban pero no se podía tipear en los inputs de texto del paso 2). Fix: se espera el evento hidden.bs.modal del login antes de mostrar el registro (nueva _mostrarRegistro); si el login no está abierto, muestra directo |
| Index.html | 1.37 | Cotizador · Mano de obra: se elige del dropdown y se agrega SOLO (onchange="_cotAgregarMO()"), sin botón "Agregar" (era confuso). Se quitó el botón; _cotAgregarMO al volver a la opción vacía no hace nada (sin toast de error). Sigue siendo una sola mano de obra por presupuesto (reemplaza la anterior) con toast "agregada/actualizada" |
| Index.html | 1.36 | El botón "Catálogo" (sub-modo de Agregar Repuestos) ahora tiene el mismo movimiento que el tab Campaña para invitar a usarlo: el ícono pulsa (reusa @keyframes camp-pulse vía clase .cat-nuevo, igual que .camp-viva) y se le agregó un pill "Nuevo". El pulso se frena cuando el modo está activo (.cat-nuevo.active i → animation:none) |
| Index.html | 1.35 | Catálogo MULTI-MODELO. obtenerCatalogoExplotadas ahora devuelve {modelos:[...]}. Nuevo paso de selección de modelo (_catRenderModelos/_catElegirModelo): si hay >1 modelo el reseller elige primero (T100 / T70P) y luego el conjunto; con "← Cambiar modelo". Si hay 1 solo, salta directo. _catRenderConjuntos/_catAbrir usan _catModeloActual. Compat con el formato viejo de un modelo por las dudas |
| Index.html | 1.34 | Catálogo (modal detalle): la imagen ya no abre el lightbox al hacer clic (zoom "a lo loco"). Ahora al pasar el mouse sobre la imagen aparece una LUPA que sigue al cursor (magnifier lens circular de 190px, aumento 2.6×, _catLens/_catLensHide) — el cursor se vuelve crosshair y el lente muestra la zona ampliada, corrigiendo object-fit:contain (calcula el rect real de la imagen) y clampeando el lente al box con fórmula general de background-position. El botón de la esquina pasó a fa-expand = "ver a pantalla completa" (abre el lightbox con rueda/arrastre) como opción explícita, ya no es el clic accidental |
| Index.html | 1.33 | Catálogo (modal detalle): 1) el botón "Ampliar" se reemplazó por una lupa discreta (ícono circular fa-magnifying-glass arriba a la derecha de la imagen) — ocupa mucho menos. 2) La tabla de repuestos ahora muestra columna PRECIO al reseller: cruza el SKU con _pedIndice (lista de precios, mismo índice que "Buscar SKU") y muestra el precio con _fmtPedUsd; si no tiene precio en su lista muestra "A consultar" (naranja). Se ensanchó un poco la columna derecha (flex 1 1 340px) para las 5 columnas |
| Index.html | 1.32 | Catálogo: el detalle del conjunto ahora abre en un MODAL grande (#cat-modal, min(1500px,96vw) x 95vh) por encima de la página en vez de estirar el layout — el fondo (880px) queda intacto y armónico. Adentro: imagen grande a la izquierda (columna flex:2, clic o botón Ampliar → lightbox de zoom) y lista de repuestos a la derecha con scroll propio + encabezado fijo. Cierra con la X, clic en el fondo o Esc (Esc cierra primero el zoom si está abierto). La grilla de conjuntos queda visible debajo |
| Index.html | 1.31 | Catálogo: el detalle rompe el límite de ancho del .wrap (880px) y usa min(1500px,96vw) centrado (left:50% + translateX(-50%)) → imagen mucho más grande, aprovecha el ancho de la pantalla. Columna imagen más ancha (flex:1.9), imagen width:100% max-height:82vh. Nuevo lightbox de ZOOM (#cat-zoom): clic en la imagen o botón "Ampliar" → pantalla completa con rueda para zoom (1–7×, hacia el cursor no, centrado), arrastrar para mover, doble clic alterna, Esc/clic-fondo cierra, botones +/−/1:1 e indicador de %. Fallback _catImgError ajustado (la img ahora va dentro de un contenedor relativo con el botón Ampliar) |
| Index.html | 1.30 | Catálogo: layout del detalle a 2 columnas (flex-wrap) — imagen a la IZQUIERDA (sticky, max-height 74vh, object-fit contain) y lista de repuestos a la DERECHA con scroll propio (max-height 74vh, overflow auto) y encabezado de tabla fijo (thead sticky). En pantallas angostas las columnas se apilan. Antes estaba todo apilado y costaba relacionar número↔pieza |
| Index.html | 1.29 | Catálogo: fallback de imagen. La imagen de cada conjunto intenta lh3.googleusercontent.com/d/ID=w1600 y, si falla, drive.google.com/thumbnail?id=ID&sz=w1600 (helper _catImgError con data-thumb), y recién ahí muestra el mensaje de error. Así el reseller ve la foto aunque un formato de embed esté bloqueado |
| RS_Catalogo.js | 2.2 | Se ocultan del catálogo 2 variantes internas no vendibles al reseller: BC.AG.SS000674 "Aerial-Electronics System Module (CN)" y BC.AG.SS000837 "(H)" (T50 "Placas principales" N12). gen_catalogo.js ahora tiene una lista HIDE_SKUS que filtra esos ítems al generar → no aparecen en la lista ni se pueden agregar. Se CONSERVA la variante (L) BC.AG.SS000838 (no estaba en el pedido) y los "Aerial-Electronics Module (Overseas)" de T70P/T100. T50: 398→396 ítems; T100/T70P sin cambios |
| RS_Catalogo.js | 2.1 | Se suma el modelo DJI Agras T50 al catálogo multi-modelo (15 conjuntos / 398 ítems, desde "T50 Aircraft_Material information 2023040327.xlsx"). Ahora _CATALOGO_EXPLOTADAS.modelos = [T100 (16c/355), T70P (14c/337), T50 (15c/398)]. Importador: parse.js ahora acepta el nombre de salida por CLI (node parse.js catalogo_t50.json) para no pisar el JSON de otros modelos; gen_catalogo.js descarta pestañas índice/sin ítems (la hoja "目录/List" del T50) y lista T50 en MODELS. FALTAN los diagramas numerados del T50 (render LibreOffice + subir a Drive + pegar en CATALOGO_IMAGENES; el backend ya auto-agrega las 15 filas vacías de T50). NB: el archivo venía en 2.0 (multi-modelo) sin fila en este registro; esta 2.1 la incorpora |
| RS_Catalogo.js | 1.2 | Recuperados los ítems con sub-número (variantes der/izq del mismo N°, ej. 7 y 7-2, 21 y 21-2, 24-1..24-5). El parser (Explotadas/_importador/parse.js) descartaba los "N-M" porque el regex era /^\d+$/ (solo dígitos puros) → caía al fallback de columna izquierda y perdía el número; ahora usa /^\d+(-\d+)?$/. Se regeneró SOLO la constante _CATALOGO_EXPLOTADAS (355 ítems vs 345, +10 recuperados) preservando las funciones v1.1. También se sincronizó gen_catalogo.js para que emita v1.2 (con _catDriveId + normalización) y no regrese a v1.0 si se re-corre | extrae el FILE_ID de cualquier formato de Drive (.../file/d/ID/view, ?id=ID, lh3 /d/ID) y obtenerCatalogoExplotadas devuelve imagenUrl ya incrustable (lh3.googleusercontent.com/d/ID=w1600) + imagenId para el fallback del front. Ahora se puede pegar en CATALOGO_IMAGENES el link de "Compartir" tal cual lo copia Drive |
| RS_Catalogo.js | 1.0 | NUEVO — Catálogo visual de despieces DJI (EPC). Datos generados desde el Excel oficial de DJI (T100 Material Information): 16 conjuntos, 345 ítems (N°·SKU·nombre). Constante _CATALOGO_EXPLOTADAS + obtenerCatalogoExplotadas() que mergea los links de imagen desde la hoja CATALOGO_IMAGENES (auto-creada por _asegurarHojaCatalogoImagenes con 1 fila por conjunto para pegar el link público de Drive). El precio/estado/kits/sustitución NO viven acá: los resuelve el frontend con _pedIndice |
| Index.html | 1.28 | Nueva pestaña "Catálogo" en Agregar Repuestos, entre "Buscar SKU" (intacto) y "Cargar Excel". _pedSetMode maneja el 3er modo 'catalogo'. Vista: grid de conjuntos del T100 → detalle con imagen de despiece + tabla numerada (N°·SKU·repuesto) + input de cantidad + botón agregar. _catCargar/_catRenderConjuntos/_catAbrir/_catAgregar. Al agregar, busca el SKU en _pedIndice (mismo índice que Buscar SKU) → reusa precio/estado/kits/sustitución vía _pedAgregarItem; si el SKU no está en la lista de precios se agrega como "a consultar" (precio 0, estado consultar_Backorder) igual agregable. Imagen por conjunto desde CATALOGO_IMAGENES con fallback si falta el link |
| Index.html | 1.27 | Vista RTV: al iniciar sesión RTV se ocultan las pestañas del reseller que no puede usar (Nueva Reparación, Mis Órdenes, Pedidos, Cotizador, Campaña, Mensajes). Quedan visibles Recursos, Novedades y Mis Resellers |
| RS_RTV.js | 1.1 | Nuevo obtenerOrdenesRTV(): devuelve las OTs de los resellers asignados al RTV, con autorización re-calculada server-side desde el email de la sesión (mismo criterio que obtenerPedidosRTV, no confía en el cliente). Reusa _leerOrdenes; incluye reseller, equipo, SN, estado, garantía, técnico, cliente, prioridad, días, fechas. Ordena activas primero. Se agrega header @version |
| Index.html | 1.26 | Vista RTV "Mis Resellers" ahora tiene subtabs Pedidos / Órdenes. Nueva subvista de OTs (obtenerOrdenesRTV) con cards (OT, equipo/SN, reseller, cliente, badge de estado, garantía, prioridad, días/fecha). _rtvSwitchTab/_rtvOnFiltro/_rtvActualizar/_rtvCargarOrdenes/_rtvFiltrarOrdenes/_rtvRenderOrdenes; contadores por subtab; el filtro de reseller aplica a la subvista activa. _iniciarSesionRTV carga ambas. Sin PIN (auth por cuenta Google) |
| Index.html | 1.25 | Botón "Salir" (cerrar sesión) ahora visible en el header, al lado de Actualizar (btn-header-logout, rojo tenue) — antes estaba escondido dentro de la tarjeta de Mis Órdenes. Se muestra en las 3 vías de login (sesión guardada, Google, PIN). cerrarSesion ahora pide confirmación y hace recarga limpia (location.reload) forzando el modal de PIN vía flag sessionStorage portal_logout (window.onload lo detecta y muestra pinModal, ignorando sesión guardada y auto-login por Google) |
| Index.html | 1.24 | login() ahora aterriza en "Recursos" tras validar el PIN (antes saltaba a "Mis Órdenes" + cargarDashboard, con demora visible). Completa el cambio de v1.23: el reseller cae en Recursos (estático, instantáneo) tanto por PIN como por sesión guardada. Si había _pendingTab (tab bloqueada que disparó el login), respeta ese destino. El dashboard sigue cargándose en segundo plano para los contadores. Por eso NO hace falta ventana de carga post-login |
| Index.html | 1.23 | Pestaña por defecto al ingresar ahora es "Recursos" (antes "Nueva Reparacion"): se mueve la clase show/active a tab-rec/tb-rec; cerrarSesion también vuelve a Recursos |
| Index.html | 1.22 | Novedades: _novIcono mapea categoría "Cotizador"/"Presupuesto"/"Herramienta" → ícono fa-file-invoice-dollar |
| RS_Novedades.js | 1.0 | NUEVO — CMS de Novedades por hoja. obtenerNovedadesPortal() lee la hoja NOVEDADES (Fecha, Título, Descripción, Categoría, Link URL, Texto del botón, Activo) y devuelve las activas ordenadas por fecha (más nuevas primero). _asegurarHojaNovedades autocrea la hoja con encabezado + fila de ejemplo del T55 (pegar link PPT en col E). _novActivo: vacío/SI=visible, NO/FALSE/0/OCULTO=oculto |
| Env.js | 1.5 | SCHEMA.SHEETS.NOVEDADES = 'NOVEDADES' (hoja-CMS de la pestaña Novedades del portal) |
| Index.html | 1.21 | Pestaña Novedades: botón en la barra (fa-bullhorn) con badge de "nuevas" (localStorage nov_lastseen, se limpia al abrir la pestaña). Panel tab-nov con cards (ícono por categoría, badge de categoría, fecha, título, descripción, botón al link/PPT). _cargarNovedades/_renderNovedades/_novIcono/_novActualizarBadge/_novMarcarVistas; se carga en window.onload para el contador. Reusa _escHtml. Contenido 100% editable desde la hoja NOVEDADES |
| Index.html | 1.20 | Tiempo promedio de envío: se muestra SOLO "con stock" (se quita el backorder de la vista). Queda "Envío promedio 1,2 días háb.". El backend sigue calculando ambos, pero la línea solo muestra con stock |
| Index.html | 1.19 | Tiempo promedio de envío ahora MUY sutil: se reemplazan las dos cajas grandes por una sola línea fina y tenue (11px, gris) arriba del historial — "Envío promedio · con stock 1,2 días háb. · backorder 6,4 días háb.". Se elimina _tiempoBox, nuevo _tiempoTxt |
| RS_Pedidos.js | 2.0 | Fix del tiempo promedio "Backorder": antes tomaba el despacho MÁS TEMPRANO → medía la parte con stock (que sale primero), no la espera del backorder. Ahora mide recibido → despacho del ÚLTIMO ítem que estaba en backorder (col I snapshot < col E cantidad, con fecha de despacho col O). Solo cuenta el pedido si TODAS sus líneas en backorder ya se despacharon (si la espera no terminó, se excluye). "Con stock" sigue usando el primer/único despacho. obtenerTiempoPromedioEnvioPortal reescrito con boLineas/boResueltas/boUltimoDespacho/primerDespacho |
| Index.html | 1.18 | Kits en vivo en el carrito: al agregar C, _pedAgregarItem lo explota a A + B (misma proporción) igual que la sustitución 1→1 — el reseller ve y confirma A y B, no C. Carga el mapa con obtenerKitsPortal (_pedKits) junto al índice. Toast "C → A + B (kit)". Aviso en resultados de búsqueda "Se pedirá como A + B" (violeta, estilo del "Reemplazado por"). El backend sigue explotando como red de seguridad |
| RS_Pedidos.js | 1.9 | Nuevo obtenerKitsPortal() → {kits:{KITSKU:[{sku,cantidadPorKit}]}} para explotar kits en vivo en el frontend (paralelo a REEMPLAZADO_POR). Idempotente con _explotarKits del backend |
| RS_Pedidos.js | 1.8 | Explosión de kits: si el reseller pide un SKU "kit", el portal lo reemplaza por sus componentes en la misma proporción (ej: 10 de C → 10 de A + 10 de B). Nueva hoja KITS (Kit SKU · Componente SKU · Cantidad por kit · Descripción opc.), auto-creada. Helper _explotarKits (descripciones desde DB_REPUESTOS, cantidad = pedida × cantidad por kit). Se aplica en confirmarPedidoPortal (antes del enforcement de precios → A y B se cotizan solos) y en revisarItemsConStock (la revisión ya muestra los componentes con su stock real). Ambos puntos idempotentes |
| Env.js | 1.4 | SCHEMA.SHEETS.KITS = 'KITS' (hoja de explosión de kits del carrito de repuestos) |
| RS_Pedidos.js | 1.7 | BUGFIX crítico: confirmarPedidoPortal escribía el pedido con SpreadsheetApp.openById(NOTAS_SS_ID).getActiveSheet() → si alguien tenía ese archivo abierto en otra pestaña, el pedido se escribía en la pestaña equivocada (no en "Pedidos_resellers"). Se mandaba el mail y quedaba en el historial (PEDIDOS_REPUESTOS) pero invisible para WOS despacho. Fix: getSheetByName('Pedidos_resellers'), consistente con todas las lecturas |
| RS_Cotizador.js | 1.7 | Descuento máximo por pieza 99% (evita 100% = gratis). Enforcement en _cotNormalizarItems (desc>99→99). Hardening antimanipulación: cantidad forzada a entero >=1 (Math.floor + isNaN), precioLista no negativo (isNaN/<0→0) |
| Index.html | 1.17 | Tiempo promedio de envío: ahora son DOS números en vez de uno — "Con stock" (verde) y "Backorder" (naranja), cada uno con su promedio de días hábiles + cantidad de pedidos. Card rediseñada con 2 cajas (_tiempoBox). Se quitó el rango min–max. Se oculta si no hay ningún pedido enviado en ninguna categoría |
| Index.html | 1.16 | Pedido de Repuestos: card "Tiempo promedio de envío" arriba del historial — muestra el promedio en días hábiles (Lun–Vie) desde que recibimos el pedido hasta despacharlo, con rango min–max y cantidad de pedidos considerados. _cargarTiempoEnvio/_renderTiempoEnvio; se refresca junto con el historial. Se oculta si no hay pedidos enviados |
| RS_Pedidos.js | 1.6 | Tiempo promedio de envío partido en dos: obtenerTiempoPromedioEnvioPortal devuelve {conStock:{muestras,promedio}, backorder:{muestras,promedio}}. Clasifica cada pedido por backorder usando el snapshot de stock de Carmen al cargar (WOS col I) vs cantidad pedida (col E): si algún ítem no tenía stock suficiente → bucket backorder, si no → con stock. Se quitó min/max. Manejo de envío en partes se difiere (por ahora usa el despacho más temprano) |
| RS_Pedidos.js | 1.5 | Nuevo obtenerTiempoPromedioEnvioPortal(reseller): promedio de días hábiles recibido→despachado sobre los pedidos enviados del reseller (cruza PEDIDOS_REPUESTOS fecha recepción con WOS Pedidos_resellers col 14 fecha despacho, mismo join que el historial; toma el despacho más temprano). Helper _diasHabilesEntre (Lun–Vie, excluye día inicial, incluye final). Devuelve {muestras, promedio, min, max} |
| Index.html | 1.15 | Guía "¿Cómo cargo una garantía?" en Nueva Reparación: botón verde en la help-bar abre modal con flujo paso a paso (timeline numerada). Flujo 1 "Reparé en mi taller": reparás con tu stock → armás carpeta Drive (checklist: factura, fotos recepción, piezas dañadas c/SN, piezas nuevas c/SN, video funcionando, lista repuestos) → ingresás → tipo "Reparación en mi taller" → datos equipo → estado "Ya reparé" → agregás repuestos cambiados → pegás link Drive → Registrar. Estructura _guiaGarantiaFlujos extensible + abrirGuiaGarantia() (selector de flujos aparece al agregar más) |
| Index.html | 1.14 | Cotizador: tope de descuento por pieza 99% — input max=99 y _cotSetDesc clampa a 99 (antes 100) |
| RS_Cotizador.js | 1.6 | Mano de obra sin cantidad: _cotNormalizarMO fija cantidad=1 (importe=precio). PDF: tabla MANO DE OBRA sin columna Cant./Subtotal → Descripción (cols 1-6) + Importe (col 7). Email: tabla MO a 2 columnas (Descripción + Importe) |
| RS_Cotizador.js | 1.5 | Cotizador full verde: email y header de la hoja COTIZACIONES pasan de violeta #6c5ce7 a verde #3a9e3a (tablas, botón PDF, SKU, número). Subtítulo del membrete PDF: "Servicio Técnico Oficial · Repuestos originales DJI · Mano de obra calificada" |
| Index.html | 1.12 | Cotizador full verde: todo el violeta del frontend (#6c5ce7 → #3a9e3a; rgba 108,92,231 → 58,158,58) — icono de la pestaña, info box, botones, total, inputs de descuento/precio, header de mano de obra en el carrito, links de historial. Ya no queda violeta en el cotizador |
| RS_Cotizador.js | 1.4 | PDF más corporativo/formal: cabecera tipo membrete (logo + subtítulo "Servicio Técnico Oficial · Repuestos DJI", bloque documento a la derecha SIN relleno, regla inferior verde SOLID_THICK); headers de tabla y MANO DE OBRA pasan a grafito #2d3436 (antes verde); filas alternadas a gris neutro #f6f7f8 (antes verde claro); SKU en grafito; borde de tabla neutro #dfe3e6. El verde queda como acento fino: logo + regla del membrete + monto TOTAL. Charcoal dominante |
| RS_Cotizador.js | 1.3 | PDF rediseño (paleta A): saca el violeta, verde de marca #3a9e3a como acento (headers de tabla, MANO DE OBRA, fila TOTAL) + grafito #2d3436 (bloque PRESUPUESTO/N°/Fecha y bandas EMISOR/CLIENTE) + filas alternadas verde claro #f2f8f2. SKU: columna a 140px con wrap (no se corta el código). Tablas con borde propio #d9e2d9. Pie legal nuevo: "emitido por [reseller] para [cliente]… solo por daños observados/descritos; si aparecen daños adicionales se emite nuevo presupuesto…". Limpieza: setHiddenGridlines(true) + recorte de filas/columnas vacías (deleteRows/deleteColumns) |
| RS_Cotizador.js | 1.2 | PDF: la cabecera muestra el logo "BidcomAgro" con "Bidcom" en negro (#111) y "Agro" en verde (#3a9e3a) sobre fondo blanco, igual que las etiquetas de WOS (.logo-b/.logo-a). Usa RichTextValue de Sheets (newTextStyle + setTextStyle 0-6 / 6-10) en vez del texto plano "BIDCOMAGRO" en banda violeta |
| RS_Cotizador.js | 1.1 | Mano de obra en el cotizador: obtenerManoObraCotizador (lee hoja Precios_mano_obra, la misma que HUB PRO; precio recomendado parseado con _cotNum), _cotNormalizarMO (precio editable + cantidad), RS_confirmarCotizacion suma manoObra al total y permite presupuesto solo-MO, PDF con sección MANO DE OBRA, email con tabla de mano de obra, historial guarda {repuestos, manoObra} |
| RS_Cotizador.js | 1.0 | NUEVO. Cotizador de presupuestos para cliente final: RS_confirmarCotizacion (enforcement de precio de lista vía _buildPriceMap/0.60, descuento por ítem clampeado 0-100, calcula precioFinal/subtotal/total), _generarPdfCotizacion (PDF violeta con emisor+cliente+tabla+total), _enviarEmailCotizacion (al reseller y cliente si cargó email), obtenerHistorialCotizaciones, hoja COTIZACIONES autocreada. Descuento inicial 25% |
| Index.html | 1.11 | Cotizador: la mano de obra es UNA SOLA por presupuesto — _cotAgregarMO/_cotAgregarMOManual reemplazan (_cotMO=[línea]) en vez de acumular; toast "actualizada/agregada"; texto de la card "(una sola)" |
| Index.html | 1.10 | Cotizador: agrega Mano de obra (card con select del catálogo recomendado + cantidad + "agregar manual"), render en el carrito con precio editable y cantidad por línea, total combinado repuestos+MO, permite generar presupuesto solo con mano de obra. _cotMO/_cotMOCatalogo/_cotCargarManoObra/_cotAgregarMO(+Manual)/_cotMO* handlers |
| Index.html | 1.9 | Nueva pestaña Cotizador (tb-cot/tab-cot): datos de cliente (nombre req + email opc), buscador reusando _pedIndice/_pedBuscarLocal, carrito con cantidad y % de descuento POR ÍTEM (25% inicial, editable), total en vivo, generar (PDF+email), historial. pedirPinCotizador gatea login; ruteo post-login goTab==='cot'. Precio base = PVP = precio40/0.60 |
| Env.js | 1.3 | SCHEMA.SHEETS: agrega COTIZACIONES |
| RS_Auth.js | 1.2 | Resellers desactivados (baja): helper _resellerInactivo(fila) lee col Q (SCHEMA.RESELLERS.ACTIVO); vacío=activo, NO/BAJA/INACTIVO/FALSE/0/false=baja. obtenerResellers oculta inactivos del selector; validarAccesoInicial (individual y grupo) y validarAccesoReseller rechazan con motivo:'inactivo'; login de grupo excluye sucursales de baja (motivo:'inactivo' si no queda ninguna); RS_recordarClave no manda clave a inactivos. Conserva email/PIN reales; reactivar = borrar la marca. Sin cambios OAuth |
| Env.js | 1.2 | SCHEMA.RESELLERS: agrega ACTIVO: 16 (col Q — estado de la cuenta) |
| Index.html | 1.8 | Fix guardar perfil: el withSuccessHandler de guardarPerfil tenía `var btn = ...btn-enviar-rep` que por hoisting tapaba al btn "Guardar cambios", haciendo que `btn.disabled=false` corriera sobre undefined → TypeError → el handler moría y el botón quedaba girando "Guardando…" para siempre (el guardado en servidor sí ocurría). Renombrada la variable interna a btnRep. Ahora confirma "Datos actualizados correctamente" |
| Index.html | 1.7 | Login OBLIGATORIO: se quita el botón "Continuar sin ingresar →" (bypass anónimo a Nueva Reparación) y la función cancelarPin; el modal de login es no-descartable (backdrop:'static', keyboard:false); la red de seguridad hidden.bs.modal solo quita el overlay con usuario válido (reseller o RTV) y si no hay sesión reabre el modal. "Solicitá tu acceso" sigue disponible para nuevos. Sin usuario válido no se accede al portal |
| Index.html | 1.6 | Login: el selector de empresa pasa de <select> a combobox buscable (filtra mientras se escribe). Input de texto #ov-reseller-search + input oculto #ov-reseller (login/recordarClave lo leen sin cambios); _ovLista/ovFiltrar/ovRender/ovSeleccionar/ovKey; navegación teclado ↑↓/Enter/Esc, match exacto fija el valor sin clickear, filtrado insensible a acentos (ovNorm), al elegir salta al PIN. Grupos ★ siguen apareciendo |
| Index.html | 1.5 | Login: mensaje específico "Esta cuenta está desactivada. Contactá a soporte." cuando el backend responde motivo:'inactivo' |
| Index.html | 1.1 | Fix enviarPedido() → generarPedidoRepuestosPortal |
| Index.html | 1.4 | Registro: 4 campos de dirección (Calle, CP, Localidad, Provincia) — se muestran solo si están vacíos en el sheet; Calle es requerida si visible; sección "Dirección fiscal" con separador; CP+Localidad en row flex |
| RS_Registro.js | 1.1 | REG_obtenerCamposVacios: expone campos direccion/cp/localidad/provincia; REG_solicitarAcceso: acepta y persiste los 4 campos en SOLICITUDES_RESELLER (cols J-M); _REG_procesarDecision: los escribe en hoja Resellers al aprobar; email admin muestra dirección consolidada |
| RS_Pedidos.js | 1.5 | Fórmula CANT_PEND col G incluye -Z (CANT_CANCEL) al crear filas | Fix PDF: total ocupa cols 6-7 merge (160px) en vez de col 7 sola (80px) |
| RS_OTs.js | 1.8 | enviarCasoAlHub precarga el "origen del repuesto" en el HUB: tras appendRow escribe la col AA (27) de la fila nueva con "Stock reseller" (aftEstado='repuesto' → el reseller reparó con su stock) o "Adelantado" (necesita que le adelantemos el repuesto), con guarda de columnas. Alimenta el badge que HUB PRO muestra; el operador lo puede editar allá. enviarLoteAlHub sin cambios (no captura aftEstado) |
| RS_OTs.js | 1.7 | Fix threading: replyHtml→replyAll en agregarComentario y aprobarPresupuestoPortal (replyHtml no existe en GmailThread, siempre fallback a sendEmail nuevo) |
| RS_OTs.js | 1.6 | Flujo batería unificado: detección esBateriaOT antes de ramas de flujo; 7 estados nuevos con paso/quePasa propios; esBateria en return usa variable en lugar de IIFE duplicado |
| Index.html | 1.3 | Stepper batería: 4 pasos (Registro→Caso DJI→Aprobado DJI→Finalizado); detección esBat aplica a cualquier circuito (no solo Reseller Propio); btn-repuestos oculto para baterías |
| RS_Auth.js | 1.1 | Perfil: agrega provincia (col 5) en obtener y actualizar |
| Env.js | 1.1 | SCHEMA.RESELLERS: agrega PROVINCIA: 5 |
| RS_Email.js | 1.0 | |
| RS_Main.js | 1.0 | |
| RS_OTs.js | 1.0 | |
| RS_Auth.js | 1.0 | |
| RS_Repuestos.js | 1.8 | FIX integración Portal→HUB PRO (mismo bug que RS_OTs 1.11, otro flujo): registrarPedidoRepuestos (reseller SUMA repuestos a una OT existente) escribía la col Q con join('\n'), que el HUB lee con split(' ; ') → tomaba solo el primero. Ahora escribe con ' ; '. Además el merge lee el valor actual con split tolerante (/ ; |\r?\n/) para soportar OTs viejas con '\n' y OTs del HUB con ' ; ' → auto-sana el formato al re-guardar. Sin re-auth |
| RS_Repuestos.js | 1.7 | ACCESORIOS: buscarRepuesto y RS_getListaPrecios incluyen hoja ACCESORIOS del mismo spreadsheet |
| RS_Código.js | 1.0 | |
| RS_CuentaCorriente.js | 1.0 | |
| RS_Registro.js | 1.0 | |
| RS_RTV.js | 1.0 | |
| RS_Campana.js | 1.0 | |
| RS_Avisos.js | 1.0 | |
| Env.js | 1.0 | |

## WOS
| Archivo | Versión | Notas |
|---------|---------|-------|
| WOS_GmailFlow.js | 2.28 | 3 mejoras al flujo de avisos de ingreso: (1) Alertar sin contacto — cuando el pedido es una OT o un reseller sin email/hilo cargado, ya no queda "resuelto en silencio": _wosAlertarSinContacto manda un mail interno a soporte con lo ingresado y lo pendiente para que lo contacten a mano (completo→resultado auto_completo_sin_contacto, parcial→sin_contacto_alertado). (2) Recordatorio/escalado — WOS_recordarIngresosPendientes (piggyback en el detector, sin trigger nuevo) reenvía el aviso A/B a los 3 días sin respuesta (_wosRecordarIngresoPedido→'recordado') y escala a soporte a los 6 días (_wosEscalarIngresoPedido→'escalado'); idempotente vía el RESULTADO de NOTIF_INGRESOS_WOS. (3) Base para el panel: WOS_procesarRespuestaIngreso ahora deja rastro de la respuesta. Sin re-auth |
| Despacho_Código.js | 3.23 | Panel "Avisos de ingreso" (auditoría de NOTIF_INGRESOS_WOS): WOS_cargarColaIngresos() devuelve las últimas 300 filas + resumen por resultado. _wosMarcarRespondidoIngreso(numero, op) marca 'respondido_A'/'respondido_B' en las filas del pedido tras el clic A/B (WOS_procesarRespuestaIngreso la llama) para que el recordatorio/escalado de WOS_GmailFlow deje de perseguir un pedido que ya contestó. Sin re-auth | Aviso al reseller cuando ingresa mercadería bloqueada (procesa la cola NOTIF_INGRESOS_WOS que llena SM al recibir un CAS; corre en cada pasada del detector). Por pedido: si el stock de HOY (Carmen fresco, sin cache) cubre TODO su backorder → reactiva solo (WOS_reactivarBackorder 'auto_ingreso', reservas Cumplida) y mail "llegó todo, lo preparamos y despachamos — no tenés que hacer nada" (sin pregunta: default, nadie paga un envío extra); si sigue faltando algo → mail con tabla de lo ingresado + lo pendiente con su ETA y botones "Despachar AHORA lo disponible" / "Esperar y recibir todo junto (un solo envío)" (links ?page=resp_ingreso). Fallback texto: "respondé este correo" (sin Opción A/B para no chocar con el parser del faltante). Idempotente por fila (Pendiente→Notificado; error reintenta). Sin re-auth |
| Despacho_Código.js | 3.22 | Ruta ?page=resp_ingreso + _doGetRespIngreso + WOS_procesarRespuestaIngreso: A = reactiva (Backorder→Preparado) solo las líneas cuyo SKU tiene stock suficiente HOY para su pendiente (lee Carmen fresco) y cierra sus reservas (Cumplida); las de cobertura parcial siguen en Backorder con reserva (las maneja el operador). B = no cambia nada (sigue esperando con reservas activas; al llegar el resto la cola lo re-evalúa y si queda completo se prepara solo). Log en WOS_LOG. Sin re-auth |
| Despacho_Index.html | 3.42 | Grilla de Stock: cuando hay unidades en camino pero SIN ETA cargada, el chip "✈ N" ahora agrega "sin ETA" en naranja (con tooltip: correr el cruce en SM) en vez de no mostrar nada — antes "✈ 25" pelado parecía OK y hacía creer que Despacho parcial estaba mal cuando en realidad ninguna vista tenía la fecha. Sin re-auth |
| Despacho_Código.js | 3.21 | WOS_despachoParcialData incluye cas y air en cada reserva activa (para el tooltip de auditoría del overlay Despacho parcial). Sin re-auth |
| Despacho_Index.html | 3.41 | Claridad en el overlay Despacho parcial ("sin fecha" parecía "no hay compras"): distingue "N u en camino · sin ETA cargada" (la compra existe pero COMPRAS_DETALLE no tiene FECHA_ETA → correr el cruce en SM) de "N u sin compra · a confirmar" (ninguna compra la cubre); chip del pedido con 3 estados (⏳ sin compra que las cubra / 🚚 en camino · falta cargar ETA / 📦 entrega completa ~fecha); banner naranja arriba con el total de u. sin ETA y la acción correctiva (SM → Compras → cruce con sheet externo); tooltip por lote con CAS · AIR y si está bloqueado o es propuesta (audita el reparto FIFO, ej. por qué una línea cae al lote del 03/09 si el del 16/08 se lo llevó un pedido más viejo). "Copiar aviso": "en camino, fecha a confirmar" para lotes sin ETA. Sin re-auth |
| Despacho_Código.js | 3.20 | Despacho parcial: cumplimiento + bloqueo global. Nueva WOS_despachoParcialData() (lotes DJI disponibles por SKU netos de reservas + reservas activas con ETA, para que el overlay proyecte con qué lote y qué fecha se cumple cada falta) y WOS_bloquearEnCaminoParciales(): bloquea (reserva en RESERVAS_EN_CAMINO) las unidades en camino para TODOS los pedidos con deuda, FIFO global por fecha de pedido —la misma asignación que muestra el overlay—, reservando solo la falta no cubierta por stock; idempotente (descuenta lo ya reservado por pedido+SKU) y con LockService. WOS_reconciliarReservas ahora considera deuda viva cualquier línea con pend>0 en estado no cerrado (_WOS_DP_CERRADOS: Entregado_Cerrado/Confirmado, Listo_Retiro, Cancelado), no solo Backorder → los bloqueos del overlay no se auto-cancelan a los 10 min. Sin re-auth |
| Despacho_Index.html | 3.40 | Overlay "Despacho parcial" enriquecido: al abrir trae WOS_despachoParcialData y muestra por línea CÓMO se cumple la falta ("🚚 🔒 4 u ~04/08/2026 · 4 u ~15/08/2026 · 2 u a confirmar"; 🔒 = ya bloqueado, sin candado = propuesta), chip por pedido con la entrega completa (✅ completo con stock / 📦 entrega completa ~fecha máx / ⏳ N u. sin fecha), totales en el subtítulo (bloqueadas / llegan en compras DJI / a confirmar) y botón "🔒 Bloquear compras DJI" (WOS_bloquearEnCaminoParciales, idempotente, re-renderiza con lo bloqueado). "Copiar aviso" ahora incluye cuántas unidades llegan con cada ETA por ítem. Sin re-auth |
| WOS_GmailFlow.js | 2.26 | FIX formato ETA + desglose por lote en el mail de faltantes. La columna "Reposición estim." ya NO muestra una sola fecha (y encima con hora/timezone): ahora lista, por SKU, **cuántas unidades llegan con cada ETA** (ej. "60 u · llega ~04/08/2026" / "100 u · llega ~15/08/2026" / "26 u · a confirmar"), repartiendo el faltante (cantSol−cantDisp) entre los lotes DJI disponibles (batchesDisp, que descuenta lo reservado a otros). Reemplaza _faltEta por _faltReposicion + _repMap; el bloque OPCIÓN A usa la ETA más próxima ya limpia. Sin re-auth |
| Despacho_Código.js | 3.19 | FIX "la ETA sale como Tue Aug 04 2026 00:00:00 GMT-0300 (…)": Sheets convierte el texto de FECHA_ETA en una fecha real, y WOS le hacía String() crudo. Nuevo _wosEtaFmt(v) formatea Date-o-texto a dd/MM/yyyy limpio; _wosEtaToDate acepta Date. WOS_getEnCaminoMap normaliza dEta con _wosEtaFmt → arregla el formato en TODOS lados (mail, grilla de stock, lista de compras, reporte de backorder, reservas). Sin re-auth |
| Despacho_Código.js | 3.18 | Mejoras de la reserva en camino. ROBUSTEZ: WOS_reconciliarReservas() libera/cierra las reservas activas cuyo pedido ya no tiene backorder pendiente de ese SKU (huérfanas: se despachó por otro camino, se canceló, o el reseller no volvió) → Cumplida si hubo despacho, si no Cancelada; la llama el detector cada 10 min (sin trigger nuevo) y se puede correr a mano. OPTIMIZACIÓN: _wosReservasActivas se cachea 60s (CacheService) y la invalidan reservar/cerrar/reconciliar; _wosReservarEnCamino toma LockService.getScriptLock (evita doble reserva del mismo lote en accesos concurrentes) y lee fresco; el sort de lotes en WOS_getEnCaminoMap precalcula la fecha (no re-parsea por comparación). UX: _wosReservarEnCamino devuelve {cantidad, etaProx} y WOS_cargarStock expone enCaminoReservado. Sin re-auth |
| WOS_GmailFlow.js | 2.25 | Mejoras de la reserva: al aceptar "Esperar", el mail de confirmación al reseller ahora dice "Reservamos X u. del lote que llega ~fecha" (ambos paths: detector por mail y WOS_procesarRespuestaManual link/teléfono, usando el resumen de _wosReservarEnCamino). El detector llama WOS_reconciliarReservas() al final de cada corrida (cada 10 min) para liberar reservas huérfanas. Sin re-auth |
| Despacho_Index.html | 3.39 | Reserva en camino visible para el operador: la grilla de Stock muestra junto al ✈ y la ETA un chip "(N reserv.)" con las unidades en camino ya reservadas a un reseller (it.enCaminoReservado). Sin re-auth |
| Despacho_Código.js | 3.17 | Reserva de unidades en camino (Fase 4a+4b). Nueva hoja RESERVAS_EN_CAMINO (MASTER: Fecha·Pedido·Reseller·SKU·CAS·N°AIR·ETA·Cantidad·Estado) + módulo de helpers (_wosResSheet, _wosReservasActivas, _wosReservarEnCamino, _wosCerrarReservas). WOS_getEnCaminoMap ahora resta las reservas activas y agrega por SKU: disponible, reservado, batchesDisp y etaMinDisp (ETA del lote libre más próximo) — así no se re-promete un lote ya comprometido. 4b: WOS_buscarBackorderPorSKU ordena los pedidos con reserva primero (y expone .reservado); al llegar el stock, WOS_recibirMercaderia y WOS_reactivarBackorder cierran las reservas del pedido (Estado→Cumplida). La reserva se crea al aceptar "Esperar" y se libera (Cumplida/Cancelada) según corresponda. Sin scopes nuevos → sin re-auth |
| WOS_GmailFlow.js | 2.24 | Reserva de unidades en camino (Fase 4a). Al aceptar "Esperar" (Opción A) —por el detector de respuestas por mail y por WOS_procesarRespuestaManual (link/teléfono)— se reservan las unidades pendientes contra los lotes DJI disponibles (_wosReservarEnCamino); al elegir "Cancelar" (Opción B) se liberan (_wosCerrarReservas →Cancelada). En el mail "esperar/cancelar", _faltEta pasa a usar la ETA DISPONIBLE (etaMinDisp, que descuenta lo reservado a otros) → dos resellers no reciben la misma fecha por las mismas unidades. Sin re-auth |
| WOS_GmailFlow.js | 2.23 | ETA en el mail "esperar/cancelar" al reseller (Fase 3). WOS_notificarFaltante ahora consulta WOS_getEnCaminoMap y agrega a la tabla de faltantes una columna "Reposición estim." con la ETA por SKU ("llega ~fecha" o "a confirmar"); el bloque OPCIÓN A (Esperar) suma "la reposición llegaría aprox. el ~fecha" (la más próxima entre los faltantes), en HTML y en el texto plano. Así, al pedirle al reseller que espere o cancele, ya ve cuándo llegaría. Sin cambio de estados/flujo ni scopes → sin re-auth |
| Despacho_Código.js | 3.16 | ETA de compras DJI en el "en camino" (Fase 2). WOS_getEnCaminoMap ahora lee FECHA_ETA(6) y N_AIR(7) de COMPRAS_DETALLE y agrega, por SKU, batches:[{cas,air,eta,qty}] (ordenados por fecha, sin fecha al final) + etaMin (fecha del lote más próximo); mantiene ocs:{CAS:qty} para compat con los consumidores previos. Nuevo helper _wosEtaToDate (parsea dd/MM/yyyy o dd/MM). WOS_cargarStock: enCaminoOcs pasa a "CAS (Nu.) · llega ~fecha" y agrega enCaminoETA (la más próxima). WOS_reporteBackorder: calcula enCaminoEta por SKU (más cercana) y el mail muestra "llega ~fecha" bajo "En camino" (filas rojas y verdes). Cubre el caso de un mismo CAS con 2 fechas (batches distintos). Sin re-auth |
| Despacho_Index.html | 3.38 | ETA de compras DJI (Fase 2): la grilla de Stock muestra "llega ~fecha" junto al badge ✈ de en-camino (it.enCaminoETA) y el tooltip por lote incluye la fecha; la Lista de Compras arma ecOcs desde batches (con fecha) y muestra la ETA más próxima (row.ecEta) junto al ✈. Sin re-auth |
| WOS_GmailFlow.js | 2.22 | Confirmación de recepción del reseller para medir "precisión de resultado". En el mail de despacho de WOS_despacharCompleto se reemplaza el texto "respondé Recibido" por dos botones: "✓ Recibí todo bien" y "⚠ Reportar un problema" que apuntan al webapp ?page=confirma_entrega&num=<pedido>&r=ok|prob (URL vía ScriptApp.getService().getUrl()). El clic lo procesa _doGetConfirmaEntrega (Despacho_Código.js) → registra ok/problema en WOS_QA y, si ok, pasa a Entregado_Confirmado. Si no hay URL disponible, cae al texto "Recibido" anterior (fallback). Sin scopes nuevos → sin re-auth |
| WOS_GmailFlow.js | 2.21 | Completa el ciclo de "cancelar el faltante" (sobre 2.20). Caso: pidió 10, hay 9, cancela el faltante → tras Opción B la línea queda "Preparado Parcial" con Z(CANT_CANCEL)=1 y G=9 pendientes; al despachar los 9 DEBE quedar Entregado_Cerrado. Pero el despacho comparaba cantFinal(9) >= cantSol(10) SIN restar lo cancelado → la devolvía a Backorder y disparaba aviso/2º envío falso. Fix: (a) estado final por ítem en WOS_despacharCompleto ahora cierra cuando cantFinal >= (cantSol − Z) → Entregado; (b) detección hayBackorderPostDesp usa (cantSol − Z) → no marca backorder por unidades canceladas; (c) el 3er camino de Opción B —respuesta del reseller por email en WOS_detectarRespuestasResellers— se alineó igual que WOS_procesarRespuestaManual (En_Espera O Backorder + math sobre E−F−Z acumulando Z). Los 3 caminos de A/B (modal operario, link del mail, texto del mail) ahora deciden idéntico. Caso común (Z=0) sin cambios |
| WOS_GmailFlow.js | 2.20 | FIX "Respuesta Telefónica → Opción B (Cancelar el faltante)" dejaba líneas en Backorder en vez de cancelarlas. El botón "📞 El reseller ya respondió" aparece cuando el estado CONSOLIDADO del pedido es En_Espera_Reseller (prioridad En_Espera > Backorder), así que en un pedido MIXTO (una línea faltante En_Espera + otra ya aparcada en Backorder) el botón se ve igual. Pero WOS_procesarRespuestaManual Opción B sólo tocaba líneas con estado === En_Espera_Reseller → la línea en Backorder se salteaba y "quedaba en backorder" aunque el operario eligió cancelar (bug reportado). Fix: la Opción B ahora procesa las líneas del faltante pendiente en En_Espera_Reseller O Backorder. Además pasa a calcular sobre lo REALMENTE pendiente (E−F−Z) en vez de sobre CANT_SOL y ACUMULA sobre CANT_CANCEL previo → una línea con despacho/cancelación parcial (típico en Backorder) ya no se cancela de más. El caso común (línea nunca despachada, F=0/Z=0) da idéntico resultado que antes (sin regresión). La Opción A no cambia (En_Espera→Backorder; las ya-Backorder quedan bien). Nota: un pedido 100% Backorder no muestra el botón telefónico (sólo consolidado En_Espera) — fuera de alcance |
| WOS_GmailFlow.js | 2.19 | FIX destinatarios de los mails asincrónicos de WOS: thread.replyAll() responde a los participantes del ÚLTIMO mensaje del hilo → si alguien tuvo una conversación aparte con el reseller, el 2° envío / mail de administración le llegaba a ESA gente y NO al reseller/facturación originales. Nuevo helper _wosReplyHiloOriginal(threadId, plain, opts, mustCc): responde al PRIMER mensaje (getMessages()[0] = destinatarios originales, ignora conversaciones aparte) y fuerza en CC a los obligatorios (reseller + facturación) sin duplicar; devuelve false si no hay hilo (el caller hace fallback sendEmail). Aplicado en WOS_despacharCompleto (despacho/2° envío + sección administración, mustCc=[reseller, facturación]) y WOS_notificarFaltante (pregunta A/B, mustCc=[reseller]). Las CONFIRMACIONES (A/B, Recibido) NO se tocan: responden justo después del mensaje del reseller, ahí replyAll al último es correcto |
| Despacho_Código.js | 3.15 | Backend del armado guiado + precisión medible. Nuevos: WOS_verificarSnDuplicados(numero,seriales) (dups internos + contra SERIALES ya despachados en OTROS pedidos, ignora sufijo "xN" de bolsas); hoja WOS_QA + WOS_registrarQA(numero,qa) (1 fila por cierre de caja: items/unidades/cantOk/snDupOk/envioOk/overrides/resultado/tiempo); _doGetConfirmaEntrega + ruta doGet ?page=confirma_entrega (r=ok→Entregado_Confirmado, r=prob→problema) que actualiza WOS_QA vía _wosRegistrarConfirmacion; WOS_getPrecisionMetrics(desde,hasta) = precisión combinada proceso (limpio/total) + resultado (ok/(ok+problema)) global y por operador + flags recientes; WOS_previewPrecision para probar desde el editor. Reusa _rfHtml/_wosLogAccion/_getHojaPorNumero. Sin cambios de appsscript.json (doGet ya existía) → sin re-auth |
| Despacho_Código.js | 3.14 | Auditoría de contabilidad de cantidades/estados (a pedido: "que no puedan pasar"). 5 fixes de la misma clase que 2.20/2.21 (ignorar CANT_CANCEL=Z o pisar estados): (1) WOS_buscarBackorderPorSKU calculaba pendiente = E−F sin restar Z → inflaba la Lista de Compras; ahora E−F−Z. (2) WOS_reporteBackorder calculaba necesidad = E−F → reportaba de más a logística internacional; ahora E−F−Z. (3) WOS_recibirMercaderia usaba _wosSetEstado (SIN filtro) → pisaba TODAS las filas del pedido a Preparado, reviviendo líneas Entregado_Cerrado/Cancelado (riesgo de doble despacho / descancelar); ahora reactiva SOLO las filas en Backorder DE ESE SKU (y no notifica si no reactivó nada). (4) WOS_cambiarEstado al CANCELAR no seteaba Z → la línea Cancelada quedaba con pendiente fantasma (G=E−F>0) y había que correr WOS_aplicarCanceladosSinCancel a mano; ahora setea Z=E−F (G→0) en el momento. (5) _procesarFilasPedidos: el fallback de cantPend cuando la col G está vacía usaba E−F; ahora E−F−Z (consistencia). El frontend ya usa it.cantPend (col G) y salta líneas Cancelado, así que hereda bien. Sin cambios de appsscript.json → sin re-auth |
| Despacho_Código.js | 3.13 | FIX mismo problema de replyAll en los mails asincrónicos de Código: WOS_cambiarEstado (cancelación de pedido) y WOS_recibirMercaderia (aviso "llegó el stock de tu backorder") usaban getThreadById(id).replyAll() → iban a quien quedó en el último mensaje. Ahora usan _wosReplyHiloOriginal (responde a los destinatarios originales + reseller garantizado en CC), con fallback a sendEmail directo al reseller si no hay hilo (antes el aviso de stock se perdía si el reply fallaba) |
| Despacho_Index.html | 3.37 | Armado guiado + cierre con checklist (0 errores) + panel de Precisión. (1) Botón "▶ Armar pedido guiado" (_wzGuia) en renderAcciones para estados armables: overlay con los pasos en orden (Revisar → Preparar → Cerrar la caja) que abre cada modal existente; marca _wzStartMs para medir tiempo. (2) El botón del modal de despacho pasa de confirmarDespacho() a _wzRevisarCerrar(): abre el checklist "Revisá antes de cerrar la caja" con gates — SN duplicados (backend WOS_verificarSnDuplicados, BLOQUEA), datos de envío completos (peso/transportista/tracking/destinatario; BLOQUEA, con override justificado que baja la precisión), y despacho parcial (pide reconocer que el resto va a backorder). SN faltante sigue permitido (opcional). Al pasar, _wzEjecutarCierre arma el resumen QA y llama al confirmarDespacho() de siempre; en el success se registra WOS_registrarQA. (3) Panel "🎯 Precisión de armado" bajo los KPIs (colapsable, selector 30/90/todo) que consume WOS_getPrecisionMetrics: % total/proceso/resultado, ranking por operador y flags para revisar; el headline se refresca en cada cargar(). Reusa el modal de preparar/despachar, escáner, bins y _buscarPedido/escH/toast. Solo front |
| Despacho_Index.html | 3.36 | FIX (reporte de operador): en un ítem "En espera del reseller" (o Backorder) no se podían cargar los N° de serie en el modal de DESPACHO. Esos ítems arrancan con cantidad 0 (a propósito: no se despachan por defecto, esperan la decisión A/B). El campo de SN se dibuja según la cantidad, así que al tildar "Requiere N° de serie" con la cantidad en 0, _syncSeriales(row,0) no dibujaba ningún input → "aparece pero no deja cargar SN". Los ítems normales no sufrían esto porque arrancan con cantidad > 0. Fix en _toggleSeriales: al activar SN con la cantidad en 0 se sube al pendiente (máx del input) para que aparezcan los campos; el operador puede ajustarla. El modal de PREPARAR no tenía el problema (dibuja SN directo sin checkbox, incluye En_Espera con qtyPrep>0 si hay stock). Solo front |
| Despacho_Index.html | 3.35 | "Preparar lo disponible" ahora incluye las líneas En_Espera_Reseller con faltante PARCIAL (radar #2). Antes abrirModalPreparar salteaba toda línea En_Espera → para un ítem parcial (pide 10, hay 5) no se podía cargar SN+peso en Preparar (había que hacerlo en Despachar); si el pedido era esa sola línea el modal salía vacío. Ahora la línea En_Espera se trata como parcial (itParcial = parcial || esEnEspera): se incluye con cantidad disponible editable (min stock, cantPend), se saltea solo si stock=0. Junto con Despacho_Código.js 3.12, preparar NO la saca de En_Espera → el reseller sigue pudiendo decidir A/B |
| Despacho_Código.js | 3.12 | Radar #2 (backend): WOS_prepararConSeriales mantiene el estado En_Espera_Reseller al preparar lo disponible de una línea con faltante parcial (registra SN + peso pero NO pasa a Preparado/Parcial). Así la línea sigue "en espera" y la respuesta A/B del reseller la resuelve. _estNuevo = (estActual===EN_ESPERA) ? EN_ESPERA : (qty>=pend ? Preparado : Parcial) |
| WOS_GmailFlow.js | 2.18 | FIX radar #1: al DESPACHAR lo disponible de una línea En_Espera_Reseller que queda con pendiente, ahora se MANTIENE En_Espera (antes pasaba a Backorder). El problema: si se despachaba lo disponible antes de que el reseller contestara, la línea salía de En_Espera → la Opción B (cancelar) del reseller la salteaba (el detector/manual filtran por En_Espera) → el faltante quedaba en Backorder (se enviaría en 2° envío) en vez de cancelarse. Ahora en WOS_despacharCompleto: cantFinal>=cantSol → Entregado; si queda pendiente y estaba En_Espera → sigue En_Espera; resto → Backorder. La respuesta A/B la resuelve después (A→Backorder, B→cancela con CANT_CANCEL) |
| Despacho_Index.html | 3.34 | FIX peso/seriales que se escapaban al DESPACHAR sin haber pasado por "Preparar" (backorder que se despacha directo, "en espera" que se manda igual): el peso y los seriales solo estaban garantizados en Preparar (obligatorios con validación), pero Despachar no los re-exigía (_gateDespachoBtn solo bloqueaba por backorder-con-stock) → PESO_ENVIO vacío + mail sin peso ni seriales. Ahora: (1) PESO OBLIGATORIO en despacho (salvo retiro): _gateDespachoBtn deshabilita el botón si la suma de pesos de los bultos no es > 0; se reevalúa con oninput en cada .bulto-peso y al agregar/quitar bulto y tras el pre-llenado del peso preparado; red de seguridad extra en confirmarDespacho. Se guarda _modalDesp.esRetiro (individual y batch) para eximir al retiro. (2) AVISO no-bloqueante de seriales: nuevo _confirmarSinSerie(list) — al despachar unidades cuyo ítem no tiene ni serial reingresado ni serial registrado (col Y), pide confirmación (lista SKU x cant) antes de mandar; aplica en modo individual y batch. _modalDesp.items ahora lleva seriales+desc. Frontend puro, sin backend, sin re-auth |
| Despacho_Index.html | 3.33 | Red de seguridad de escaneo (pistola Zebra DS2278): con layout de teclado no coincidente la pistola mapea el '-' de los códigos SO a "'" (escanea "SO'YC.ST.LL000750'000003" en vez de "SO-YC.ST.LL000750-000003"). Nuevo helper _normSO(code) — aplicado en el chokepoint _prepFeed (pistola + cámara) — que SOLO en códigos SO (empiezan con SO seguido de guion/apóstrofo) revierte apóstrofo→guion y pasa a mayúsculas; los N° de serie reales (DJI: alfanuméricos sin guiones, ej. 93PCN63001825K) NO se tocan. El fix de fondo es la config del lector (Keystroke Delay=Medium ya arregló caracteres perdidos/minúsculas/pegados; falta destildar Quick Keypad Emulation o setear Keyboard Country=Spanish LatAm para el '-'); esto blinda WOS aunque un '-' se escape. Probado con node (6/6 casos OK) |
| Despacho_Index.html | 3.32 | FIX real del reclamo del operador: en un pedido En_Espera_Reseller NO podía cargar los N° de serie ni el peso de lo disponible mientras el reseller no decidía. Causa: (1) el estado En_Espera solo ofrecía "Despachar lo disponible", sin botón de preparar; (2) abrirModalPreparar bloqueaba con "sin stock suficiente" porque los ítems faltantes (En_Espera) no tienen stock. Ahora: se agrega botón "📦 Preparar lo disponible" en el estado En_Espera, y abrirModalPreparar SALTEA los ítems En_Espera_Reseller tanto en el pre-chequeo de stock como en el listado de seriales (igual que ya hacía con Cancelado). Así el operador prepara (SN + peso) solo lo disponible; el faltante queda esperando la decisión A/B. confirmarPreparar/WOS_prepararConSeriales solo tocan las filas del payload, así que los En_Espera quedan intactos |
| Despacho_Index.html | 3.31 | FIX flujo "espera o cancela": al despachar un pedido En_Espera_Reseller, el modal de despacho (abrirModalDespacho) pre-cargaba los ítems faltantes (estado En_Espera) con la cantidad COMPLETA igual que los disponibles → el operador veía el faltante con unidades que no tenía y creía que no podía continuar hasta la respuesta del reseller. Ahora hay una rama esEnEspera que los pre-carga en 0 y los marca "⏳ EN ESPERA DEL RESELLER — faltante, no se despacha ahora" (borde violeta, input editable por si consiguió stock). Así "Despachar lo disponible" manda solo lo que hay y el faltante queda esperando la decisión A/B. Sin cambios de backend (WOS_despacharCompleto ya deja intactos los ítems no despachados que no son Preparado/Parcial) |
| Despacho_Index.html | 3.30 | FIX "no me deja cargar los seriales" tras cancelar faltantes (Opción B): abrirModalPreparar ahora saltea las líneas en estado 'Cancelado' tanto en el pre-chequeo de stock (que abortaba con "No se puede preparar — sin stock suficiente") como en el listado de seriales. Antes, una línea cancelada cuya col G (CANT_PEND) había quedado sin actualizar figuraba como pendiente sin stock y bloqueaba toda la preparación del pedido. Robustece contra datos viejos aunque no se haya seteado CANT_CANCEL |
| WOS_GmailFlow.js | 2.17 | FIX inconsistencia de datos en Opción B (cancelar faltante) PARCIAL vía RESPUESTA POR MAIL: WOS_detectarRespuestasResellers pisaba CANT_SOL con el disponible (ej. pide 10, hay 5 → CANT_SOL=5) → se PERDÍA la cantidad original del pedido y NO se registraba la cancelación en CANT_CANCEL. Los otros 2 caminos (link A/B del mail y respuesta manual, ambos vía WOS_procesarRespuestaManual) hacían lo correcto: CANT_CANCEL = solicitado − disponible, preservando CANT_SOL. Ahora el detector automático es IDÉNTICO al manual: parcial → CANT_CANCEL=cantSol−cantDisp + PREP_PARCIAL (CANT_SOL intacto); total → CANT_CANCEL=cantSol + Cancelado. CANT_PEND operativo no cambia (seguía en lo disponible), pero ahora la auditoría (solicitado original + cancelado) queda bien y los 3 caminos coinciden. Se quitó cantDespB (sin uso) |
| WOS_GmailFlow.js | 2.16 | Diagnóstico/arreglo en bloque de líneas 'Cancelado' sin CANT_CANCEL (datos viejos previos al fix 2.15): WOS_previewCanceladosSinCancel() lista (log + return) las filas afectadas en Pedidos_resellers y Pedidos_OTs (estado Cancelado con CANT_PEND=E−F−Z aún > 0 porque Z quedó vacío); WOS_aplicarCanceladosSinCancel() las corrige seteando Z = CANT_SOL − CANT_DESP → CANT_PEND = 0. Read-only el preview; el apply escribe col Z (se corre a mano desde el editor, sin deploy). Desbloquea de una todos los pedidos tipo Leber antes del deploy |
| WOS_GmailFlow.js | 2.15 | FIX raíz del bloqueo de preparación al cancelar faltantes (Opción B en WOS_detectarRespuestasResellers): al pasar una línea a 'Cancelado' (cantDisp=0) ahora también setea CANT_CANCEL (col Z) = pendiente (CANT_SOL − CANT_DESP), para que CANT_PEND (=E−F−Z) quede en 0. Antes solo cambiaba el estado y dejaba col G con la cantidad completa → la línea cancelada seguía contando como pendiente y rompía la preparación/otros lectores. Complementa el guard de Despacho_Index (que tolera el dato viejo de pedidos ya cancelados) |
| WOS_GmailFlow.js | 2.14 | Soporte a pedidos de cliente EXTERNO (carga super-RTV del Portal): el nombre del pedido (col B) puede no existir en la hoja Resellers → antes _wosGetEmailReseller devolvía '' y WOS_despacharCompleto ABORTABA ("Email no encontrado") → imposible despachar. Ahora NO se aborta por falta de email: el aviso de despacho sale por replyAll al hilo ancla (col R, que incluye al cliente si dejó su mail en la carga + facturación); el email directo queda solo como fallback (si el hilo se cae → se manda a facturación en vez de fallar). Mismo cambio en WOS_notificarFaltante (si no hay hilo ni email de cliente externo se omite el aviso Opción A/B y solo cambian los estados). Retiro y cancelación ya toleraban la falta de email. PENDIENTE (cosmético): el remito PDF (_wosGetResellerMeta) y la etiqueta (WOS_getEtiquetaData) no muestran la dirección estructurada del cliente externo — la dirección va en Observaciones (col N), que el Portal completa en modo super |
| Despacho_Index.html | 3.29 | FIX "Despacho parcial": faltaban códigos que se deben. El filtro de _computeDespachable era un whitelist de estados (Confirmado/Preparado/Preparado Parcial/Backorder) que dejaba afuera las líneas En_Espera_Reseller (SKU faltantes ya notificados al reseller vía WOS_notificarFaltante) y Pendiente_Revision, aunque tuvieran cantPend>0. Ahora es un blacklist de estados TERMINALES (DP_ESTADOS_CERRADOS: Entregado_Cerrado/Entregado_Confirmado/Listo_Retiro/Cancelado): muestra TODA línea con cantPend>0 que no esté cerrada = todo lo que le debemos al reseller. Los estados terminales tienen cantPend≤0, así que el cambio no agrega ruido de líneas ya entregadas |
| WOS_GmailFlow.js | 2.13 | Recuperación acotada al incidente real (usuario: "pasó ayer 15/07 a las 12pm; todo lo despachado después no está en Entregados"). _wosCalcularEntregadosFaltantes ahora acepta corte con HORA exacta ('YYYY-MM-DDTHH:mm:ss'), no solo fecha (FECHA_DESPACHO se escribe con new Date() completo, incluye hora). Wrappers sin argumentos para el botón "Ejecutar": WOS_previewFaltantes_Desde15Jul12h() y WOS_aplicarFaltantes_Desde15Jul12h() con corte fijo 2026-07-15T12:00:00. Como después del corte Entregados está vacío, actual=0 y faltante = cantidad DESPACHADA completa: NO resta nada, escribe tal cual lo entregado y no registrado. Deja afuera junio y el 06/07 (otro tema). Sigue idempotente (re-correr no duplica) |
| WOS_GmailFlow.js | 2.11 | FIX GRAVE de despacho: el descuento de stock (línea en Carmen "Entregados") fallaba EN SILENCIO — un catch vacío al abrir Carmen dejaba carmenHoja=null, se salteaba el appendRow a Entregados pero el mail salía igual → despacho sin descontar (causa real vista en prod: el sheet Entregados se quedó sin filas y el appendRow falló). Ahora: reintento (3× con 600ms) por blip transitorio de openById, y si aun así no puede abrir Carmen o no encuentra la pestaña "Entregados", ABORTA el despacho ANTES de tocar el pedido y de mandar el mail, con error claro al operario. NUEVA WOS_diagnosticoCarmen(): corré desde el editor → dice si abre Carmen, lista pestañas y prueba escribir/borrar. RECUPERACIÓN de descuentos perdidos: _wosCalcularEntregadosFaltantes reconcilia por (pedido,SKU) esperado=CANT_DESP vs actual=suma en Entregados (Origen=pedido); WOS_previewEntregadosFaltantes(desdeISO?) muestra los gaps (solo lectura) y WOS_aplicarEntregadosFaltantes(desdeISO?) escribe solo la diferencia faltante (línea "<pedido> (recupero)"), idempotente (no duplica el descuento) |
| Despacho_Index.html | 3.28 | Cámara de escaneo en preparación más robusta y diagnóstica: _prepLoadQrLib prueba 3 CDNs (cloudflare→jsdelivr→unpkg) por si una está bloqueada. _prepCamToggle ahora pre-chequea navigator.mediaDevices y pide getUserMedia explícito para disparar el prompt y capturar el error REAL (NotAllowedError/NotFoundError/NotReadableError…) con mensaje claro. Nuevo _prepCamStart usa Html5Qrcode.getCameras() para elegir la trasera por deviceId (más confiable que facingMode), con fallback a facingMode:environment. El panel queda abierto con el mensaje de error. NOTA: si Apps Script bloquea la cámara por Permissions-Policy del iframe, ningún cambio de código la habilita → el lector físico (pistola) sigue siendo el flujo principal |
| Despacho_Index.html | 3.27 | Círculo cerrado con Stock Manager: al escanear/tipear el código de una bolsa creada en SM (SO con cantidad), WOS autocompleta la cantidad de unidades de esa bolsa. Nuevo _prepBolsaResolver(inp) llama a WOS_resolverBolsa(codigo) y setea la .prep-bag-qty + _prepBolsaChange; se dispara en onchange del input de código de bolsa y desde _prepFeed (escaneo) cuando el input es .prep-bag-code. Mensaje "📦 Bolsa de N u." en la barra de escaneo |
| Despacho_Código.js | 3.11 | Círculo cerrado: WOS_resolverBolsa(codigo) devuelve la cantidad de unidades de una bolsa creada en Stock Manager. Lee SO_ETIQUETAS de la planilla MASTER (que WOS ya referencia) vía _wosMapaBolsas() — mapa {SO→{cantidad,sku}} solo de bolsas (CANTIDAD>1), cacheado 2 min (<90KB). Sin cambios de scope → sin re-auth |
| Despacho_Index.html | 3.26 | Preparación POR BOLSA para consumibles (tornillos/gaskets): cada ítem tiene toggle 🔢 Por unidad / 🔩 Por bolsa. En "por bolsa" un mismo código SO representa N unidades — el operario agrega bolsas (chips + Bolsa 10/20/50/Otra), pone la cantidad y escanea 1 SO por bolsa (obligatorio). El manifiesto queda "SO-123 x50, SO-124 x20" (qtyPrep = suma de bolsas). El modo viene pre-marcado desde el maestro (WOS_maestroArticulos) o si el manifiesto previo ya trae bolsas. Nuevos helpers: _parseManifest/_serialUnits/_bagsToManifest/_autoSeedBags/_prepBultoRepresentativo, y funciones de modo/bolsa (_prepSetMode/_prepRenderMode/_prepRenderBolsas/_prepBolsaChange/_prepAddBolsa/_prepDelBolsa/_prepItemState/_prepAutoAllocBins). _prepCount reescrito (mode-aware: valida SN por unidad o bolsas). confirmarPreparar arma payload por modo y manda sku/desc/porBolsa/bulto. El default de despacho ahora usa _serialUnits (suma de bolsas) en vez de contar comas. _cargarMaestro() en cargar() y tras preparar |
| Despacho_Código.js | 3.10 | Maestro de artículos auto-construido (tab MAESTRO_ARTICULOS en NOTAS_SS): WOS_maestroArticulos() devuelve mapa SKU→{porBolsa,bulto} (cache 5min) para pre-marcar la preparación; _wosGetHojaMaestro() crea la hoja con encabezado si falta; _wosUpsertMaestro() registra/actualiza qué SKU va por bolsa (refleja la última elección del operario). WOS_prepararConSeriales aprende del payload (sku/desc/porBolsa/bulto) y hace upsert tras marcar preparado (en try/catch, nunca rompe la preparación). qtyPrep/seriales del payload se guardan igual que antes (el front ya manda la suma de bolsas y el manifiesto "SO x50") |
| Despacho_Env.js | 1.5 | Maestro por bolsa: HOJA_MAESTRO='MAESTRO_ARTICULOS' + COL_MAESTRO {SKU,DESC,POR_BOLSA,BULTO,FECHA,OPERADOR} |
| WOS_GmailFlow.js | 2.10 | Anexo N° de Serie de la NE: un token "SO-123 x50" se muestra como "SO-123 · bolsa de 50 u." (regex xN) para auditar bolsas de consumibles; seriales sueltos sin cambios |
| Despacho_Index.html | 3.25 | Consulta de Stock: la carga ahora usa la MISMA pantalla de carga que al ingresar a WOS (wosLoadStart/wosLoadDone, overlay z-index 9980 con barra de progreso + ícono + pasos). Nuevo STEPS_STOCK ('Conectando con el inventario' → 'Leyendo stock de CARMEN' → 'Cargando ubicaciones WMS'). _cargarStockRemoto llama wosLoadStart(STEPS_STOCK) al inicio y wosLoadDone() en success/failure. Reemplaza el spinner en el cuerpo de la 3.24 |
| Despacho_Index.html | 3.24 | Consulta de Stock: la carga (WOS_cargarStock lee CARMEN+MASTER+UBICACIONES, puede tardar) ahora muestra un spinner animado centrado (reusa .spinner) en vez del texto plano "Cargando stock…". El spinner global sp() no sirve acá porque su overlay (z-index 150) queda detrás del overlay de stock (z-index 250), por eso va dentro de stk-content |
| Despacho_Código.js | 3.9 | BUGFIX backorder: el flujo de Backorder se saltaba la preparación obligatoria (SN/peso/bins). WOS_prepararConSeriales ahora acepta qtyPrep por fila y fija el estado por fila: Preparado si qtyPrep>=cantPend, Preparado Parcial si es menos (cantPend = sol-desp-cancel, igual que col G). Solo toca las filas del payload → las sin stock quedan en Backorder. La cantidad preparada se deriva del nº de SN (col Y) — sin columna nueva |
| Despacho_Index.html | 3.23 | BUGFIX backorder: reemplaza los 2 botones ("Despachar lo que llegó" directo + "Todo disponible" que solo cambiaba estado) por UNO: "📦 Preparar lo que llegó" → abre el modal de preparación (captura SN/peso/bins) en MODO PARCIAL. abrirModalPreparar(numero, parcial): en parcial incluye solo ítems con stock (los sin stock quedan en backorder) y muestra por ítem un input "Preparar q de N · llegó M" (max=min(cantPend,stock)); al cambiarlo re-renderiza los SN (nuevo _prepRenderSers) y los bins (_prepQtyChange). confirmarPreparar manda qtyPrep=nº SN por fila. El modal de despacho pre-llena la cantidad con las unidades preparadas (nº de SN registrados, acotado a cantPend). Modo normal (Confirmado→Preparado) sin cambios (cantidad fija, gate de stock intacto) |
| Despacho_Env.js | 1.4 | Nueva col AB PESO_PREP (27): peso exacto del paquete (kg) capturado al preparar |
| Despacho_Código.js | 3.8 | Peso del paquete al PREPARAR: WOS_prepararConSeriales acepta 4º arg peso y lo escribe en col AB (PESO_PREP) en cada fila del pedido; la guarda de columnas ahora asegura hasta AB (getMaxColumns < PESO_PREP+1). WOS_cargarPedidos expone pesoPrep por pedido (Number col AB) para pre-llenar el bulto al despachar |
| Despacho_Index.html | 3.22 | El peso exacto del paquete se pide al PREPARAR (antes se pedía recién al despachar y el operador se lo olvidaba). Modal-prep: campo obligatorio "Peso exacto del paquete (kg)" en el pie; _prepCount exige peso>0 además de todos los SN/SO para habilitar Confirmar; confirmarPreparar valida y lo manda como 4º arg a WOS_prepararConSeriales; abrirModalPreparar precarga el peso si ya estaba guardado. Modal de despacho: pre-llena el peso del 1er bulto con ped.pesoPrep (editable; si hay varios bultos el operador lo reparte) → nunca se despacha sin peso |
| WOS_GmailFlow.js | 2.9 | Mail de despacho (a reseller + administración/facturación en CC): además del PDF de la Nota de Entrega, ahora incluye el LINK a la MISMA NE en versión planilla (Google Sheets). _wosGenerarPDF conserva la hoja temporal TEMP_NE en Drive (la renombra NE_<num>-<nn>, la mueve a la carpeta de NE, la comparte con link) en vez de mandarla a la papelera, y devuelve sheetUrl junto al PDF. El bloque "Para administración" (HTML) y el texto plano muestran ambos links (📄 PDF · 📊 Planilla). Sin adjuntar xlsx → sin UrlFetchApp → SIN cambio de scopes / SIN re-autorización |
| Despacho_Env.js | 1.3 | Nueva col AA UBIC_PREP (26): guarda los bins (ubicación WMS) elegidos al preparar como JSON [{bin,cant}] por ítem. El descuento real del WMS se sigue aplicando al despachar |
| Despacho_Código.js | 3.7 | WOS_prepararConSeriales acepta ubicaciones:[{bin,cant}] por fila en el payload y las guarda en col AA (UBIC_PREP) como JSON al marcar Preparado. Mueve la elección del BIN al momento de preparar (antes de "Preparado"), en el mismo modal donde se piden SN/SO. INTEGRIDAD: getRange no auto-expande columnas → guarda que inserta la col AA con insertColumnsAfter si la hoja tiene <27 columnas, antes de escribir (evita excepción "coordenadas fuera de la hoja" que abortaría la preparación); insertar al final no desplaza A–Z ni afecta la fórmula CANT_PEND =E-F-Z |
| WOS_GmailFlow.js | 2.8 | WOS_despacharCompleto ya NO recibe los bins del modal de despacho: los lee de col AA (UBIC_PREP), elegidos al preparar, vía nuevo helper _parseUbicPrep (JSON tolerante). Descuenta UBICACIONES de Carmen igual que antes y limpia col AA de la fila tras descontar para que un 2° despacho de la misma fila (backorder) no vuelva a descontar. El batch queda cubierto (llama a WOS_despacharCompleto) |
| Despacho_Index.html | 3.21 | El BIN (ubicación WMS) se elige al PREPARAR, no al despachar. Modal-prep: por cada ítem, sección "De qué ubicación WMS lo sacás" (carga bins con WOS_cargarUbicacionesPedido + _prepInyectarUbicaciones; cantidad fija = cantPend). Helpers de bins (_renderBinAlloc/_binAllocAuto/_getBinAllocForRow) parametrizados con prefijo (default 'mdi', prep usa 'prep'); _getBinAllocForRow cae a d.cantTotal en 1-bin cuando no hay input editable. confirmarPreparar manda ubicaciones por fila. Modal de despacho: se quitó la sección de bins (contenedores mdi-ubic-wrap, carga de ubicaciones y ubicaciones en despachos.push) y la función muerta _inyectarUbicaciones |
| Despacho_Index.html | 3.20 | Modal de series (preparar): MODO ESCANEO. Barra arriba con un campo grande "escaneá el QR" (pistola/lector físico: escanea→Enter→carga la próxima unidad vacía y limpia para el siguiente) + botón "Cámara" que abre la cámara del dispositivo con html5-qrcode (CDN, lazy-load) y va cargando por QR leído. _prepFeed carga la próxima unidad vacía y bloquea duplicados (beep ok/err + flash verde). Anti-relectura del mismo QR (1.6s). La cámara es best-effort: si Apps Script bloquea getUserMedia, muestra mensaje y queda el lector físico. Se corta la cámara al cerrar/confirmar. NO toca appsscript.json (sin re-auth) |
| Despacho_Index.html | 3.19 | Ajuste del modal de series: se quitó la autogeneración de códigos SO (botón por ítem + "SO a los vacíos" + _prepGenerarSO/_prepSyncSoSeq/_prepPad3). Ahora hay UN campo por unidad donde el picker escribe el código a mano — SN de fábrica o SO indistintamente (el SO funciona igual que un SN; cómo se asignan se define después). Sigue siendo obligatorio 1 código único por unidad, con validación de duplicados y Enter para saltar al siguiente |
| Despacho_Index.html | 3.18 | N° DE SERIE OBLIGATORIO al preparar. "📦 Listo para despachar" (y "Marcar preparado" de la cola de picking) ya no cambia el estado directo: abre modal #modal-prep que exige un N° de serie por CADA unidad (una fila de input por unidad de cantPend). Si una pieza no tiene serie → botón "🏷 SO" por ítem o "SO a los vacíos" autogenera códigos SO-<pedido>-NNN. Contador X/Y y botón Confirmar deshabilitado hasta completar todo; valida duplicados; Enter salta al siguiente input (para escáner). Confirma vía WOS_prepararConSeriales (escribe col Y por fila + estado Preparado). El modal de despacho muestra "🏷 Series registradas: …" y la nota de entrega hereda esas series aunque no se reingresen. Objetivo: manifiesto por unidad para auditar discrepancias (picker envió 10, reseller recibió 4). z-index 350 para verse sobre la cola de picking |
| Despacho_Código.js | 3.6 | Nueva WOS_prepararConSeriales(numero, seriales[{row,seriales}], operario): marca Preparado + escribe N° de serie/SO por fila en col Y (SERIALES), saltea filas ya cerradas (Entregado/Cancelado), loguea acción. WOS_cargarPedidos: cada ítem ahora incluye seriales (col Y) para prefill/visualización en el front |
| WOS_GmailFlow.js | 2.7 | WOS_despacharCompleto: los itemsDesp de la nota de entrega usan como fallback las series ya registradas al preparar (col Y) cuando el modal de despacho no reingresa seriales → el manifiesto de series siempre aparece en la NE |
| Despacho_Index.html | 3.17 | Nueva vista "Despacho parcial" (nav-tab, overlay dp-overlay): resumen por reseller de qué se puede enviar AHORA vs qué queda para el próximo envío. 100% cliente desde _pedidos (usa stockActual por SKU). _computeDespachable reparte el stock GLOBALMENTE en orden FIFO (pedido más viejo primero) para no prometer las mismas unidades a dos líneas/resellers; considera líneas con cantPend>0 en estados Confirmado/Preparado/Preparado Parcial/Backorder. Por reseller muestra "Ahora: X u/$" (verde) y "Falta: Y u/$" (naranja), desglose por pedido y SKU (pend/ahora/falta), y botón "Copiar aviso" que arma el texto para mandarle al reseller (lo despachable ahora + lo que queda + pregunta si quiere envío parcial). Sin backend nuevo, sin escrituras, sin mails |
| Despacho_Código.js | 3.5 | Fix vista: un pedido con ítems entregados + alguno cancelado se mostraba como "Cancelado". El rollup de estado del pedido (WOS_cargarPedidos) sacaba Cancelado de EST_PRIORIDAD; ahora Cancelado solo es el estado del pedido si TODOS los ítems están cancelados. Sin ítems accionables pendientes, prioriza el terminal ENTREGADO (Entregado_Cerrado > Listo_Retiro > Entregado_Confirmado) sobre Cancelado. Solo derivación de vista (los estados por SKU no cambian) |
| WOS_GmailFlow.js | 2.7 | BUGFIX: al hacer un 2° despacho de un pedido, el código de seguimiento (col Q) se PISABA con el del envío nuevo → se perdía el tracking anterior de esa fila. Ahora WOS_despacharCompleto acumula el tracking por fila con _wosMergeTracking (une "T1 \| T2" sin duplicar); la cantidad ya era acumulativa. FECHA_DESPACHO y NOTA_ENTREGA siguen tomando el valor del último despacho. + Recuperación: WOS_recuperarTracking(numero, guardar) parsea los códigos pisados desde los mails de despacho del hilo ("Código de seguimiento: ...") y opcionalmente los reescribe en las filas despachadas; wrapper WOS_recuperarTrackingTest para correr desde el editor. Usa Gmail de solo lectura (ya usado por WOS → sin re-auth) |
| WOS_GmailFlow.js | 2.6 | Anti doble-submit extendido a las demás acciones que mandan mail al reseller: helper _wosLockIdempot(reqToken, fn) (lock + idempotencia reutilizable) envuelve WOS_notificarFaltante y WOS_procesarRespuestaManual → un doble-click / dos pestañas / reintento no manda 2 mails ni cambia estado dos veces. Clave de idempotencia estable por acción+pedido (dedup incluso entre pestañas) |
| Despacho_Código.js | 3.4 | Anti doble-submit: WOS_cancelarPedido y WOS_enviarResumenEnvios envueltos en _wosLockIdempot (reqToken) → no duplican mail de cancelación / resumen. WOS_cambiarEstado NO se bloquea (lo llama cancelarPedido; evita lock anidado; sus escrituras de estado son idempotentes). Nota: WOS_recibirMercaderia quedó sin caller en el front (huérfana) → no se tocó |
| Despacho_Index.html | 3.16 | Ajuste del aviso global: _esBackorderListo ahora exige que el backorder se pueda FINALIZAR COMPLETO (todos los ítems pendientes con stock >= pendiente), no parcial — los parciales se resuelven con el bloqueo al despachar (feature 1) cuando el reseller pide otra cosa. Textos: banner "N backorder se pueden finalizar completos" + badge "📦 finalizable" |
| Despacho_Index.html | 3.15 | Aviso global de backorders listos: banner persistente ámbar arriba del listado cuando hay pedidos con ítems en Backorder que ya tienen stock (≥1u) — cuenta + resellers + botón "Ver y finalizar" que filtra a Backorder (_backordersListos/_syncBackorderBanner/_verBackordersListos, enganchado en actualizarKPI). Cada tarjeta de backorder listo muestra badge "📦 stock listo" (_esBackorderListo). No bloquea el resto de la app pero es imposible de ignorar (pulso). Solo frontend |
| Despacho_Index.html | 3.14 | Backorder con stock del mismo reseller al despachar: al abrir el modal de despacho de un pedido, detecta otros pedidos de ESE reseller con ítems en Backorder cuyo SKU ya tiene stock (≥1u, usa item.stockActual) y BLOQUEA el despacho con aviso ámbar arriba del modal (_backordersConStock/_avisoBackorderHTML). Dos salidas: "Agregar backorder y despachar junto" (re-arma como batch reutilizando WOS_despacharBatch vía _abrirBatchCon refactorizado desde abrirBatchDespacho) o "Despachar solo este pedido" (override con confirm). Botón Confirmar deshabilitado hasta resolver (_gateDespachoBtn + guard en confirmarDespacho). Solo frontend; usa --yellow-m/--yellow (--amber-m no está definida en WOS) |
| Despacho_Index.html | 3.13 | Pasa token de idempotencia estable a las 4 acciones que mandan mail: notificarFaltante ('falt_'+num), procesarRespuestaManual ('rt_'+num+'_'+opcion), cancelarPedido ('canc_'+num), enviarResumenEnvios ('resum_'+reseller+'_'+mes) — junto con el lock backend cierran doble-click/2 pestañas/reintento |
| WOS_GmailFlow.js | 2.5 | Anti doble-despacho (defensa en profundidad): WOS_despacharCompleto ahora toma LockService.getScriptLock (waitLock 30s, release en finally) para serializar ejecuciones, + idempotencia por reqToken (CacheService TTL 10 min: _wosIdempotResultado/_wosIdempotGuardar) → un doble-click / reintento tras respuesta perdida devuelve el resultado previo sin re-ejecutar (no duplica nota de entrega ni mail de facturación ni suma cantidad dos veces). Devuelve {ok,nota}. Sin cambios de OAuth (Lock/Cache son servicios internos) |
| Despacho_Código.js | 3.3 | WOS_despacharBatch acepta reqToken y lo propaga como token por pedido (reqToken + '::' + numero) a WOS_despacharCompleto → el batch también es idempotente ante reintento |
| Despacho_Index.html | 3.12 | Anti doble-submit de despacho: confirmarDespacho tiene guard de re-entrada (_despachoEnCurso) + genera token de idempotencia (_wosGenToken) que pasa a WOS_despacharCompleto/WOS_despacharBatch; el flag se libera en success/failure. Cierra el hueco de doble-click / dos pestañas / reintento tras timeout |
| Despacho_Index.html | 3.11 | Descuento multi-bin: reemplaza select de ubicacion por tabla de asignacion por bin; auto-allocacion ASC (menos stock primero); boton Auto para recalcular; total asignado validado en tiempo real; envia ubicaciones:[{bin,cant}] al backend |
| WOS_GmailFlow.js | 2.4 | WOS_despacharCompleto: ubicMap pasa de string a [{bin,cant}]; descuento multi-bin en UBICACIONES de Carmen (lee una vez, descuenta en cascada, elimina filas vacias en reversa); Entregados registra bins como "BIN×cant, BIN×cant" |
| Despacho_Código.js | 3.2 | WOS_cargarUbicacionesPedido: sort de bins cambiado a ASC (menos stock primero) para que la auto-asignacion vacia los bins mas chicos antes |
| Despacho_Código.js | 3.1 | WOS despacha todo: WOS_cargarPedidos fusiona Pedidos_resellers + Pedidos_OTs; _procesarFilasPedidos extrae loop; todas las funciones per-numero (cancelar, cambiarEstado, reactivar, revertir, obs, etiqueta) usan _getHojaPorNumero; WOS_buscarBackorderPorSKU + WOS_recibirMercaderia + WOS_reporteBackorder + WOS_checkCambios + WOS_getResumenEnvios escanean ambas hojas; WOS_actualizarValidacion aplica a ambas; WOS_migrarPedidosOTs() función one-time que recrea Pedidos_OTs con 26 cols |
| WOS_GmailFlow.js | 2.3 | Fix: _esDJIAprobado y formaPago se declaraban DESPUÉS de usarlas en adminHtml (hoisting var → undefined); movidas al bloque previo a tituloAdmin |
| WOS_GmailFlow.js | 2.2 | WOS_despacharCompleto: email admin muestra "Sin costo — DJI aprobado" (verde) o "A 30 días" según OBS del pedido OT; PR mantiene campo PAGO existente |
| WOS_GmailFlow.js | 2.1 | WOS_despacharCompleto: al despachar OT-, actualiza E: por SKU en col Q (REPUESTOS) del HUB PRO + estado "Repuestos enviados" solo si despacho completo (sin backorder) |
| WOS_GmailFlow.js | 1.9 | _wosLeerPedido usa _getHojaPorNumero(numero); WOS_recuperarThreadIds escanea ambas hojas con hoja-por-numero; WOS_procesarRespuestaManual usa _getHojaPorNumero |
| Despacho_Env.js | 1.2 | HOJA_PEDIDOS_OT, _getHojaPedidosOT(), _esNumeroOT(), _getHojaPorNumero() — resolver de hoja por prefijo OT- vs resellers |
| Despacho_Index.html | 3.7 | Modal despacho: selector de ubicacion WMS por item (recomendacion = mayor stock); pick ticket muestra ubicacion sugerida en negrita |
| WOS_GmailFlow.js | 1.8 | WOS_despacharCompleto: escribe ubicacion en col H de Entregados y resta cantidad de UBICACIONES |
| Despacho_Código.js | 2.9 | WOS_cargarUbicacionesPedido(skus): carga ubicaciones WMS de multiples SKUs en una lectura, ordenadas desc por cantidad |
| Despacho_Código.js | 3.0 | CANT_CANCEL col Z: demanda perdida usa CANT_CANCEL con fallback a CANCELADO viejo | Optimizaciones: elimina funciones duplicadas (_lookupResellerEmail, _emailHead/_emailFoot), unifica cache key CARMEN, enCaminoMap en WOS_cargarStock llama a WOS_getEnCaminoMap(), alinea lógica de compras activas en reporteBackorder, batch writes en WOS_revertirAPreparado, elimina logs debug |
| Despacho_Index.html | 3.0 | Pick ticket: columna Estado + fix multi-página al imprimir (position:fixed→static) |
| Despacho_Index.html | 3.6 | Pick ticket: badge de cada ítem usa it.estado real (mismo que la tarjeta de pedido); print-color-adjust para colores en impresión |
| Despacho_Index.html | 3.3 | Pick tracker: badge ciclable reemplazado por select directo; _buildPickCajaOpts/_onPickCajaSel/_actualizarPickCajaSelects en lugar de _cyclePickCaja |
| WOS_GmailFlow.js | 1.7 | Nota de entrega agrupa items por caja con fila separadora Caja N + tracking cuando hay multi-bulto; Fix 1.6: fila TOTAL en col 6 |
| Despacho_Código.js | 2.8 | WOS_cargarStock: lee ubicaciones desde Carmen UBICACIONES tab (multi-ubicacion WMS); fallback a STOCK_REPUESTOS para items sin mapear |
| Despacho_Env.js | 1.1 | Agrega CARMEN_UBICACIONES_TAB = 'UBICACIONES' |
| Despacho_Index.html | 3.10 | Fix Lista Compras: aComprar = max(0, bo - enCamino - stockActual); stk se calculaba después de aComprar y nunca se descontaba — SKUs con stock suficiente mostraban "a comprar" en lugar de "Cubierto" |
| Despacho_Index.html | 3.9 | Batch despacho: checkbox incluye Backorder; en abrirBatchDespacho items de BO renderizan con value=0 + hint ámbar + borde ámbar; badge BACKORDER en separador de sección; ítems cancelados/ya-enviados excluidos del batch |
| Despacho_Index.html | 3.8 | Badges WMS en tabla de ítems: columna "Ubicación WMS" con wosPintarBadgesUbicacion(); CSS .wms-bin picking/reserva (tokens nativos); snapshot cargado post-render via _cargarWmsSnapshot() + _actualizarBadgesWms() sin bloquear UI |
| WOS_StockSnapshot.js | 1.1 | WOS_obtenerStockSnapshot() función pública para google.script.run; devuelve stockMap serializable al frontend |
| WOS_StockSnapshot.js | 1.0 | Módulo read-only WMS: _wosCargarStockSnapshot() triple in-memory mapping (Carmen STOCK + UBICACIONES + MASTER TABLA_POSICIONES); sort PICKING-first + alfanumérico natural; _wosStockBySku() helper de consulta |
| Despacho_Env.js | 1.0 | |

## STOCK_MANAGER
| Archivo | Versión | Notas |
|---------|---------|-------|
| Sm_Compras.js | 1.4 | Aviso al reseller cuando ingresa mercadería bloqueada: recibirMercaderia ahora llama _encolarNotifIngresoWOS(cas, items), que cruza el CAS recibido con RESERVAS_EN_CAMINO (Activa) y encola una fila por (pedido, SKU) en la hoja nueva NOTIF_INGRESOS_WOS del MASTER (FECHA·CAS·PEDIDO·RESELLER·SKU·CANTIDAD·ESTADO·RESULTADO; se auto-crea). SM solo encola — el mail lo arma y envía WOS (detector). Sin re-auth |
| Env.js | 1.3 | ETA de compras DJI (Fase 1): SCHEMA.COMPRAS_DETALLE suma FECHA_ETA(6) y N_AIR(7) — ETA por línea (dd/MM/yyyy) y N° de envío (AIR), importadas del planner externo "DRP - Operaciones". Columnas al final → no corren índices que ya usan SM/WOS. Sin re-auth |
| Sm_Compras.js | 1.3 | ETA de compras DJI (Fase 1): el importador existente (cruzarComprasExternas + sincronizarItemsCAS, que ya lee la hoja externa 15Y4… pestaña "Pedidos ") ahora también trae la ETA. Nuevo _normEtaVal (Date→dd/MM/yyyy; '0'/vacío→sin ETA). cruzarComprasExternas detecta la col "FECHA ESTIMADA EN PLANTA" (por nombre: estimada/planta), la compara con la guardada y, si cambió, la reporta en diferencias (nuevos campos etaExt/etaSm/etaChg/qtyChg) aunque la cantidad no haya cambiado → verifica ETA y te avisa. sincronizarItemsCAS guarda ETA + N° AIR por línea en COMPRAS_DETALLE (cols nuevas), normalizando todas las filas a 8 columnas (rellena las viejas de 6). Header de COMPRAS_DETALLE actualizado a 8 cols. Sin re-auth |
| SM_Index.html | 5.29 | ETA de compras DJI (Fase 1): el panel de Cruce externo ahora muestra los cambios de ETA. La sección "Diferencias" pasa a "Diferencias de cantidad / ETA vs sheet externo" y cada ítem con ETA cambiada agrega bajo el SKU una nota "🚚 ETA: <vieja> → <nueva>" (dd.etaChg/etaSm/etaExt). El botón "Usar externo" (sincronizarItemsCAS) ya persiste la ETA nueva. La primera corrida tras el deploy va a marcar ETA en casi todos los CAS (backfill inicial, la guardada estaba vacía). Sin re-auth |
| SM_Index.html | 5.28 | Tres mejoras de etiquetas SO: (1) RECORDAR ETIQUETA POR CÓDIGO — nuevo _etqMap (SKU→lleva etiqueta, viene de getEtiquetaMetaRecepcion.etiquetar); _recepRowHTML pre-tilda "🏷 Etiqueta" en los códigos aprendidos (excepto serializados y bolsas, que se hacen aparte), así el operador no tiene que tildarlos cada vez. Se aprende solo al generar por unidad (backend). (2) CONFIRMAR ANTES DE IMPRIMIR — _confirmarTandaEtq(total) pide confirmación nativa solo si la tanda supera _ETQ_CONFIRM_UMBRAL (20 etiquetas); red de seguridad contra impresiones enormes por error. Gate en confirmarReciboParcial, confirmarEstadoCAS (si cancela, recibe igual sin imprimir) y confirmarImprimirSO (manual). Helper _recepTotalEtq suma las cantidades. (3) REIMPRIMIR ETIQUETAS — el modal SO ahora tiene toggle "Generar nuevas / Reimprimir" (_soModoVista); el panel de reimpresión toma códigos SO escaneados/pegados (uno por línea) y reimprime tal cual vía backend reimprimirSOs → _imprimirEtiquetasSO. Sin re-auth |
| SM_Index.html | 5.27 | Plantilla de impresión de etiquetas SO (_imprimirEtiquetasSO) mejorada: (1) el SKU ya NO se parte en dos renglones — se le sacó word-break:break-all y ahora sale en 1 sola línea con auto-shrink por JS (fit(): baja el font-size de 9pt hasta 5pt hasta que entra); (2) se agregó el NOMBRE DEL REPUESTO (l.descripcion, que el backend ya devolvía) debajo del SKU, en .desc 5.5pt con clamp a 2 líneas; (3) el código SO también sale en 1 línea con auto-shrink (6.2→3.6pt). Para dar lugar al texto, el QR se achicó de 21mm a 19mm y se ajustaron gaps. Aplica a TODAS las etiquetas SO (recepción de compras y generador manual de SO). Sin cambios de backend. Sin re-auth |
| SM_Index.html | 5.26 | Etiquetas en la recepción ahora son ELECCIÓN POR FILA del operador, no automáticas: cada tarjeta trae un control "🏷 Etiqueta" (destildado por defecto) que, si se tilda, imprime 1 etiqueta por unidad recibida de ese código. Esto resuelve el problema de que un código nuevo en gran cantidad (ej. 500 tornillos) imprimía 500 etiquetas innecesarias. Se ELIMINÓ toda la lógica automática de bolsas en la recepción (round(qty/uxb), modo:'bolsa') y el interruptor global "🏷 Imprimir etiquetas de lo recibido" (recep-print-toggle / recep-print-toggle-est): las bolsas se etiquetan aparte después. Los ítems con N° de serie muestran un chip "🔢 N° serie / sin etiqueta" en vez del control (llevan su serie real, no SO). _recepEtiquetasAuto → renombrado a _recepEtiquetasElegidas (solo filas con "Llegó"+"🏷 Etiqueta" tildados, 1 por unidad); _recepRowHTML agrega el control/chip; _recepToggleRow muestra/oculta etiqueta+chip junto a "Recibí"; nuevo _recepToggleEtq resalta el rótulo; fila manual (agregarItemRecepcionManual) también trae el control. Hint guía bajo cada checklist: "Tildá Etiqueta solo en los códigos que necesiten etiqueta individual. Las bolsas se etiquetan aparte." CSS nuevo .recep-etq/.recep-sn-tag; .recep-print-lbl queda sin uso. Sin re-auth |
| SM_Index.html | 5.25 | Recepción de Compras DJI rediseñada para operadores básicos: dejó de ser un formulario complicado y pasó a un CHECKLIST tipo "tildar lo que llegó OK contra el PL". Cada ítem es una tarjeta grande (.recep-card) con un tilde de 26px "Llegó" (pinta la fila de verde) + SKU/descripción + cantidad pedida bien visible; al tildar aparece "Recibí" con la cantidad pre-cargada al total (editable solo si vino menos). "Recibir todo" arranca todo tildado (destildás lo que falte); "Recibir parcial" arranca vacío para ir tildando; botones "Marcar todo/Limpiar". Se ELIMINARON los dropdowns u./bolsa/mixto por fila: las etiquetas se deciden solas (serie→sin SO, bolsa→por bulto, resto→por unidad) con un único interruptor global "🏷 Imprimir etiquetas de lo recibido". Nuevos helpers _recepRowHTML/_recepToggleRow/_recepMarcarTodo/_recepColectarItems/_recepEtiquetasAuto; se aplicó el mismo checklist al modal de estado→"En depósito" (_cargarManifiestoEnRecepcion, agregarItemRecepcionManual, confirmarEstadoCAS). Los helpers viejos por-fila (_etiqCellInline/_recepColectarEtiquetas/_recepModoEtiq/_recepRecalcEtiq/_recepSyncEtiqQty/_recepAutoBolsaManual) quedan sin uso (código muerto inofensivo). Sin re-auth |
| SM_Index.html | 5.24 | FIX bug de doble selección de etiqueta en la recepción de Compras DJI: la elección de etiqueta/bulto aparecía en el manifiesto (para "Recibir todo") y OTRA VEZ, reseteada, al entrar a "Recibir parcial" → el operador la elegía dos veces. Ahora la selección ocurre en UN solo lugar: se quitó la columna Etiqueta del manifiesto (queda solo lectura) y tanto "Recibir todo" como "Recibir parcial" abren la misma pantalla de filas (cantidad a recibir + etiqueta) vía nueva _recepMostrarFilas(titulo) → confirman con confirmarReciboParcial(). "Recibir todo" ya no recibe directo: muestra la pantalla con cantidades pre-cargadas al total para revisar/confirmar (además evita imprimir SO de más sin revisar). recibirCASCompleto/recibirCASParcial quedan como wrappers finos |
| SM_Index.html | 5.23 | FIX visual de la recepción de Compras DJI: (1) el modal "Contenido del Pedido" (#modal-detalle-cas) se agrandó de 680px a min(960px,95vw); (2) los números de la columna Etiqueta se cortaban ("15"→"1!") porque los inputs usaban la clase form-control que fuerza padding:8px 12px!important con ancho chico. Se les sacó form-control/form-select y se estilan con clases propias .recep-etiq-modo/.recep-etiq-cant/.recep-etiq-uxb/.recep-etiq-sueltas (inputs 62px, select 88px, fuente 13px, sin las flechitas del number vía -webkit-appearance:none). _etiqCellInline reescrito con esas clases y fuentes más grandes; sin cambios de lógica |
| SM_Index.html | 5.22 | Los OPERADORES ahora también ven "Compras DJI": en applyRoleUI se sacó nt-comp/tab-comp de la lista toHide (queda solo Movimientos nt-mov/tab-mov como admin). El backend de compras nunca chequeó rol (solo toma operador), así que el operador puede registrar CAS, actualizar estados y recibir mercadería igual que hoy. ADMIN sigue viendo todo |
| SM_Index.html | 5.21 | Etiquetas de recepción: (1) los SKU marcados "requiere N° de serie" (STOCK_REPUESTOS.REQUIERE_SN) arrancan DESTILDADOS y muestran "SN" en vez de 🏷 (llevan SN real, no SO; se pueden tildar igual) — mapa _reqSNMap; (2) nuevo modo "mixto" en el toggle (u./bolsa/mixto): N bolsas × u. + M sueltas por unidad en una misma fila, prellenado floor(recibidas/uxb) bolsas + resto sueltas → _recepColectarEtiquetas devuelve DOS ítems del mismo SKU (bolsa + unidad, el backend ya los procesa); _recepRecalcEtiq/_recepModoEtiq manejan el 3er modo + campo .recep-etiq-sueltas; (3) los mapas bolsa+SN se cargan con _cargarEtiquetaMeta (una sola llamada getEtiquetaMetaRecepcion) en onload y se refrescan al abrir el CAS (verDetalleCAS/abrirEstadoCAS) → mata la carrera de carga y la data vieja. _recepAutoBolsaManual también destilda si el SKU manual requiere SN |
| SM_Index.html | 5.20 | La impresión automática de etiquetas en la recepción ahora contempla BULTO vs unidad (algunas cosas van por bolsa). Cada fila trae un toggle "u./bolsa": los SKU marcados por bolsa en el maestro MAESTRO_ARTICULOS arrancan en modo bolsa solos (se carga _bolsasMap en window.onload con getMaestroBolsas). En modo bolsa la "cantidad" pasa a ser Nº de bolsas (prefijada = round(recibidas / u.porBolsa)) y aparece "× N u." editable; cada etiqueta sale con el badge amarillo "BOLSA xN u.". _recepRecalcEtiq recalcula la cantidad al cambiar recibir/modo/bulto (respeta data-touched); _recepModoEtiq togglea; _recepAutoBolsaManual pasa a bolsa un ítem manual si el SKU tipeado es de bolsa. _recepColectarEtiquetas ahora manda {sku,cantidad,modo,unidadesPorBolsa}. _etiqCellInline pasó a (sku, unidadesRecibidas). Si el SKU no está en el maestro, sale por unidad (y podés togglear a mano) |
| SM_Index.html | 5.19 | Impresión AUTOMÁTICA de etiquetas SO al recibir una compra DJI, con elección por código. Las 3 vistas de recepción (manifiesto de "Recibir todo" en _renderDetalleCAS, filas editables de recibirCASParcial, y _cargarManifiestoEnRecepcion del modal estado-cas + ítems manuales de agregarItemRecepcionManual) agregan por fila un tilde "🏷 Etiqueta" (MARCADO x defecto) + cantidad editable (arranca en las unidades recibidas, se refleja sola mientras no se toque vía _recepSyncEtiqQty). Al confirmar la recepción (recibirCASCompleto / confirmarReciboParcial / confirmarEstadoCAS) se juntan los códigos tildados (_recepColectarEtiquetas: ignora filas con recibir=0), se abre la ventana de impresión ANTES del google.script.run (preserva el gesto → no la bloquea el popup blocker), y al recibir OK se generan+imprimen en un tiro con generarSOLote → _imprimirEtiquetasSO. Si no se tilda nada, no abre ventana. Helpers nuevos: _etiqCellInline, _recepSyncEtiqQty, _recepColectarEtiquetas, _recepAbrirVentanaEtiq, _recepPostImprimirEtiq. Reusa _imprimirEtiquetasSO (QR 50×25mm). Sin re-auth |
| Sm_Etiquetas.js | 1.6 | Soporte de las 3 mejoras de etiquetas: (1) el maestro compartido con WOS suma la col G "Lleva etiqueta" (ETIQUETA); nuevo helper _smMaestroHoja() asegura las 7 columnas, _smMarcarEtiquetar(sku,desc,op) la setea true sin tocar POR_BOLSA, y getMaestroEtiquetar() la lee → {SKU:true}. getEtiquetaMetaRecepcion() ahora devuelve {bolsas, sn, etiquetar}. generarSO y generarSOLote (modo unidad) llaman _smMarcarEtiquetar → el sistema APRENDE qué SKUs llevan etiqueta para pre-tildarlos en la próxima recepción. (2) — (front). (3) reimprimirSOs(texto): recibe uno o varios códigos SO (escaneados/pegados, separados por espacio/coma/salto), los busca en SO_ETIQUETAS y devuelve {ok, labels:[{so,sku,descripcion,cantidad?}], faltan:[...]} para reimprimir idénticos. Sin re-auth |
| Sm_Etiquetas.js | 1.5 | Nueva getReqSNMap() → {SKU:true} de STOCK_REPUESTOS.REQUIERE_SN (para destildar en la recepción los que llevan N° de serie real) y getEtiquetaMetaRecepcion() que devuelve {bolsas, sn} en una sola llamada (la usa el front en onload / al abrir el CAS). generarSOLote no cambió: ya procesaba el mismo SKU dos veces con maxN incremental, así que el modo "mixto" (bolsas + sueltas) del front sale sin tocar el backend |
| Sm_Etiquetas.js | 1.4 | generarSOLote ahora soporta BULTO además de unidad: cada item = {sku, cantidad, modo, unidadesPorBolsa}; en modo 'bolsa' genera `cantidad` etiquetas de bulto (cada una = unidadesPorBolsa, registra la col F=CANTIDAD y marca el SKU por bolsa en el maestro vía _smUpsertMaestro, igual que generarBolsasSO), en 'unidad' 1 por unidad (col F vacía). Todas las filas se escriben con 6 columnas para poder mezclar modos en una sola escritura; maxN por SKU se incrementa dentro del lote (soporta SKU repetido). Nueva getMaestroBolsas() → {SKU: unidadesPorBolsa} leyendo MAESTRO_ARTICULOS (POR_BOLSA + BULTO) para que el front sepa qué códigos son de bulto |
| Sm_Etiquetas.js | 1.3 | Nueva generarSOLote(items,operador) para la impresión automática en la recepción: genera etiquetas SO POR UNIDAD para varios SKUs en una sola pasada (un solo lock + una sola escritura). Agrupa por SKU (suma cantidades), cachea en 200 por SKU, calcula el último NNN por SKU con el mismo criterio de prefijo que generarSO y reusa _getHojaSO/_descDeSku. Devuelve labels[{so,sku,descripcion}] (sin badge de bolsa) + porSku. No toca generarSO/generarBolsasSO (el modal manual sigue igual) |
| Sm_ComprasInternas.js | 1.0 | NUEVO módulo: lista interna "qué hay que comprar" (herramientas e insumos) que cargan los operarios. Hoja "Compra de herramientas e insumos" del MASTER (cols A–I del usuario; J=ID lo gestiona la app para ubicar filas). Funciones: getComprasInternas (lee + parsea pedido/llego truthy), agregarCompraInterna (fecha auto al cargar, total=cant×precio, valida descripción), marcarCompraInterna(id,campo,valor) para tildar ¿Pedido?/¿Llegó? (cualquiera puede, guarda 'SÍ'/''), editarCompraInterna, eliminarCompraInterna. Helpers: _ciSetup (idempotente: crea hoja si falta, asegura encabezado ID y rellena IDs faltantes de filas viejas), _ciFindRowById, _ciSafe (anti-inyección de fórmulas en desc/tipo/link). LockService en todas las escrituras. Sin mail |
| Env.js (SM) | 1.2 | SCHEMA.SHEETS.COMPRAS_INTERNAS='Compra de herramientas e insumos' + SCHEMA.COMPRAS_INTERNAS {FECHA_PEDIDO,DESCRIPCION,CANTIDAD,TIPO,PRECIO_UNITARIO,TOTAL,PEDIDO,LLEGO,LINK,ID} (A–I del usuario + J=ID interna) |
| SM_Index.html | 5.18 | Pestaña nueva "Compras internas" (nav nt-compint, tab-compint): lista de herramientas e insumos a pedir que cargan los operarios. Toolbar: "Agregar ítem" + buscador (descripción) + filtro Tipo (Herramienta/Insumo) + filtro Estado (Por pedir/Pedido/Llegó) + KPIs (ítems mostrados · por pedir · total est. $). Tabla con checkboxes inline ¿Pedido?/¿Llegó? (1 click, update optimista con rollback si falla), badge de Tipo, link seguro (solo http/https como <a>), editar/borrar. Modal alta/edición #modal-compra-interna con Total en vivo (cant×precio) y Enter para guardar. Fecha se completa sola al cargar. showTab('compint')→cargarComprasInternas. Funciones ci*: cargar/render/abrirNueva/abrirEditar/guardar/_ciToggle/_ciBorrar/_ciCalcTotal |
| Sm_Compras.js | 1.2 | FIX "Cobertura pedidos" en el detalle de un CAS (obtenerDetalleCAS): la demanda pendiente por SKU se leía de SOLICITUDES_DESPACHO (solo repuestos de OT de HUB PRO + ventas directas), que NO incluye los pedidos de resellers → el número de "N pend." estaba mal (le faltaba el canal principal y contaba solicitudes 'Pendiente' fantasma de OTs no finalizadas). Ahora la cobertura mide la demanda REAL de resellers desde Pedidos_resellers (WOS): pendiente por SKU = Σ max(0, CANT_SOL − CANT_DESP − CANT_CANCEL) — descuenta lo ya despachado y lo cancelado (misma fórmula que el mail de "Backorders desbloqueados", que ya usaba WOS). Se quitó la dependencia de SD/SOLICITUDES_DESPACHO en esa función |
| Sm_Stock.js | 1.2 | Espejo a Carmen (Carmen es la ÚNICA fuente de stock; SM lo lee de ahí): registrarEventoLedgerSeguro ahora, tras cada movimiento, escribe en Carmen (delta>0→Recibidos, delta<0→Entregados, vía _registrarMovimientoCarmen, mismo formato que compras/WOS) — cubre el motor "seguro" (hoy sin callers) para que a futuro también refleje en Carmen. Origen LEGIBLE = referencia/tipo + operador; ubicación tomada del propio ítem. ajustarInventario: origen de la línea de Carmen ahora "Ajuste: motivo · operador" (legible en el sheet). NO se tocan compras/ajuste/ventas (ya escribían Carmen) ni transferencias (neto 0) ni importación inicial (baseline). Equipos NO entran (Carmen es solo repuestos). (No hay reconciliación SM vs Carmen: no existen dos stocks, SM ya muestra Carmen) |
| Sm_Equipos.js | 1.0 | NUEVO módulo EQUIPOS EN DEPÓSITO (inventario de equipos completos por N° de serie, distinto del stock de repuestos). FASE 1: getEquiposDeposito() lista la hoja EQUIPOS_DEPOSITO (auto-creada); guardarEquipo(data) hace upsert por SN (valida SN único, alta fija fecha ingreso, no pisa la fecha en edición). Campos: SN, modelo, condición (Nuevo/Usado), destino (Venta/Harvesting/HV/Donante de partes), estado (Completo/Incompleto), ubicación, fecha ingreso, origen, valor, faltantes (nota texto), observaciones, situación (En depósito/Vendido/Baja). El "qué le falta" es nota de texto por ahora; Fase 2 = ledger de partes por equipo |
| Env.js (SM) | — | SCHEMA.SHEETS.EQUIPOS_DEPOSITO='EQUIPOS_DEPOSITO' + SCHEMA.EQUIPOS_DEPOSITO {SN,MODELO,CONDICION,DESTINO,ESTADO,UBICACION,FECHA_INGRESO,ORIGEN,VALOR,FALTANTES,OBSERVACIONES,SITUACION,FECHA_ACT,OPERADOR} |
| SM_Index.html | 5.17 | Pestaña nueva "Equipos" (nav nt-equip, tab-equip): inventario de equipos en depósito para conciliaciones. Toolbar con buscador (SN/modelo/origen) + filtros destino/estado/situación (default "En depósito") + Nuevo equipo + "Copiar para Excel" (TSV vía execCommand, pegás en Sheets) + contador de incompletos. Tabla con badges de destino y estado (Completo/Incompleto). Modal alta/edición (#modal-equipo) con datalist de modelos (getModelosActivos). showTab('equip')→cargarEquiposTab. Funciones: cargarEquiposTab/renderEquipos/filtrarEquipos/abrirEquipo/guardarEquipoUI/_copiarEquipos |
| SM_Index.html | 5.16 | Etiquetas SO: el SKU ya NO se tipea a mano (evita ceros de menos/typos). Buscador de repuesto tipo RS_Pedidos: se busca por código / nombre / modelo sobre stockData (que ya trae .modelos), dropdown de hasta 20 resultados (código + stock + descripción + modelos compatibles), click o Enter selecciona. Al elegir muestra tarjeta con SKU + descripción + "🛠 Compatible con: …" y habilita Imprimir (deshabilitado hasta elegir). confirmarImprimirSO exige un repuesto elegido (o match exacto de código) → _soSkuSel. Nuevos: _soBuscar/_soBuscarEnter/_soPick/_soFindStock/_soAsegurarStock (carga stockData en silencio si el modal se abre desde el botón general). Reemplaza el input libre + _soDesc |
| SM_Index.html | 5.15 | Operadores dinámicos: los <select> de operador (antes 3 nombres hardcodeados repetidos en 6 lugares) ahora se llenan desde Usuarios_Internos. Nuevo _llenarOperadores() rellena todo select.op-select con la lista de getOperadores() (fetch en window.onload), preservando el valor elegido; si el fetch falla quedan los <option> de fallback del HTML. Se agregó class op-select a #ajuste-op, #so-op, #cas-op, #est-cas-op, #venta-op y al #recep-parcial-op (dinámico, se rellena tras crear el modal). Agregar/sacar operadores = editar la hoja, no el código |
| Sm_Core.js | 1.2 | getOperadores() ahora filtra SOLO técnicos: incluye la fila únicamente si Usuarios_Internos col D (índice 3, esTecnico) == "si"/"sí". Devuelve {ok, operadores:[nombres]} únicos y ordenados (localeCompare es). Alimenta los selectores de operador del front |
| Sm_Etiquetas.js | 1.2 | BOLSAS de consumibles: nueva generarBolsasSO(sku, unidadesPorBolsa, cantBolsas, operador) genera 1 código SO por bolsa (cada uno representa N unidades), los registra en SO_ETIQUETAS con nueva col CANTIDAD (F) y devuelve labels con cantidad. _getHojaSO ahora asegura la col CANTIDAD (upgrade de hojas viejas de 5 cols). Además _smUpsertMaestro escribe en la MISMA hoja que WOS (MAESTRO_ARTICULOS en WOS_NOTAS_SS_ID) marcando el SKU como "por bolsa" + bulto → WOS lo reconoce solo. SM ya abría WOS_NOTAS_SS_ID (Dashboard/Compras) → sin re-auth. generarSO (por unidad) sin cambios |
| SM_Index.html | 5.14 | Modal "Imprimir etiquetas SO": toggle Por unidad / 🔩 Por bolsa (_soSetMode). En bolsa: "Unidades por bolsa" + "Cuántas bolsas" → generarBolsasSO; la etiqueta impresa muestra un badge amarillo "BOLSA xN u." (cell() usa l.cantidad). abrirImprimirSO resetea a modo unidad. confirmarImprimirSO ramifica por modo y avisa "N bolsas de X u. — SKU marcado por bolsa" |
| Sm_Etiquetas.js | 1.1 | El contador del SO ahora se rellena a 6 dígitos (SO-<SKU>-000041) en vez de 3. No trunca números > 6 dígitos. La detección del último NNN usa parseInt, así que convive con los SO viejos de 3 dígitos sin romper la secuencia |
| Sm_Etiquetas.js | 1.0 | NUEVO. Impresión de etiquetas SO para productos SIN N° de serie. generarSO(sku,cantidad,operador): formato POR SKU (SO-<SKU>-NNN), contador incremental por SKU con LockService, registra cada SO en la hoja SO_ETIQUETAS [SO,SKU,DESC,FECHA,OPERADOR] (auto-creada) para que el NNN nunca se repita; toma la descripción de STOCK/DB_REPUESTOS. Devuelve labels[{so,sku,descripcion}] |
| Env.js | — | SCHEMA.SHEETS.SO_ETIQUETAS = 'SO_ETIQUETAS' |
| SM_Index.html | 5.13 | Etiquetas SO: driver vuelto a 100×25mm → @page 100×25mm y 1 fila (2 etiquetas) por página (se sacó el wrapper .page de 2 filas). Se mantiene QR + gutter central |
| SM_Index.html | 5.12 | Etiquetas SO: se cambió Code128 → CÓDIGO QR (librería qrcodejs por CDN). Cada etiqueta: QR cuadrado de 21mm a la izquierda + SKU y SO en texto a la derecha (word-break). Más robusto con el lector 2D y sin el problema de que el barcode largo se corte a los lados. Se mantiene el layout 2 filas/página (100×50mm) con gutter central |
| SM_Index.html | 5.11 | Etiquetas SO: la columna derecha se corría a la izquierda (el papel tiene gap entre las 2 columnas). Se agregó gutter central (.row justify-content:space-between, celdas a 48mm → ~4mm de gap en el medio, la columna derecha se pega al borde derecho) y se angostó el barcode a 44mm para dar margen de tolerancia a la desalineación. Si aún se corre, ajustar el ancho de celda (más chico = más gutter) |
| SM_Index.html | 5.10 | Etiquetas SO: se adaptó al driver de la Xprinter configurado en 100×50mm (2 renglones). @page pasó a 100×50mm y ahora se imprimen 2 filas (4 etiquetas) por página → ya no salta un renglón en blanco. Barcode a 49×16mm (más ancho/alto), menos padding. Si el driver se pone en 100×25mm, hay que volver a 1 fila por página |
| SM_Index.html | 5.9 | Etiquetas SO: el barcode se renderiza como IMG (JsBarcode a canvas interno) con tamaño fijo 48×14mm + object-fit:fill → llena el ancho de la etiqueta de forma confiable en impresión (el truco de escalar SVG con preserveAspectRatio no estiraba parejo, quedaba ~60% centrado). Timeout de print subido a 700ms para que carguen las imágenes |
| SM_Index.html | 5.8 | Etiquetas SO: el barcode ahora LLENA el ancho de la etiqueta (preserveAspectRatio="none" + estilo inline 48mm×14mm) en vez de quedar chico y centrado. Bars sin texto (displayValue:false) y el SO va como texto legible debajo (monospace) + SKU arriba. Ajuste tras prueba física (imprimía 2-up OK pero chico) |
| SM_Index.html | 5.7 | Impresión de etiquetas SO. Modal #modal-so (SKU + cantidad + operador) → generarSO → _imprimirEtiquetasSO abre ventana con código de barras Code128 (JsBarcode CDN), 2 etiquetas por fila de 50×25mm (@page 100×25mm), SKU arriba + SO en el barcode, y manda a imprimir (window.print) a la Xprinter. Accesos: botón "Imprimir SO" en la barra de Stock (SKU libre/escaneable) + botón 🏷 por fila en la tabla (precarga el SKU). La ventana se abre en el gesto del click para no ser bloqueada. viewBox reescala el barcode al tamaño físico de la etiqueta |
| Sm_Stock.js | 1.1 | ETA en "unidades en camino": cargarStock arma casEtaMap desde COMPRAS_DJI.ETA y cada entrada de enCaminoCas ahora incluye eta (dd/MM/yyyy) → el tooltip del ✈ +N en la tabla de stock muestra "CAS: Nu (estado, ETA dd/MM/yyyy)" |
| Sm_Dashboard.js | 1.1 | ETA en el dashboard: casTransito incluye eta (panel "CAS en vuelo/aduana"); enCaminoProno/enCaminoInfo incluye eta (columna +Camino del pronóstico de quiebre). Ambos leen COMPRAS_DJI.ETA |
| SM_Index.html | 5.6 | Muestra el ETA de compras DJI en 3 lugares de stock/dashboard: (1) tooltip del ✈ +N en la tabla de Stock ("…, ETA dd/MM/yyyy"); (2) celda +Camino del pronóstico de quiebre (ícono plane-arrival + "ETA dd/MM"); (3) panel "CAS en vuelo/aduana" del dashboard (renderCASdashboard: badge "ETA dd/MM/yyyy" en lugar de la fecha de pedido cuando hay ETA cargada) |
| Sm_Compras.js | 1.1 | ETA (llegada estimada) para compras DJI. Nueva columna O (índice 14) ETA en COMPRAS_DJI (schema Env.js COMPRAS_DJI.ETA=14). cargarCompras devuelve eta (dd/MM/yyyy para mostrar) + etaISO (yyyy-MM-dd para el input date). actualizarEstadoCAS(cas, estado, obs, op, eta) acepta 5° parámetro opcional: si eta es 'yyyy-MM-dd' setea la fecha en col O, si es '' la limpia, si es undefined no toca (así recibirMercaderia y llamadas viejas no la pisan). Filas viejas sin ETA: f[14] undefined → '' |
| SM_Index.html | 5.5 | ETA en Compras DJI (frontend): input type=date "ETA — llegada estimada" en el modal Avanzar estado; abrirEstadoCAS(cas, estado, etaISO) precarga el valor y confirmarEstadoCAS lo envía como 5° arg a actualizarEstadoCAS (rama no-depósito); la tarjeta del kanban muestra "ETA: dd/MM/yyyy" con ícono plane-arrival, en rojo + "(vencida)" si la fecha ya pasó |
| Sm_Core.js | 1.0 | Split de Sm_Código.js: variables globales (MASTER_SHEET_ID, SM_CONFIG, Carmen singletons), Carmen helpers, utilidades base (getUserRole, _catValida, _fmtFecha, _registrarMovimiento, registrarMovimientoSeguro, _binSortKey, getModelosActivos/setModelosActivos/getModelosDisponibles, doGet, asegurarHojas) |
| Sm_Dashboard.js | 1.0 | Split de Sm_Código.js: cargarDashboard, obtenerTopRotacion, obtenerOTsBloqueadasConCAS, obtenerCASEnTransitoSM |
| Sm_Stock.js | 1.0 | Split de Sm_Código.js: stock CRUD (cargarStock, ajustarInventario, transferirStock, cargarBinsSKU), ledger engine (registrarEventoLedgerSeguro, reconciliarCacheDesdeMovimientos, obtenerSaldoRealEnMemoria), clasificación ABC, reservas, multi-depósito, calcularLeadTimeHistorico, calcularSlottingOptimo, revisarReabastecimientoAuto, TEST_generarValeMovimiento |
| Sm_WMS.js | 1.0 | Split de Sm_Código.js: cargarUbicacionesItem, guardarUbicacionInicial, cargarUbicacionesSector, eliminarUbicacion, SM_repararFormatoUbicaciones, SM_moverStock, SM_moverCaja, guardarConteoUbicacion, cargarSinMapear, SM_cargarLayout, SM_guardarLayout |
| Sm_Compras.js | 1.0 | Split de Sm_Código.js: cargarCompras, registrarCAS, actualizarEstadoCAS, recibirMercaderia, cruzarComprasExternas, sincronizarItemsCAS, calcularPedidoDJI, sincronizarCatalogoDJI, crearBorradorCompraAutomatico, revisarReabastecimientoAuto, getConfigAutoReabastecimiento |
| Sm_Ventas.js | 1.0 | Split de Sm_Código.js: parsearCarritoReseller, confirmarVentaDirecta, cargarVentas, completarFactura, generarNumeroOrden, obtenerResellers |
| Sm_Código.js | 5.0 | Convertido a stub: lógica distribuida en Sm_Core/Dashboard/Stock/WMS/Compras/Ventas.js |
| SM_Index.html | 5.4 | BIN_ID ahora 4 partes ESTANTE-PAÑO-ALTURA-CAJA (ej "1-A-2-3"); caja = contenedor físico móvil dentro del bin; RECIBIR y CONTAR agregan campo "CAJA #"; MOVER agrega caja para destino; Mapa: detalle de bin muestra cajas como tarjetas con qty editable inline, agregar SKU inline, "Mover caja" con picker de destino bin+caja; _mapaConstruirBinStock agrupa por 3-part key y trackea cajas; SM_moverCaja backend; CSS .det-caja-card |
| Sm_Código.js | 4.7 | SM_moverCaja(origenBin4, destinoBin4): mueve todos los SKUs de una caja completa a otro bin (col B de UBICACIONES), actualizando destinos existentes y eliminando filas de origen en orden inverso |
| SM_Index.html | 5.3 | Layout pasa a 3 niveles ESTANTE-PAÑO-ALTURA (ej "1-A-2"); editor reintroduce acordeón (estante=outer, paño=inner con stepper de alturas); mapa: rows=estantes, cols=paños únicos, celda agrega alturas con dots y total de stock; detalle de celda lista cada bin ESTANTE-PAÑO-ALTURA; _getAllBins genera "ESTANTE-PAÑO-ALTURA" |
| Sm_Código.js | 4.6 | Layout 3 niveles: SM_cargarLayout devuelve {estantes:[{estante,orden,panos:[{pano,orden,alturas}]}]}; SM_guardarLayout(estantes) escribe esquema [ESTANTE,ORDEN_ESTANTE,PAÑO,ORDEN_PAÑO,NUM_ALTURAS] celda por celda con setNumberFormat('@') en cols A y C (evita auto-fecha) |
| SM_Index.html | 5.2 | Fix bug raíz "todos los bins vacíos": _mapaConstruirBinStock leía item.bins (TABLA_POSICIONES, hoja legacy nunca escrita por el WMS actual) en vez de item.ubicaciones (tab UBICACIONES de Carmen, fuente real de Recibir/Contar/Mover); _wmsTabAbierto ahora llama _mapaConstruirBinStock apenas carga stockData (no solo al entrar a Mapa); todas las búsquedas de binKey uniformadas a .toUpperCase() |
| SM_Index.html | 5.1 | Layout sin pasillo: estructura simplificada a ESTANTE-NIVEL (ej "1-3"); editor reemplaza acordeón de pasillos por lista plana de estantes; mapa renderiza rows=estantes, cols=niveles; detalle de celda muestra bin individual; _getAllBins genera "ESTANTE-NIVEL" |
| Sm_Código.js | 4.5 | Fix fecha en layout: SM_guardarLayout fuerza col A a formato texto (@) + escribe celda por celda con setNumberFormat('@') — igual que fix UBICACIONES; SM_cargarLayout saltea filas donde el valor ESTANTE es Date (corruptas) |
| Sm_Código.js | 4.4 | Layout sin pasillo: SM_cargarLayout devuelve {estantes:[{estante,orden,niveles}]}; SM_guardarLayout(estantes) escribe esquema plano; asegurarHojas() actualiza headers LAYOUT_ALMACEN a [ESTANTE,ORDEN_ESTANTE,NUM_NIVELES] |
| SM_Index.html | 5.0 | WMS rediseñado: 3 tareas grandes (Recibir/Contar/Mover) reemplazan los modos abstractos "Por ítem"/"Por sector"; bin picker con autocomplete del layout en todos los campos de ubicación; RECIBIR con qty stepper y guardar en un click; CONTAR muestra ítems del bin con +/− inline y "Guardar conteo completo"; MOVER: buscar repuesto→chips de ubicaciones→bin destino→confirmar (SM_moverStock); layout se pre-carga en background al abrir WMS |
| Sm_Código.js | 4.3 | Fix fecha: guardarUbicacionInicial / guardarConteoUbicacion / SM_moverStock usan setNumberFormat('@') en col B de UBICACIONES — evita que Sheets auto-convierta "1-1" a fecha; SM_repararFormatoUbicaciones() para reparar filas ya corrompidas |
| Sm_Código.js | 4.2 | SM_moverStock(sku,origen,destino,cant): mueve unidades entre bins de UBICACIONES de Carmen atómicamente; guardarConteoUbicacion(ubicacion,items): reescribe el conteo completo de un bin (update+delete+insert en un solo paso) |
| SM_Index.html | 4.0 | Mapa aéreo del almacén: 4to botón "Mapa" en WMS toggle; panel wms-panel-mapa con grid por pasillo/estante; celdas por tipo (picking/sobrestock/cuarentena/mixto/vacío) con dots de niveles; click en celda abre detalle por nivel; modal Editor de Layout para definir pasillos, estantes y niveles de forma visual; SM_cargarLayout / SM_guardarLayout backend |
| Sm_Código.js | 4.1 | SM_cargarLayout(): lee LAYOUT_ALMACEN, arma estructura {pasillos:[{pasillo,estantes:[{estante,niveles}]}]} ordenada; SM_guardarLayout(pasillos): reescribe hoja LAYOUT_ALMACEN completa e invalida caché |
| SM_Index.html | 3.5 | Panel "Compras en tránsito" → "En vuelo / en aduana"; KPI idem; filtro solo muestra esos 2 estados |
| Sm_Código.js | 4.0 | cargarDashboard casTransito: filtro cambiado a CAS_ACTIVOS=['En vuelo','En aduana'] — ya no incluye Comprado/Pagado/Forwarder HK |
| SM_Index.html | 3.4 | Top rotación: segundo panel "por valor" ($); _renderTopRotList helper unifica render de ambos; cargarTopRotacion maneja nuevo retorno {cantidad, valor} |
| Sm_Código.js | 3.9 | obtenerTopRotacion: retorna {cantidad, valor} — ambos top-5 en una sola llamada; valor = suma CANT_SOL × PRECIO por SKU |
| SM_Index.html | 3.3 | renderOTTracking: muestra cantPend por SKU con badge naranja/verde; header de cada OT siempre tiene _wosBadge (wos nunca null) |
| Sm_Código.js | 3.8 | obtenerOTsBloqueadasConCAS: fuente cambiada a Pedidos_OTs WOS (en lugar de HUB OT estado=Espera de repuestos); obtenerTopRotacion: lee Pedidos_resellers+Pedidos_OTs WOS (en lugar de MOVIMIENTOS_STOCK), cuenta CANT_SOL por SKU en últimos 7 días |
| SM_Index.html | 3.2 | Dashboard: KPI y panel renombrados a "Pedidos WOS pendientes" — incluye Pedidos_resellers + Pedidos_OTs |
| Sm_Código.js | 3.7 | cargarDashboard: solicPendientes fusiona Pedidos_resellers + Pedidos_OTs del WOS (ambos tipos de pedido tratan igual al stock); SOLICITUDES_DESPACHO deprecada removida |
| SM_Index.html | 3.1 | Dashboard: panel "Solicitudes pendientes HUB" reemplazado por "Pedidos OT pendientes (WOS)" — lee Pedidos_OTs del WOS (estado, cantPend, diasEspera); renderOTTracking muestra badge de estado WOS por OT (_wosBadge) |
| Sm_Código.js | 3.6 | cargarDashboard: solicPendientes lee Pedidos_OTs del WOS en lugar de SOLICITUDES_DESPACHO (deprecada); SOLICITUDES removida del preload de hojas |
| SM_Index.html | 2.0 | Borrador: buscador usa catálogo completo DJI (1372 repuestos del sheet externo) en lugar de solo STOCK_REPUESTOS; muestra modelo y precio en resultados |
| SM_Index.html | 2.9 | WMS panel Sin mapear: tercer modo con lista de items con stock pero sin ubicacion, badge con conteo, click abre modo Por item pre-cargado |
| SM_Index.html | 3.0 | Cruce externo: nueva seccion Diferencias de items vs sheet externo (tabla SKU/SM/Externo, boton Usar externo sincroniza COMPRAS_DETALLE) |
| Sm_Código.js | 3.5 | obtenerOTsBloqueadasConCAS: agrega campo wos con estado de despacho leido de Pedidos_OTs (WOS); null si aun no generado en WOS |
| Env.js | 1.4 | Agrega WOS_NOTAS_SS_ID para que SM pueda leer Pedidos_OTs |
| Sm_Código.js | 3.4 | _alertarBackordersPendientes: reescrita — cruza SKUs del CAS contra WOS Pedidos_resellers (estado Backorder); cobertura basada en stock actual de STOCK_REPUESTOS (no en items del CAS); email muestra stock disponible vs pendiente por pedido |
| Sm_Código.js | 3.3 | cruzarComprasExternas: compara items de CAS no-deposito contra sheet externo, devuelve diferencias; sincronizarItemsCAS(): reemplaza items en COMPRAS_DETALLE preservando cantidades recibidas |
| Sm_Código.js | 3.2 | Fix stock WOS: elimina llamadas a _actualizarCarmenStock (sobreescribía la fórmula de Carmen col C); agrega restaurarFormulasCarmenStock() para reparar filas con número plano |
| Sm_Código.js | 3.1 | Despachos eliminados: funciones cargarSolicitudesDespacho, limpiarSolicitudesDeOTsCerradas, generarReportePendientesPDF, procesarDespacho, _actualizarEnviadosEnRep, _notificarDespacho removidas; todo el despacho pasa por WOS |
| Sm_Código.js | 3.0 | cargarSinMapear(): cruza Carmen STOCK vs UBICACIONES, devuelve hasta 200 items sin ubicacion ordenados desc por stock |
| SM_Index.html | 2.8 | Ingreso DJI: campo Destino WMS en modal detalle-cas y modal estado-cas; ubicacion se pasa a cada item al recibir |
| Sm_Código.js | 2.9 | _escribirEnRecibidos: escribe ubicacion en col I de Recibidos y suma cantidad a UBICACIONES tally |
| SM_Index.html | 2.7 | WMS modo sector: toggle Por item / Por sector; conteo rapido por rack con autocomplete y lista editable |
| Sm_Código.js | 2.8 | cargarUbicacionesSector(): devuelve todos los items de una ubicacion especifica |
| SM_Index.html | 2.6 | WMS: advertencia B cuando total asignado supera stock Carmen; barra de estado Asignado/Sin asignar/Todo asignado |
| Sm_Código.js | 2.7 | UBICACIONES sin formula: guardarUbicacionInicial y _registrarMovimientoCarmen usan numero directo en col C; STOCK col C de Carmen nunca se toca |
| SM_Index.html | 2.5 | WMS tab: tabla Ubicacion/Cantidad sin columna Inicial; edicion directa de cantidad |
| Sm_Código.js | 2.6 | WMS: cargarUbicacionesItem, guardarUbicacionInicial, eliminarUbicacion |
| SM_Index.html | 2.4 | Modal ubicaciones SKU: reemplaza cargarBinsSKU (TABLA_POSICIONES) por cargarUbicacionesItem (Carmen WMS) |
| SM_Index.html | 2.3 | Tab WMS: busqueda de repuesto, asignacion de ubicaciones con cantidad inicial, edicion inline, eliminacion |
| Sm_Código.js | 2.5 | WMS Opcion A: _registrarMovimientoCarmen() appendea a Entregados/Recibidos segun diff; asegura fila con formula SUMIFS en UBICACIONES; ajustarInventario ya no llama _actualizarCarmenStock (Stock Actual es formula en Carmen) |
| Env.js | 1.3 | Agrega CARMEN_ENTREGADOS_TAB y CARMEN_RECIBIDOS_TAB |
| Sm_Código.js | 2.4 | Multi-ubicación WMS en Carmen: _getCarmenUbicMap() + _actualizarCarmenUbicacion() leen/escriben tab UBICACIONES de Carmen; cargarStock y ajustarInventario redirigidos a Carmen |
| Env.js | 1.2 | Agrega CARMEN_UBICACIONES_TAB = 'UBICACIONES' |
| Sm_Código.js | 2.3 | cargarCatalogoBorrador(): agrega stockActual cruzando con Carmen (_getCarmenStockMap), sin precio |
| SM_Index.html | 2.2 | Pantallas de carga: spinner global con label dinámico, filas skeleton en stock y movimientos, texto de carga en despachos/compras/ventas, placeholder del borrador indica estado del catálogo |
| Sm_Código.js | 2.2 | cargarStock(): fuente primaria cambiada a Carmen STOCK (5428 ítems) en lugar de STOCK_REPUESTOS; STOCK_REPUESTOS sigue proveyendo metadata (mínimo, categoría, ubicación) |
| Sm_Código.js | 2.1 | cargarCatalogoBorrador(): lee sheet externo CATALOGO_REPUESTOS_ID, devuelve codigo/codigoCorto/descripcion/modelo/precio |
| Env.js | 1.1 | Agrega CATALOGO_REPUESTOS_ID con ID del sheet externo de catálogo DJI |
| SM_Index.html | 1.8 | Borrador de pedido: buscador interactivo con dropdown (código+descripción+stock), navegación por teclado ↑↓/Enter, igual al portal reseller |
| Sm_Código.js | 1.5 | cruzarComprasExternas: incluye items (cols D/E/F) en cada entrada nuevas |
| Env.js | 1.0 | |

## LAUNCHER
| Archivo | Versión | Notas |
|---------|---------|-------|
| Launcher_Index.html | 1.7 | Nueva fila de config "Reparar repuestos Portal ↔ HUB" (junto a PIN) → modal #modal-repfix con flujo revisar→aplicar: al abrir hace un preview (dry-run) que muestra cuántas OTs tienen el separador viejo '\n' en la col Repuestos + hasta 6 ejemplos antes/después (rojo/verde); el botón "Aplicar corrección" se habilita solo si hay algo que corregir y pide confirmación. Funciones repfix* (abrir/cerrar/Revisar/Aplicar/_repfixRender/_repfixSetApl/_repfixEsc) sobre LAUNCH_migrarRepuestosSeparador. Arregla de una pasada las OTs viejas del bug Portal→HUB (ver RS_OTs 1.11 / RS_Repuestos 1.8). Sin re-auth |
| Launcher_Código.js | 1.5 | LAUNCH_migrarRepuestosSeparador(aplicar): recorre la col Q (REPUESTOS) de 'Ordenes de trabajo' y reescribe las celdas que tengan salto de línea al separador canónico del HUB ' ; ' (split tolerante / ; \|\\r?\\n/ + trim + join). aplicar=false → solo cuenta/preview con ejemplos {ot,antes,despues}; aplicar=true → escribe. Soporta la migración de una pasada del bug Portal→HUB desde el Launcher. Sin re-auth |
| Launcher_Index.html | 1.6 | Card "Curso / Eventos · Portal Reseller" (junto a Campaña/PIN) que abre un modal de administración de eventos: selector de eventos + "Nuevo", formulario (Título, Fecha, Lugar, Descripción, Fecha límite, Activo/visible) que guarda con LAUNCH_saveEvento (upsert por ID), y sección de inscriptos con total de personas/resellers + lista por reseller con sus asistentes (nombre+email) vía LAUNCH_getEventoStats. La tarjeta muestra badge (N activos) + total de personas anotadas (cursoCargarLista en el arranque). Funciones curso*: abrir/cerrar/CargarLista/Nuevo/CargarEvento/Guardar/CargarStats + _cursoRenderStats/_cursoActualizarCard/_cursoEsc. Reemplaza la edición manual de la hoja EVENTOS |
| Launcher_Código.js | 1.4 | Administración de Curso/Eventos desde el Launcher (las hojas EVENTOS e INSCRIPCIONES_EVENTOS viven en el MASTER, mismas que RS_Eventos.js del Portal; scope 'spreadsheets' ya presente → sin re-auth): LAUNCH_getEventos() (lista con activo + nº personas/resellers por evento), LAUNCH_saveEvento(data) (upsert por ID, crea la hoja/ID si falta), LAUNCH_getEventoStats(eventoId) (total personas/resellers + lista por reseller con asistentes). Helper _launchAgruparInscripciones agrupa la hoja de inscripciones por evento→reseller |
| Launcher_Código.js | 1.3 | Config de ComandasPedidos editable desde el Launcher: nuevas constantes CP_LOG_SS_ID + CP_CONFIG_TAB, esquema CP_CFG_SCHEMA (15 claves con grupo/label/tipo/ayuda, refleja CP_CONFIG_DEFAULTS), y funciones LAUNCH_getComandasConfig() (lee la pestaña _CONFIG → schema+valores) / LAUNCH_setComandasConfig(data) (escribe SOLO las claves recibidas, por clave, sin clearContents → preserva claves ajenas; crea la hoja si falta). Usa el scope 'spreadsheets' ya presente → sin re-auth |
| Launcher_Index.html | 1.5 | Red de seguridad en el arranque: el overlay "Conectando con el sistema BIDCOMAGRO…" ya NO puede quedar pegado para siempre. load() ahora arma un timeout de 15 s (_ovTimer) que saca el overlay igual si obtenerKPIs no contesta, y ambos handlers (éxito/falla) lo limpian y cierran el overlay vía helper _cerrarOverlay(); en falla/timeout muestra "reintentando sola…" en el sello de fecha (el auto-refresh de 2 min reintenta). Diagnóstico: la config de Comandas es lazy (no corre en el load), así que no afecta el arranque |
| Launcher_Index.html | 1.4 | Card "Configuración · Comandas" (junto a la de WOS) que abre un modal con TODA la config de ComandasPedidos, generado dinámicamente desde el esquema del backend y agrupado (Automático / Destinatarios / Textos / Tiempos / Permisos): AUTO_MAIL_DESPACHO (SI/NO), EMAIL_PRUEBA, mails de aprobación/destinatarios/CC/CCO, asuntos y remitente, URL OCA, recordatorio + SLA, operadores autorizados y tope anti-abuso. Carga diferida al abrir (abrirModalComandas/cpCfgRender) y guardado por clave (cpCfgGuardar → LAUNCH_setComandasConfig) con feedback ✓/✗ |
| Launcher_Código.js | 1.2 | Baja/reactivación de resellers desde el modal PIN: helper _launchResellerInactivo (col Q, coincide con RS_Auth.js) + LAUNCH_getResellersConEstado (lista con activo) + LAUNCH_setResellerActivo(nombre, activo) escribe col Q (''=activo / 'NO'=baja) en todas las filas del nombre. Conserva PIN/email |
| Launcher_Index.html | 1.3 | Modal PIN·Portal Reseller: sección "Estado de la cuenta" con badge Activo/De baja y botón toggle Dar de baja/Reactivar (pinEstadoRefrescar/pinToggleEstado); select carga con LAUNCH_getResellersConEstado (mapa _resellerEstados, marca ⛔ los de baja); confirma antes de togglear |
| Launcher_Código.js | 1.1 | Default emailFact incluye lucia.c@bidcom.com.ar |
| Launcher_Index.html | 1.2 | HUB PRO card: boton Guia de flujos; Tools WOS: trigger backorder; campo emailFact acepta multiples |

## Raíz
| Archivo | Versión | Notas |
|---------|---------|-------|
| SharedLogger.js | 1.0 | |

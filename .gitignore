# Claude Code
.claude/

# Apps Script deploy metadata (no son código fuente)
**/.clasp.json
**/.claspignore
**/.deploy-hash

# Agentes IA
.ai_agents/

# Catálogo EPC — Excel fuente + imágenes extraídas (pesan cientos de MB, algunos
# superan el límite de 100MB de GitHub). Si hace falta versionarlos, usar Git LFS.
Explotadas/

# Node / misc
node_modules/
*.log
.DS_Store
Thumbs.db
